# 2026-06-20

## Ticket Listing Face Value/Admin Fee Validation
- Marc reported the seller listing flow showed an unclear warning when tickets could not be listed because face value was not confirmed, did not say when an official face value differed from the seller-entered value, had no field for original admin fees paid above face value, and did not clearly catch a purposely wrong event date.
- Added `Ticket.adminFeePaidCents` with migration `20260620103500_add_ticket_admin_fee_paid`.
- Added shared listing validation that distinguishes:
  - official event date mismatch,
  - official event detail mismatch,
  - event not confirmed,
  - official face value missing,
  - seller-entered face value differing from official face value,
  - list price above confirmed face value plus admin fees paid.
- Seller listing form now collects face value, list price, and admin fees paid, then shows face value plus admin fees paid as the maximum allowed list price.
- API create/edit, auto verification, official-market sync, ticket search/detail, and debug confirmation log were updated to respect the fee-inclusive fair-price ceiling.
- Verification run: focused Jest listing-validation test passed, `npm run typecheck` passed, and `npm run build` passed with only the existing multiple-lockfile workspace-root warning.
- Deployed commit `2c1cee5` (`Clarify listing face value validation`) to `dev` and pushed to GitHub. Vercel deployment `https://truefantix-n4myetsoh-marc-villeneuves-projects.vercel.app` became Ready and is aliased to `https://truefantix-web.vercel.app`.
- Applied Neon migration `20260620103500_add_ticket_admin_fee_paid` successfully with `npx dotenv -e .env.local -- npx prisma migrate deploy`.
- Live checks passed: `/api/health`, `/api/health/verification`, and `/api/tickets?status=AVAILABLE&take=1` returned healthy/ok; ticket API response includes `adminFeePaidCents`.

## Easier Listing Time Selection
- Marc reported that selecting event minutes was difficult when the desired value was `00`.
- Updated the seller listing form to replace the create-listing `datetime-local` picker with explicit controls: event date, hour, minutes, and AM/PM. Minutes default to `00` and offer 5-minute increments.
- Added a `Set minutes to 00` helper button to the edit-listing datetime picker for existing active listings.
- Verified `npm run typecheck`, page-specific ESLint (warnings only), and `npm run build`.
- Committed `bd61467` (`Make listing event time easier to select`) and pushed to `dev`. Vercel deployment `https://truefantix-ehmkwkilv-marc-villeneuves-projects.vercel.app` became Ready and is aliased to `https://truefantix-web.vercel.app`.
- Live `/api/health` check passed after deploy.

## Listing Receipt Proof Requirement
- Marc reported the face-value warning still dead-ended when official face value was missing, and asked for the error to show the app-found face value/service fees and require proof of purchase confirming event, fees, and ticket possession.
- Seller create-listing now requires an image/PDF receipt proof upload plus an explicit seller confirmation checkbox before listing.
- The listing API now returns structured `pricingConfirmation` details on validation failures, including official face value found, seller-entered receipt face value, service fees paid, max list price, and whether proof/confirmation is missing.
- If the official event is confirmed but official face value is unavailable, a seller receipt-confirmed face value can now be used as the listing cap source. The cap remains face value plus service fees/admin fees paid.
- Receipt proof is currently stored in the existing `Ticket.verificationImage` field as a small data URL for fast testing; search responses strip that raw field so receipts are not returned publicly.
- Verified focused listing-validation Jest tests, targeted ESLint (warnings only), `npm run build`, and live deployed chunk text on `https://truefantix-web.vercel.app`.
- Committed `585b390` (`Require receipt proof for listings`) and pushed to `dev`; Vercel reported success for `truefantix-web`.

## Tickets Page List Tickets CTA
- Marc asked for a `List Tickets` button on the Tickets page.
- Added a primary `List Tickets` CTA to `/tickets`, linking to `/account/tickets/selling`.
- Verified page-specific ESLint (warnings only), `npm run typecheck`, Vercel success for commit `7817e1b`, live `/api/health`, and live `/tickets` HTML containing the button/link.

## Tightened Official Listing Validation
- Marc found that after receipt upload was added, sellers could still lie about both face value and service fees, and could list a ticket with the wrong venue.
- Removed automatic seller-receipt face-value fallback: listing now requires official face value from the official ticketing source; seller-entered receipt values cannot override missing official pricing.
- Service/admin fees above face value are no longer allowed to increase the automatic listing cap until receipt fee verification/manual review support exists. The current automatic cap is official face value only.
- Ticketmaster event matching now verifies the submitted venue name against the official Ticketmaster venue name, not just city/date/title. Wrong venue returns `OFFICIAL_EVENT_VENUE_MISMATCH` and includes the official venue name when found.
- Added focused tests for listing validation and Ticketmaster venue matching.
- Committed `75802d9` (`Tighten official listing validation`) to `dev`; Vercel succeeded. Live checks passed for `/api/health` and deployed seller-page chunk containing the new fee-verification copy.

## Listing Source Review UX
- Marc asked to remove the seller-facing `Barcode payload` and `Barcode type` fields from the List Tickets form while keeping backend support for future scanners/admin/mobile parsing.
- Removed those fields from the seller form only; `/api/tickets` still accepts `barcodeData` and `barcodeType`.
- Marc also asked for warnings to be source-specific, showing differences from Ticketmaster and receipt, with buttons to accept source values.
- Listing validation now returns structured `sourceIssues` for Ticketmaster/receipt differences. The seller form shows Ticketmaster face value, venue, date, source link, per-field entered/source values, and buttons such as `Use venue`, `Use face value`, `Use max price`, and `Set fees to $0`.
- Receipt OCR/parsing is not wired yet, so receipt service fees are honestly shown as `Not automated yet`; fee uplift still requires future receipt verification/manual review.
- Committed `ba9b0ca` (`Improve listing source mismatch review`) to `dev`; Vercel succeeded. Live checks passed for `/api/health`, source-review text present in deployed seller-page chunk, and old barcode labels absent from deployed chunks.

## Automated Receipt OCR Review
- Marc asked to automate receipt review for ticket purchase proof, face value, service fees, seating, event info, and number of tickets.
- Added `src/lib/tickets/receiptOcr.ts`, which uses OpenAI Responses image input + structured JSON output to extract receipt fields from an uploaded receipt image/screenshot.
- Listing creation now runs receipt OCR before validation and stores the OCR result in `verificationEvidence.receiptProof.ocr`.
- Listing validation now blocks if OCR is unavailable/unsupported, if the upload does not clearly show a purchase receipt/tickets, or if OCR-confirmed receipt fields do not match seller-entered title, venue, date, time, quantity, row/seat, face value, or service fees.
- Service/admin fees can now raise the automatic max list price only when OCR confirms the same per-ticket fee amount from the receipt.
- Seller form now asks for image/screenshot receipts, not PDFs, because the automated OCR path uses image input. Older PDF uploads are rejected as unsupported by OCR.
- Deployed commit `e926092` (`Add automated receipt OCR review`) to `dev`; Vercel succeeded. Live `/api/health` passed and deployed seller-page chunk contains the new receipt-image/OCR UI text.
- Important env requirement: `OPENAI_API_KEY` was missing from local `.env.local`; dev/prod Vercel must have `OPENAI_API_KEY` set for receipt OCR to work. Optional model env: `OPENAI_RECEIPT_OCR_MODEL`; default is `gpt-5.5`.

## Official Source Pricing And Sold-Out Status
- Marc asked to automatically confirm ticket price/service fees directly from sourced websites such as Ticketmaster and determine if the event is sold out.
- Extended `OfficialSnapshot` to carry Ticketmaster price range min/max, official service fees when available, official event status code, sold-out source, and sold-out boolean.
- Ticketmaster Discovery currently provides event price ranges and event status, but not checkout/service fees in this integration; the UI now says `Not provided by Ticketmaster` instead of implying service-fee automation is missing.
- Listing validation exposes source price range, source service fees, event status, and sold-out status in `pricingConfirmation`. If a future official source provides service fees, mismatches are now shown as `OFFICIAL_SERVICE_FEES_MISMATCH`.
- Create/edit listing and official-market sync evidence now stores source price range, service-fee availability, verified service fees, event status code, and sold-out source.
- Official-market sync no longer uses seller-entered admin fees when enforcing caps unless those fees match official source fees or receipt OCR fees.
- Seller warning panel now shows Ticketmaster face value, price range, service fees, sold-out status, venue, event date, max list price, and source issue action buttons.
- Verified focused Jest tests (`listing-validation`, `official-pricing`), `npm run typecheck`, targeted ESLint (warnings only), and `npm run build`.
- Committed `120bc95` (`Expose official source pricing status`) to `dev` and pushed. Live `https://truefantix-web.vercel.app` `/api/health` is healthy; deployed seller-page JS contains the new Ticketmaster service-fee/sold-out labels and no barcode labels.

## Listing Warning Review Persistence
- Marc found that after entering several wrong listing fields, clicking one source-fix button cleared all warnings from the List Tickets form.
- Root cause: `applySourceIssue` updated one form field but cleared the entire `pricingConfirmation` state and `error`, and the warning panel was rendered only when `error` was truthy.
- Fixed seller form so applying one source issue removes only that matching issue from `pricingConfirmation.sourceIssues`; remaining warnings stay visible. The panel also renders when either `error` or `pricingConfirmation` exists.
- If the last issue is fixed and receipt confirmation is not still required, the panel clears normally.
- Verified `npm run typecheck`, `npx eslint src/app/account/tickets/selling/page.tsx` (warnings only), and `npm run build`.
- Committed `56658fc` (`Keep remaining listing warnings visible`) to `dev` and pushed. Vercel deployment `https://truefantix-ekx9b51vs-marc-villeneuves-projects.vercel.app` is Ready and aliased to `https://truefantix-web.vercel.app`; live seller-page JS contains the new remaining-warning message.
- Follow-up: Marc reported that manually correcting fields still left stale warnings visible and that price above face value + service fees did not show when official face value was unavailable.
- Added `clearSourceIssuesFor` on the seller form so manual edits remove only the affected warning group: title, venue, quantity, seating, date, time, list price, face value, or service fees.
- Backend validation now adds a receipt-sourced `PRICE_ABOVE_RECEIPT_FACE_VALUE_WITH_FEES` issue when receipt OCR verifies face value/service fees and the list price exceeds that receipt cap, even if Ticketmaster has not provided official face value yet.
- Added regression test for receipt price-cap warnings when official face value is unavailable.
- Verified focused listing-validation Jest test, `npm run typecheck`, targeted ESLint (warnings only), and `npm run build`.
- Committed `fdd8fbb` (`Clear corrected listing warnings by field`) to `dev` and pushed. Vercel deployment `https://truefantix-i5neqe8t6-marc-villeneuves-projects.vercel.app` is Ready and aliased to `https://truefantix-web.vercel.app`.

## Ticketmaster Venue Parsing And GA Receipt Seating
- Marc showed Daniela Andrade at The Mod Club on Ticketmaster, but TrueFanTix returned `OFFICIAL_EVENT_NOT_CONFIRMED` and also flagged General Admission seating as a receipt mismatch.
- Root cause for Ticketmaster: venue strings like `The Mod Club - Toronto, ON` were parsed as venue name `The Mod Club - Toronto` and city `ON`, causing the Discovery API query to search the wrong city and reject the official event.
- Fixed `venueCity`/`venueName` parsing to handle `Venue - City, Province` format and improved fallback web date matching for human-readable dates like `Jun 20, 2026`.
- Root cause for GA receipt warning: receipt seating comparison required exact row/seat match and did not treat `General Admission`, `GA`, standing/floor, or missing row/seat details as valid for GA listings.
- Fixed receipt seating comparison so GA listings pass when the receipt has no specific seats or has GA/standing/floor indicators.
- Added regression tests for Ticketmaster hyphenated venue-city matching and GA seating with no receipt row/seat details.
- Verified focused official-pricing/listing-validation Jest tests, `npm run typecheck`, targeted ESLint (warnings only), and `npm run build`.
- Committed `1e4557e` (`Fix primary source event matching`) to `dev` and pushed. Vercel deployment `https://truefantix-c423epxyb-marc-villeneuves-projects.vercel.app` is Ready and aliased to `https://truefantix-web.vercel.app`.

## Receipt Pricing Fallback Policy
- Marc clarified policy: ticket pricing and service fees should be checked against both Ticketmaster and the receipt. If Ticketmaster has no pricing/no match but receipt OCR confirms pricing and fees, the listing should pass. It should fail only when there is a conflict or missing receipt proof.
- Updated listing validation so receipt-confirmed face value/service fees can be the accepted pricing source when Ticketmaster has no price data or no event match.
- Ticketmaster conflicts still block: date/title/team/venue conflict reasons remain hard failures.
- If Ticketmaster supplies a face value or service-fee value and it conflicts with seller/receipt values, validation still fails with the source mismatch.
- Added tests for receipt pricing fallback, no-Ticketmaster-match receipt fallback, and Ticketmaster conflict blocking.
- Verified focused listing-validation Jest test, `npm run typecheck`, targeted ESLint (clean for touched files), and `npm run build`.
- Committed `dcd2b21` (`Allow receipt pricing when source is missing`) to `dev` and pushed. Vercel deployment `https://truefantix-1haywthkq-marc-villeneuves-projects.vercel.app` is Ready and aliased to `https://truefantix-web.vercel.app`.

## Per-Ticket Pricing Labels
- Marc asked to add `per ticket` after List price, Face value, and Admin fees paid labels.
- Updated the List Tickets create form labels: `List price per ticket (dollars)`, `Face value per ticket (dollars)`, and `Admin fees paid per ticket above face value (dollars)`.
- Updated active listing edit labels: `Price per ticket`, `Face value per ticket`, and `Admin fees paid per ticket`.
- Updated the list-price preview label to `List price per ticket`.
- Verified `npm run typecheck`, `npx eslint src/app/account/tickets/selling/page.tsx` (warnings only), and `npm run build`.
- Committed `4875678` (`Clarify ticket pricing labels`) to `dev` and pushed. Vercel deployment `https://truefantix-cwmjk8mpw-marc-villeneuves-projects.vercel.app` is Ready and aliased to `https://truefantix-web.vercel.app`.
