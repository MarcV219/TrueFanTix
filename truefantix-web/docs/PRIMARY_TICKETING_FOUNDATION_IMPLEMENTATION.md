# Primary Ticketing Foundation Implementation

**Branch:** `feat/primary-ticketing-foundation`

**Scope:** Section 13 narrow foundation only

**Status:** Ready for implementation review; disabled by default

## Implemented

- A server-only feature preflight that fails closed unless all of the following are true:
  - `PRIMARY_TICKETING_ENABLED=true`;
  - `PRIMARY_TICKETING_ENVIRONMENT_ID` is `isolated-test` or `isolated-preview`;
  - `PRIMARY_TICKETING_DEPLOYMENT_ID` exactly matches that isolated identity;
  - live production deployment identity (`VERCEL_ENV=production` or `PRIMARY_TICKETING_DEPLOYMENT_ID=live-production`) is forbidden;
  - an isolated preview requires `VERCEL_ENV=preview` (and may use the platform-standard `NODE_ENV=production`);
  - `PRIMARY_TICKETING_DATABASE_URL` exactly matches the process `DATABASE_URL`; and
  - the PostgreSQL database name explicitly identifies a primary-ticketing test or preview database.
- An unavailable-by-default `/api/primary/health` route. Disabled or unsafe configurations return the same `404 NOT_FOUND` response.
- Dedicated organizer, membership, invitation, minimal event identity, event-staff assignment, audit, and outbox models. They have no relation to secondary `Ticket`, `Order`, `Payment`, `Payout`, or `Seller` records.
- Composite tenant foreign keys prevent an event assignment or event audit record from crossing organizer boundaries.
- Deny-by-default organizer and event authorization services. Every tenant query includes `organizerId`; non-owner event access also requires an active assignment belonging to the same organizer.
- A transactional helper that writes a redacted audit record and idempotent outbox message through the caller's existing database transaction. It requires an opaque capability issued only by successful primary preflight, so a caller cannot omit the gate and still write.

## Isolated environment contract

These variables are examples only; no credential or environment file is committed:

```dotenv
NODE_ENV=test
PRIMARY_TICKETING_ENABLED=true
PRIMARY_TICKETING_ENVIRONMENT_ID=isolated-test
PRIMARY_TICKETING_DEPLOYMENT_ID=isolated-test
DATABASE_URL=postgresql://<isolated-user>:<password>@<isolated-host>/primary_ticketing_test
PRIMARY_TICKETING_DATABASE_URL=postgresql://<isolated-user>:<password>@<isolated-host>/primary_ticketing_test
```

The migration must be applied only to a newly provisioned isolated database. It must not be run against production, the existing TrueFanTix development database, or a database containing real users or organizers.

## Verification procedure

1. Provision a disposable PostgreSQL database named to match the preflight rule, such as `primary_ticketing_test`.
2. Apply the repository's existing migrations to that empty database with `prisma migrate deploy`.
3. Apply `20260910123000_add_primary_ticketing_foundation` using the same command.
4. Run the focused primary tests, full test suite, TypeScript, lint, and production build without enabling the feature.
5. Verify a synthetic cross-organizer event assignment fails at the composite foreign key.
6. Destroy the disposable database after recording non-secret results.

## Verification results — 2026-09-10

- Prisma format, schema validation, and client generation: passed.
- Clean migration deployment: all 36 repository migrations applied successfully to a newly created local PostgreSQL 16 `primary_ticketing_test` database; `prisma migrate status` reported the schema up to date.
- Database tenant constraints: PostgreSQL rejected both a synthetic cross-organizer event assignment and a synthetic cross-organizer event audit.
- Focused foundation tests: 4 suites / 14 tests passed.
- Full regression tests: 55 suites / 337 tests passed.
- TypeScript: passed with no errors.
- Focused foundation lint: passed with no warnings or errors.
- Full lint: passed with 0 errors and 619 pre-existing warnings.
- Production build: passed with primary ticketing disabled and the disposable database supplied to the build environment.
- Diff whitespace validation: passed.

### Foundation Revision 1

- Focused preflight, write-boundary, authorization, and health tests: 4 suites / 19 tests passed.
- Full regression tests: 55 suites / 342 tests passed.
- TypeScript and focused lint: passed with no errors.
- Explicit deployment-identity coverage proves an isolated Vercel preview is accepted with `NODE_ENV=production`, while `VERCEL_ENV=production`, a live-production identity, mismatched identities, and an unverified preview are rejected.
- Write-boundary coverage proves audit/outbox persistence is rejected before either database create call unless the helper receives a capability issued by successful preflight.

## Organizer Domain Services milestone

The service-only `PrimaryOrganizerService` implements the authorized organizer administration boundary with no routes or UI:

- verified-user organizer draft creation and the first active `OWNER` membership in one serializable transaction;
- owner submission and platform-admin review, approval, rejection, suspension, reopening, and restoration using explicit state transitions;
- normalized-email invitations with cryptographically random bearer tokens, peppered SHA-256 database hashes, expiry, single-active-invitation checks, matching verified-account acceptance, and revocation;
- owner-controlled membership role changes and revocation, with organizer-row locking and a final-active-owner invariant;
- owner-controlled event assignment and revocation against the existing tenancy-only event shell;
- tenant-scoped lookups that conceal foreign identifiers; and
- a protected audit and idempotent outbox record in the same transaction as every successful sensitive mutation.

The PostgreSQL integration suite uses only synthetic users on a disposable isolated database. It covers the organizer lifecycle, suspension behavior, invitation matching/revocation, role changes, membership revocation, event assignment/revocation, final-owner protection, unverified-user rejection, cross-tenant rejection, and matching audit/outbox counts. It is opt-in through `PRIMARY_INTEGRATION_DATABASE_URL`; ordinary test runs skip it rather than connecting to an unapproved database.

Verification on 2026-09-10 used a newly provisioned disposable PostgreSQL 16 database named `primary_ticketing_test`: all 36 migrations deployed cleanly; the 2 database-backed organizer integration tests passed; all 56 suites / 344 tests passed with integration enabled; TypeScript and the production build passed; focused lint passed; full lint completed with 0 errors and 619 pre-existing warnings; and `git diff --check` passed. The disposable database was destroyed after verification.

### Foundation Revision 2 hardening

- Organizer submission and admin review transitions acquire a PostgreSQL row lock before reading the current state. Concurrent conflicting transitions cannot both commit or emit audit/outbox records.
- Platform `ADMIN` bypass remains available only to explicit platform review/suspension operations. Every OWNER-only invitation, membership, and event-assignment service call passes `allowPlatformAdmin: false` and requires a current active OWNER membership after the organizer lock is acquired.
- Suspension blocks invitation creation and acceptance, invitation revocation, membership role/revocation changes, and event-assignment changes. Only the defined platform review/restoration path remains available while suspended.
- Review, invitation revocation, membership change/revocation, and assignment revocation require a trimmed non-empty reason before mutation.
- The PostgreSQL suite now separately proves conflicting-transition serialization; concurrent final-owner protection; ADMIN denial across all OWNER-only mutation families; banned and stale-role denial; invitation duplicate, expiry, replay, and suspended-acceptance behavior; reason enforcement; cross-tenant denial; and complete rollback with no audit/outbox residue when protected persistence fails.
- Revision 2 verification on a newly provisioned disposable PostgreSQL 16 database: all 36 migrations applied; all 8 organizer integration tests and the complete 56-suite / 350-test run passed; TypeScript, production build, focused lint, full lint with 0 errors and 619 pre-existing warnings, and `git diff --check` passed. The database/container was removed after verification.

## Draft Event Authoring Services milestone

- `PrimaryEvent` now contains required draft identity, description/category, inline venue/address, local start/end, IANA timezone, accessibility/contact, and draft-policy fields.
- Its only states are `DRAFT`, `SUBMITTED`, `UNDER_REVIEW`, `APPROVED`, and `REJECTED`; there is no public/published state.
- An active OWNER of an approved, non-suspended organizer may create and submit drafts. Submission accepts `DRAFT` only and revalidates the complete persisted event snapshot while the organizer and event rows are locked. Active OWNERs and active assigned EVENT_MANAGERs may edit in `DRAFT` or `REJECTED`; editing a rejected draft returns it to `DRAFT` and clears the rejection reason before it may be resubmitted.
- Platform admins may review submitted events but cannot create, edit, or submit them through organizer permissions. Review transitions lock both organizer and event rows and run serializably.
- Every successful create, edit, submit, or review transition writes its redacted event audit and outbox intent in the same transaction.
- ISO local inputs intentionally contain no UTC offset. They are stored as PostgreSQL wall-clock timestamps and paired with a validated IANA timezone. Conversion to an admission/sales instant, including explicit ambiguous/nonexistent DST-time handling, is deferred and must be resolved before publication or sales is authorized.
- Migration `20260910151000_add_primary_event_authoring` backfills any synthetic shell row with inert placeholders solely so the required-column migration is deployable. Such a row cannot pass service validation or submission until edited with complete valid draft data.
- Verification on 2026-09-10 used a newly provisioned disposable PostgreSQL 16 `primary_ticketing_test` database: all 37 migrations applied and Prisma reported the schema current; all 6 event tests plus the existing 8 organizer tests passed; the complete integration-enabled run passed 57 suites / 356 tests; Prisma format/validation/generation, TypeScript, production build, focused lint, full lint with 0 errors and 619 pre-existing warnings, and diff checks passed. The disposable database/container was removed afterward.

### Event Revision 1 hardening

- Submission revalidates the locked database snapshot rather than trusting earlier create/edit validation. A PostgreSQL test proves a migrated placeholder row remains `DRAFT` and leaves no audit/outbox residue after failed submission.
- `REJECTED` events cannot be submitted directly. The lifecycle test proves rejection must be followed by a valid edit back to `DRAFT` before resubmission.
- Event-service PostgreSQL coverage separately proves unverified, banned, and stale-role actors cannot create events and leave no event, audit, or outbox writes.
- Revision 1 verification on a newly provisioned disposable PostgreSQL 16 database: all 37 migrations applied and Prisma reported the schema current; all 9 event tests plus the existing 8 organizer tests passed; the complete integration-enabled run passed 57 suites / 359 tests; Prisma format/validation/generation, TypeScript, production build, focused lint, full lint with 0 errors and 619 pre-existing warnings, and `git diff --check` passed. The disposable database/container was removed afterward.

## GA Capacity and Ticket-Type Foundation

- `PrimaryEvent.totalCapacity` is a positive integer allocation ceiling. It is mutable only while the event is in the editable draft workflow and does not represent on-sale inventory or availability.
- `PrimaryTicketType` is isolated from secondary marketplace tickets and stores only general-admission draft configuration: organizer/event tenancy, name/optional description, positive allocation, `ACTIVE | INACTIVE`, optional positive per-order limits, one recognized ISO currency, and a positive integer face-value amount in minor units.
- Capacity and ticket-type mutations run serializably and lock organizer, event, and ticket-type rows in that order. Active allocations are summed inside the transaction; inactive allocations do not consume the event ceiling. Active types for an event must share one currency.
- OWNERs and active assigned EVENT_MANAGERs may mutate these draft records for approved, non-suspended organizers. Platform-admin bypass, cross-tenant access, unassigned managers, and post-draft mutation are denied.
- Event submission now requires a valid positive capacity and at least one active, positively allocated ticket type. This is configuration readiness only and makes no public sale, reservation, or availability claim.
- `basePriceMinor` is draft face value only. It is not an all-in price and does not calculate or promise fees, tax, processor costs, organizer proceeds, settlement, or refunds.
- Migration `20260910154500_add_primary_ga_capacity_ticket_types` backfills existing synthetic events with capacity `1`, removes the default for new events, creates the isolated ticket-type table, and enforces positive integer money/allocation/order-limit and currency-shape checks at the database layer.
- Verification on 2026-09-10 used a newly provisioned disposable PostgreSQL 16 `primary_ticketing_test` database: all 38 migrations applied and Prisma reported the schema current; all 7 GA ticket-type, 9 event, and 8 organizer PostgreSQL tests passed; the complete integration-enabled run passed 58 suites / 366 tests; Prisma format/validation/generation, TypeScript, production build, focused lint, full lint with 0 errors and 619 pre-existing warnings, and `git diff --check` passed. The disposable database/container was removed afterward.

## GA Reservation Foundation

- `PrimaryInventoryReservation` is isolated from secondary tickets/orders and contains only organizer/event/ticket-type tenancy, an authenticated verified buyer reference, positive quantity, lifecycle state/timestamps, and command idempotency keys. It has no cart, order, quote, payment-provider, or credential fields.
- The only states are `HELD`, `PAYMENT_COMMITTED`, `RELEASED`, and `EXPIRED`. Create uses the injected server-owned clock and a ten-minute hold TTL. Commit records `paymentCommittedAt` and `reconciliationAfter`; no provider call exists.
- Reservable ticket-type quantity is `allocatedQuantity - sum(unexpired HELD) - sum(PAYMENT_COMMITTED)`. The same committed quantities summed across all event types may not exceed event capacity. Expired-by-clock HELD rows no longer consume capacity even before the explicit idempotent expiry transition records their terminal state.
- Every mutation runs serializably with one lock order: organizer, event, every event ticket type ordered by ID, then every event reservation ordered by ID. This serializes last-unit holds and cross-ticket-type event-capacity contention.
- Holds require a current authenticated, verified, non-banned non-admin user; an approved non-suspended organizer; an `APPROVED` event; an `ACTIVE` ticket type; a positive quantity; and the type's optional minimum/maximum order limits. Foreign tenant/type identifiers are concealed.
- Create, release, expire, and commit commands have unique idempotency keys. Exact replay returns the existing result without duplicate audit/outbox writes; changed create input with the same key is rejected.
- `PAYMENT_COMMITTED` and expiry are internal test-only operations protected by a module-private opaque capability that can be issued only when the actual process is an `isolated-test` test runtime. PAYMENT_COMMITTED rows cannot be expired automatically and continue consuming capacity beyond both hold expiry and reconciliation timing.
- Every successful lifecycle transition records a protected audit/outbox pair in the same transaction. Payloads include only organizer/event/type/reservation IDs, quantity, and state—never buyer identity, contact, price, or payment data.
- Migration `20260910160000_add_primary_ga_reservations` adds only the four-state reservation table, positive-quantity/state-metadata constraints, tenant foreign keys, lifecycle indexes, and unique command keys.
- Verification on 2026-09-10 used a newly provisioned disposable PostgreSQL 16 `primary_ticketing_test` database: all 39 migrations applied and Prisma reported the schema current; all 8 reservation, 7 GA ticket-type, 9 event, and 8 organizer PostgreSQL tests passed; the complete integration-enabled run passed 59 suites / 374 tests; Prisma format/validation/generation, TypeScript, production build, focused lint, full lint with 0 errors and 619 pre-existing warnings, and `git diff --check` passed. The disposable database/container was removed afterward.

## Primary Order and Price-Snapshot Foundation

- One provider-free `PrimaryOrder` binds one authenticated buyer and exactly one reservation. Composite foreign keys prove the order's organizer/event/buyer match that reservation, and the sole line's reservation/ticket-type pair proves it snapshots the reserved type. Direct cross-tenant, cross-event, cross-buyer, and wrong-ticket inserts fail in PostgreSQL.
- Immutable component rows snapshot an automatic `FACE_VALUE` component plus optional internally configured, explicitly coded `MANDATORY_FEE` or `TAX` test components. No permanent TrueFanTix fee or tax policy is encoded.
- All money and quantities use positive PostgreSQL `INTEGER` values, with an explicit maximum of `2,147,483,647`. The service rejects any input or derived subtotal/gross total above that bound before writing. PostgreSQL evaluates `quantity × unitFaceValueMinor` as `BIGINT` inside its check to avoid constraint-expression overflow. `faceValueSubtotalMinor = quantity × unitFaceValueMinor`; `grossTotalMinor = sum(all component amounts)`.
- Per-unit allocation is deterministic: `allocationBaseMinor = floor(componentAmount / quantity)` and the first `allocationRemainderUnits = componentAmount mod quantity` future units receive one additional minor unit. This always reconciles exactly without floating point.
- Creation requires a current verified non-admin buyer owning an unexpired `HELD` reservation in an approved/non-suspended organizer, approved event, and active recognized-currency ticket type. Exact replay is idempotent; changed bindings conflict; concurrent one-order attempts serialize.
- The only order states are `PENDING_PAYMENT` and `PAYMENT_PROCESSING`. Preparation persists the exact idempotency key and reconciliation delay; replay must match order, organizer/event scope, and delay or returns `IDEMPOTENCY_CONFLICT` without extra audit/outbox writes. The transaction commits the reservation before advancing the order. No provider-capable action exists.
- PostgreSQL triggers reject changes to order financial identity/totals and every UPDATE or DELETE of line/component evidence. Only the narrowly permitted order status, preparation metadata/timestamp, and `updatedAt` fields may transition. The eventual runtime role must also lack `TRUNCATE`; migrations and disposable-test cleanup remain owner-only operations.
- Migration `20260910162000_add_primary_order_snapshots` adds the provider-free records. Follow-up migration `20260910163500_harden_primary_order_snapshots` adds composite tenancy/type integrity, preparation-command metadata, aligned integer checks, and database immutability triggers.
- Follow-up migration `20260910164500_bind_primary_order_children` closes the child-parent boundary: `(orderId, reservationId)` on each line must match its parent order, and `(orderLineId, orderId)` on each component must match one actual line/order pair. The existing reservation/ticket-type binding remains in force.
- Order Revision 1 verification on 2026-09-10 used a newly provisioned disposable PostgreSQL 16 `primary_ticketing_test` database: all 41 migrations applied; all 11 order, 8 reservation, 7 ticket-type, 9 event, and 8 organizer PostgreSQL tests passed; the complete integration-enabled run passed 60 suites / 385 tests; Prisma format/validation/generation and migration status, TypeScript, production build, focused/full lint, and `git diff --check` passed. The disposable database/container was removed afterward.
- Order Revision 2 verification used a fresh disposable PostgreSQL 16 database: all 42 migrations applied and migration status was current; all 13 order, 8 reservation, 7 ticket-type, 9 event, and 8 organizer PostgreSQL tests passed; the complete run passed 60 suites / 387 tests. Direct SQL tests prove cross-reservation lines and cross-order components are rejected while normal service creation succeeds.

## Stripe Test-Mode Payment Attempt Foundation

- The payment boundary is isolated behind `PrimaryStripeAdapter`. Runtime configuration requires the verified primary preflight capability, an `sk_test_` secret, and a `whsec_` signing secret; live keys fail closed. Secrets, client secrets, raw webhook bodies, card data, and buyer contact data are never persisted or audited.
- The authoritative local boundary is one serializable transaction: lock scope, reject cross-order/scope attempt-key reuse, validate the still-committable reservation, move `HELD -> PAYMENT_COMMITTED` and `PENDING_PAYMENT -> PAYMENT_PROCESSING`, create the scoped attempt, and write every audit/outbox record. Any validation or persistence failure rolls back that entire unit. Only after it commits may the adapter create an unconfirmed test PaymentIntent. The service returns the persisted attempt, never a client secret, and there is no checkout/public route.
- Expected amount/currency and organizer/event/order/reservation identity come only from immutable local snapshots. Provider metadata is server-owned. One attempt per order, unique platform/provider keys, composite scope foreign keys, and provider-event IDs enforce replay safety and durable deduplication.
- Attempt states are `PENDING_PROVIDER`, `PROCESSING`, `SUCCEEDED`, `FAILED`, `CANCELLED`, and `RECONCILIATION_REQUIRED`. Database triggers make attempt command/scope/value/provider identity immutable, permit only enumerated lifecycle transitions and one-time provider attachment, and make provider events/exceptions append-only. Signed provider events are stored only as safe identifiers/type/time plus a SHA-256 body digest. Exact repeats are no-ops; a reused provider-event ID with a different digest or material identity fails closed.
- Success requires exact provider intent, amount, currency, order, organizer, event, and reservation matching while inventory remains `PAYMENT_COMMITTED`. Any mismatch becomes an explicit `PROVIDER_MISMATCH`. Success after proven terminal release creates `LATE_SUCCESS_REFUND_REQUIRED`; it does not mark the order paid or issue admission.
- A signed failed/cancelled outcome releases committed inventory only when the adapter marks it terminal. The Stripe adapter treats cancellation—not an ordinary `payment_failed` event—as terminal proof. Release, attempt/order transition, provider-event persistence, audit, and outbox are one serializable transaction. Ambiguous provider creation or reconciliation stays capacity-safe and requires review.
- A verified webhook racing the post-provider attachment step is correlated through exact server-owned order/organizer/event/reservation metadata plus expected amount/currency. The transaction attaches the previously null provider identity once and processes the durable event; it neither drops the event nor trusts unverified client correlation.
- Verification on 2026-09-10 used a fresh disposable PostgreSQL 16 database: all 43 migrations deployed and status was current; all 7 payment, 13 order, 8 reservation, 7 ticket-type, 9 event, and 8 organizer PostgreSQL tests passed; the full integration-enabled run passed 61 suites / 394 tests.
- Migration `20260910171500_add_primary_payment_attempts` adds only scoped attempts, deduplicated provider events, exception/refund-obligation evidence, minimal paid/failed order states, composite constraints, and the reviewed order-transition trigger extension.
- Follow-up migration `20260910174000_harden_primary_payment_evidence` adds attempt evidence/transition protection and append-only provider-event/exception triggers. Payment Revision 1 expands the payment PostgreSQL suite to 11 tests covering pre-effect key conflicts, direct evidence mutation/deletion, changed-payload replay, and pre-attachment webhook recovery.
- Payment Revision 1 verification used a newly provisioned disposable PostgreSQL 16 database: all 44 migrations deployed cleanly and status was current; all 11 payment, 13 order, 8 reservation, 7 ticket-type, 9 event, and 8 organizer PostgreSQL tests passed (56/56); all 60 repository-local suites / 391 tests passed; Prisma format/validation/generation, TypeScript, production build, focused/full lint, and `git diff --check` passed. The separately deployed live-API registration probe remained externally rate-limited and is not branch evidence.
- Payment Revision 2 requires no migration: the existing order service exposes its protected preparation operation to the payment service's transaction, making attempt creation, reservation/order transitions, and audit/outbox evidence atomic. Tests prove an expired hold leaves zero new evidence and can retry after correction, while an injected preparation-outbox conflict rolls back the complete local unit before any provider call.
- Payment Revision 2 verification on the isolated PostgreSQL 16 database kept all 44 migrations current; all 13 payment tests and all 58 PostgreSQL service tests passed; the complete integration-enabled regression passed 61 suites / 400 tests. Prisma validation/migration status, TypeScript, production build, focused/full lint, and `git diff --check` passed.

## Admission Credential Foundation

- `PrimaryAdmissionTicket` is a durable entitlement created once for each immutable order-line unit. Composite foreign keys bind every row to the exact order scope and exact line/reservation/ticket-type tuple; `(orderId, unitNumber)` enforces exact cardinality under replay.
- `PrimaryAdmissionCredential` is one immutable generation in this milestone. Its ID is a cryptographically random UUID. The canonical Ed25519 payload is `{"v":1,"cid":"<credential UUID>","eventId":"<primary event ID>","iat":"<ISO-8601 UTC>","kid":"<test key ID>"}`. It contains no buyer identity, contact data, price, seat, or order data. The database stores only safe payload metadata and its SHA-256 digest—never the signature, private key, or complete bearer token. Authorized exact replay reconstructs the canonical payload and deterministically re-signs it with the protected private key.
- Signing configuration is available only after primary preflight and requires a `test_` key ID plus a matching Ed25519 PKCS#8 private key/SPKI public key pair. Missing, malformed, mismatched, non-Ed25519, or non-test configuration fails closed. Private-key custody remains environment-secret infrastructure; keys must never enter source, database, audit, or logs.
- Issuance is internal/synthetic-only and requires a strictly reconciled `PAID` order with one `SUCCEEDED` attempt and no payment exception. One serializable, retry-safe transaction locks the order, creates exactly the order-line quantity, and writes each credential plus protected audit/outbox evidence. Any failure rolls back the entire issuance.
- Internal verification requires exactly the five canonical fields with strict primitive types, a v4 UUID, and canonical ISO-8601 UTC timestamp; unknown fields are rejected. It checks the supplied signature cryptographically, then compares version, key ID, expected event, stored digest/timestamp, and current entitlement status. `VOIDED` is the only transition and can never restore usability. There is no QR rendering, public route, scanner, check-in, transfer, reissue, or refund behavior.
- Rotation in this foundation is deliberately manual and test-only: a new issuer uses a new `test_` key ID, while verification of historical credentials requires retaining its public key. A reviewed multi-key public verification keyring and custody/rotation runbook are required before any pilot; old private keys need not remain online.
- Migration `20260910181500_add_primary_admission_credentials` adds only the two admission records, composite integrity constraints, exact-unit uniqueness, checks, and database immutability/void-transition triggers.
- Follow-up migration `20260910183500_harden_primary_admission_issuance` removes stored signatures and adds a database INSERT guard. The guard locks and verifies the exact immutable line quantity, PAID parent order, SUCCEEDED scoped payment attempt, and absence of payment exceptions before any entitlement can be inserted directly or through the service.
- Verification on a newly provisioned disposable PostgreSQL 16 database: all 45 migrations deployed cleanly and status was current; all 7 admission, 13 payment, 13 order, 8 reservation, 7 ticket-type, 9 event, and 8 organizer PostgreSQL tests passed (65/65); the complete integration-enabled regression passed 62 suites / 407 tests. Prisma format/validation/generation, TypeScript, production build, focused/full lint, and `git diff --check` passed.
- Credential Revision 1 verification used a fresh disposable PostgreSQL 16 database: all 46 migrations deployed cleanly and status was current; all 8 admission tests and all 66 primary-domain PostgreSQL tests passed; the complete integration-enabled regression passed 62 suites / 408 tests. The expanded tests cover every direct database eligibility rejection, signature-free stored evidence, correctly signed schema/type/version/UUID/timestamp negatives, canonical base64url enforcement, and ordinary issuance/replay/void/rollback behavior.

## Online Admission Scan Foundation

- `PrimaryAdmissionScan` is append-only, PII-safe evidence for authenticated online scan attempts. It stores the expected organizer/event, operator, result, timestamp, optional bound ticket/credential IDs, and one validated telemetry-only device label. It never stores the bearer token, raw QR data, buyer contact data, or a token fingerprint.
- Scan authorization reloads the user and requires verified, non-banned, non-admin status; an active `OWNER`, `EVENT_MANAGER`, `BOX_OFFICE`, or `SCANNER` membership; an active assignment to the exact event; and an approved, non-suspended organizer. Owners require an event assignment and platform administrators do not bypass staffing.
- Verification uses the exact canonical v1 payload and Ed25519 signature. The test-only keyring contains the current issuer plus uniquely named prior public keys; prior private keys are neither accepted nor required. Unknown and explicitly revoked key IDs fail closed. Rotation adds the retiring issuer's public key before changing the current signing key; revocation makes its credentials unusable and therefore requires an explicit event-operations decision.
- A serializable transaction locks the entitlement. Exactly one concurrent scan can transition `ISSUED -> CHECKED_IN`; later scans create `DUPLICATE` evidence and never create another admission. `ISSUED -> VOIDED` remains supported, while `CHECKED_IN` and `VOIDED` are terminal. Successful admission and protected audit/outbox evidence commit atomically.
- Authenticated malformed, unknown, wrong-event, unsupported-key/version, voided, and duplicate attempts receive bounded internal result codes and append-only evidence. Because malformed/unknown tokens cannot safely bind to an entitlement, those rows retain only the expected event and operator scope. Unauthorized attempts return `UNAUTHORIZED_OPERATOR` without writing tenant scan evidence.
- Scanning is online-only and service/test-level. Device IDs are observability labels, not credentials or attestation, and retention remains an operations/privacy decision before pilot. Overrides, offline sync/conflict handling, QR rendering, scanner routes/UI, transfer, reissue, check-in reversal, and refund behavior are deferred.
- Migration `20260910184500_add_primary_admission_scans` adds only `CHECKED_IN`, scan results/evidence, composite event bindings, append-only protection, and the reviewed terminal transition.
- Verification on a newly provisioned disposable PostgreSQL 16 database deployed all 47 migrations cleanly with current status; all 15 admission and 58 earlier primary-domain tests passed (73/73), and the complete integration-enabled regression passed 62 suites / 415 tests. Prisma format/validation/generation, TypeScript, production build, focused/full lint, and `git diff --check` passed.
- Scan Revision 1 persists a globally unique normalized request ID and SHA-256 command digest over organizer, event, operator, normalized device identity, and a SHA-256 token digest. Exact authenticated retries return the original immutable result/binding; any material-input reuse fails with `SCAN_IDEMPOTENCY_CONFLICT` and creates no scan, audit, or outbox evidence. No bearer token is persisted.
- Follow-up migration `20260910190000_harden_primary_admission_scans` adds that idempotency evidence and an INSERT guard. Bound `ACCEPTED`/`DUPLICATE` evidence requires a currently `CHECKED_IN` exact ticket/credential pair, `VOIDED` requires a currently voided pair, and all invalid/unknown/wrong-event/unsupported results must remain unbound.
- Scan Revision 1 verification on a fresh disposable PostgreSQL 16 database deployed all 48 migrations cleanly with current status; all 16 admission and 58 earlier primary-domain tests passed (74/74), and the complete integration-enabled regression passed 62 suites / 416 tests. Prisma format/validation/generation, TypeScript, production build, focused/full lint, and `git diff --check` passed.
- Scan Revision 2 migration `20260910191500_enforce_single_accepted_admission` adds a partial unique index permitting one `ACCEPTED` row per ticket and requires every `DUPLICATE` row to follow an existing accepted row for the exact ticket, credential, and event. This makes direct database evidence preserve the same single-admission invariant as the service.
- Scan Revision 2 verification on a fresh disposable PostgreSQL 16 database deployed all 49 migrations cleanly with current status; all 16 admission and 58 earlier primary-domain tests passed (74/74), and the complete integration-enabled regression passed 62 suites / 416 tests. Prisma format/validation/generation, TypeScript, production build, focused/full lint, and `git diff --check` passed.
- Merchant-of-record, permanent fees/taxes, refund execution, chargebacks, retries after terminal attempts, provider reconciliation operations, credentials, payout/reserve accounting, and settlement remain unresolved and excluded.

## Staging-only Organizer Test Interface

- `/staging/primary` is a non-public synthetic workflow console for the separately provisioned `isolated-preview` environment. It fails closed unless both primary-ticketing preflight and a dedicated staging-console switch succeed, `VERCEL_ENV=preview`, and a non-committed access token of at least 32 characters is configured.
- The access-token exchange creates only two reserved `.example.invalid` personas: one ordinary organizer user and one platform reviewer. Every session is reloaded and accepted only when its exact reserved email and role match and the user remains verified and non-banned. No email is sent and no usable password is exposed.
- All organizer/event contact fields accepted by the console are restricted to the reserved `primary-staging.example.invalid` domain and synthetic `+1555` range. The console delegates mutations to the approved organizer, event, and ticket-type services rather than bypassing their tenant authorization or lifecycle rules.
- The browser flow supports organizer draft creation with atomic OWNER membership, organizer submission/review/approval, draft event creation/editing, GA ticket-type capacity and price configuration, event submission/review/approval, clear operation status, and a bounded redacted audit view.
- It contains no Stripe/provider action, real payment or money movement, payout, email, webhook, cron, QR scanning, public navigation, or production behavior. The later staging refund-console revision adds only local synthetic refund/cancellation evidence under the same isolated-preview gate.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 53 migrations and passed 65 suites / 439 tests, including 9 staging gate, authorization, negative-path, and browser-flow tests. Prisma validation/status, TypeScript, focused/full lint with no errors, production build, and diff checks passed.

### Organizer Console Revision 1

- Platform-reviewer reads are limited to organizers with an active `OWNER` membership held by the exact reserved staging organizer persona. The reviewer cannot use the console state endpoint to enumerate unrelated primary organizers that might share the isolated preview database.
- Every console API response, including authentication failures and fail-closed `404` responses, explicitly emits `Cache-Control: private, no-store` so persona, workflow, and audit state cannot be retained by a browser or intermediary cache.
- Five API-boundary tests cover reserved-organizer scoping, unauthenticated query suppression, session/failure cache policy, rejected-action cache policy, and non-cacheable CSRF failures on both state-changing routes. The complete non-database regression passed 58 suites / 357 tests with 8 PostgreSQL suites skipped; TypeScript, focused lint, production build with primary ticketing disabled, and diff checks passed.

## Risks and open decisions

## Refund persistence foundation checkpoint

- Provisional policy package v1 records the organizer merchant-of-record boundary, component-owner loss allocation, supervised checked-in exceptions, indemnity/insurance requirement, post-event clearance and reserves rule, no automatic resale, organizer chargeback liability, and immutable accounting/privacy/controller-processor controls. It is versioned and prospective; historical evidence cannot be rewritten.
- Migration `20260911112339_add_primary_refund_persistence_foundation` adds isolated policy, per-ticket purchase-allocation, refund/item/allocation/provider-attempt/provider-event, cancellation generation/batch, obligation, and admission-revocation persistence only. No provider dispatch or cash movement is present.
- Database checks and composite guards bind financial and admission evidence to the same paid order, payment attempt, event, ticket, credential, amount, and currency. Active/successful ticket refund overlap and allocation overrun fail closed.
- Purchase allocations use immutable order components and admission unit numbers. Exact component sums, stable remainder ranking, policy identity, owner identity, SHA-256 set digest, idempotent replay, and incomplete-history failure are enforced in PostgreSQL.
- Mutable refund, attempt, cancellation, and obligation summaries permit only reviewed transitions. Terminal parents reject retries; a new attempt requires the parent to remain `PROVIDER_PENDING` and the immediately prior attempt to carry authenticated terminal-impossibility evidence. Unique `(refundId, ordinal)` serialization permits at most one next attempt.
- Policy, allocations, items, provider events, cancellation batches, revocations, and existing protected audit/outbox records are append-only. Cancellation `RESOLVED` requires exact count/amount coverage and no unresolved obligation.
- PostgreSQL coverage exercises exact allocation and rounding, idempotent replay, incomplete-history rollback, overlapping-refund and over-allocation rejection, concurrent single-next-attempt authorization, terminal-parent rejection, cancellation coverage, obligation finality, and direct evidence mutation rejection.
- Excluded: Stripe refund calls, provider dispatch/reconciliation jobs, routes/UI, live data, payout/reserve execution, email, inventory resale, and deployment. Manual authenticated release QA remains a Marc/team dependency and does not alter this isolated checkpoint.

### Refund Persistence Revision 1

- Follow-up migration `20260911115921_harden_primary_refund_persistence` corrects the initial checkpoint without adding runtime/provider behavior.
- Allocation materialization validates every immutable order line separately and is tested with mixed line quantities. Entitlement identity is `(orderLineId, unitNumber)`, and order-line `orderId` is indexed rather than unique.
- A lock-backed ticket-claim row makes overlapping refunds concurrency-safe. Database completeness gates require exact parent/item/allocation totals, cumulative purchase-allocation bounds, and exact provider-attempt amount/currency before `PROVIDER_PENDING` or any attempt.
- Checked-in selection requires immutable named supervised approval, evidence digest, reason, fraud review, and explicit cost bearer. Immutable waiver approval is mandatory before an obligation can become `WAIVED_WITH_APPROVAL`.
- Cancellation completion is derived from immutable non-overlapping batch-ticket evidence; direct counter edits fail, batch and generation totals reconcile, obligations must be resolved, and only one active generation is permitted per event.
- Cross-aggregate guards fail closed on mismatched organizer/event/order/refund/cancellation/policy/ticket/credential/amount/currency evidence. The PostgreSQL suite includes adversarial multi-line, concurrent-claim, completeness, provider-value, checked-in, forged-counter, overlapping-batch, cross-scope, waiver, and immutability cases.

### Refund Persistence Revision 2

- Follow-up migration `20260911123000_enforce_refund_authorization_snapshot_integrity` remains persistence-only. Refund item and allocation inserts lock their parent and are permitted only in `REQUESTED`; the completeness transition and provider-attempt authorization use that same row lock, so provider eligibility cannot be invalidated by a late append.
- Checked-in exceptions and obligation waivers require a current verified, non-banned platform `ADMIN`, or an active `OWNER`/`FINANCE` membership in the exact organizer. Buyers, read-only members, and privileged members of another organizer fail closed.
- Cancellation activation derives and stores an immutable per-ticket snapshot from actual event admissions and refundable purchase allocations under the cancellation policy. Caller-supplied expected counts, amounts, and high-water identity are replaced by those authoritative values; missing allocation evidence aborts activation.
- Batch-ticket evidence must exactly match the snapshot ticket amount/currency and serializes on its obligation. Cumulative claims cannot exceed the obligation, resolution requires exact per-obligation and per-ticket coverage, and optional refund evidence must be the exact refund linked to that cancellation obligation with matching order/event/organizer/policy/currency/amount scope.
- The 13-case PostgreSQL suite covers post-gate and concurrent child append, buyer/unprivileged/cross-organizer approval rejection, authorized supervisors, forged activation expectations, cumulative obligation overuse, unrelated-refund evidence, and all prior persistence invariants.

### Refund Persistence Revision 3

- Follow-up migration `20260911130000_finalize_cancellation_evidence_integrity` makes cancellation finality stable without adding runtime/provider behavior. Snapshot rows are accepted only as nested database work during `REQUESTED -> ACTIVE`; direct caller inserts fail.
- Cancellation batches, obligations, batch-ticket claims, waiver approvals, revocations, and cancellation-refund links lock the parent cancellation and are accepted only while it is active and unresolved. They therefore serialize against `RESOLVED`, and every tested append after resolution fails closed.
- `PrimaryCancellationRefundLink` is immutable provenance joining one exact refund to its cancellation while the refund is still empty and `REQUESTED`. Creation locks both parents, checks exact organizer/event/policy scope, and must precede refund-item materialization and provider authorization; an already materialized voluntary refund cannot be relabeled later.
- A cancellation obligation that names a refund requires that provenance link, rechecks complete allocations, and must equal the complete parent refund amount. Batch evidence continues to require the exact linked item/ticket/amount/currency; a pre-existing same-order voluntary refund cannot be attached retroactively.
- Focused PostgreSQL coverage includes caller snapshot contamination, concurrent resolution-versus-obligation insertion, post-resolution child inserts, and the same-order/same-event voluntary-refund bypass.

### Staging-only synthetic refund and cancellation console

- The existing `/staging/primary` isolated-preview console now exposes three deterministic, synthetic scenarios: an ordinary unscanned refund, a checked-in refund requiring a scoped supervisor, and an event cancellation with one refunded ticket and one approved checked-in waiver. It remains behind the dedicated preview gate, reserved personas, session authentication, origin/CSRF checks, and private no-store responses.
- Admin-only reset/reseed takes a PostgreSQL advisory transaction lock and creates a fresh numbered generation while retaining prior immutable evidence. Stable IDs, amounts, ticket allocation, paid-order snapshots, credential/admission state, and accepted synthetic scan evidence make each generation repeatable without deleting or rewriting protected rows.
- Ordinary refund completion voids only the issued admission, writes revocation evidence before a local synthetic attempt, and reaches `SUCCEEDED` without a provider call. Checked-in selection cannot materialize until a current reserved admin records a reason, evidence digest, fraud review, and explicit `ORGANIZER | TRUEFANTIX` cost bearer; completion preserves the historical `CHECKED_IN` state and adds a revocation.
- Cancellation activation derives its authoritative per-ticket count and amount from immutable refundable purchase allocations. Preparation records cancellation-refund provenance before items, exact per-ticket obligations, and `REFUNDING`; resolution requires the admin waiver, synthetic refund completion, satisfied/waived obligations, exact batch-ticket coverage, and cancellation-scoped revocations.
- The browser shows ticket/refund/cancellation status, authoritative snapshots, provenance, obligations, batches, approvals, revocations, and bounded audit/rejection evidence. Commands accept no organizer, event, order, ticket, amount, status, or provider identity from the browser; the server resolves the latest reserved scenario and fails closed on replays, wrong persona, missing evidence, concurrency conflicts, or database rejection.
- Focused verification covers API routing/cache boundaries, browser supervisor controls, concurrent single-refund behavior, scoped approval rejection, missing-evidence rejection, exact cancellation resolution, and concurrent repeatable reseed on disposable PostgreSQL 16. There is no Stripe SDK use, provider dispatch/reconciliation job, real identifier, inventory resale, payout, email, webhook, cron, or live data.
- Final verification deployed all 53 migrations to a fresh disposable PostgreSQL 16 database and passed 67 suites / 450 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused and full lint with no errors, production build, and diff checks passed.
- Evidence-readability hardening exposes the named supervisor, reason, fraud review, cost bearer, evidence digest, synthetic provider outcome, revocation reason, per-obligation amount/provenance, and waiver approval directly in the scenario cards. API coverage proves rejection codes are returned privately and written to the bounded audit view; PostgreSQL adversarial coverage directly rejects an unscoped forged approval, checked-in item materialization without approval, cancellation snapshot mutation, premature resolution, and post-resolution snapshot mutation.
- Verification for the evidence-readability checkpoint again deployed all 53 migrations to a fresh disposable PostgreSQL 16 database and passed 67 suites / 452 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused/full lint with no errors, production build, and diff checks passed.
- Command-replay hardening rejects a repeated checked-in refund request before it can emit duplicate request audit evidence. The PostgreSQL console suite proves the second command fails with a stable rejection code and the successful request leaves exactly one audit record. Verification on a fresh disposable PostgreSQL 16 database deployed all 53 migrations and passed 67 suites / 452 tests with integration enabled; Prisma format/validation/generation/status, TypeScript, full lint with no errors, production build, and diff checks passed.
- Cancellation admission-gate hardening serializes activation with admission acceptance on the event identity. Once activation commits, service scans produce durable `VOIDED` rejection evidence while direct ticket check-in and forged accepted-scan inserts fail at PostgreSQL; the console makes the activation-time admission block explicit.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 54 migrations and passed 67 suites / 453 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused and full lint with no errors, production build, and diff checks passed.
- Workflow-order hardening serializes reseed and scenario commands under one staging-only transaction lock, maps database serialization/uniqueness contention to a stable private conflict code, and gives missing prerequisites and sequential command replays stable rejection codes across checked-in approval/completion and cancellation activation/preparation/waiver/resolution. Rejection audit rows attach to the exact ordinary, checked-in, or cancellation scenario instead of an unrelated event; PostgreSQL coverage exercises concurrent replay plus every out-of-order and replay boundary through final resolution.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 54 migrations and passed 67 suites / 454 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused and full lint with no errors, production build, and diff checks passed.
- The Vercel build path applies migrations only for the exact `staging` Git branch in a `preview` deployment after revalidating the enabled isolated-preview identity, console switch, matching dedicated PostgreSQL URLs, and isolated database name. Any incomplete staging boundary fails the deployment before migration; feature branches, dev, and production never enter this path.
- Rejection-provenance hardening carries the attempted scenario generation out of the serialized command transaction and uses it when persisting delayed rejection evidence. A concurrent reset/reseed can no longer move a failed command's audit row onto the newer generation.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 54 migrations and passed 67 suites / 455 tests with integration enabled, including API propagation and a reseed-between-rejection-and-audit adversarial case. Prisma validation/generation/status, TypeScript, full lint with no errors, production build, and diff checks passed.
- Completion-evidence hardening makes refund success depend on an exact successful attempt backed by immutable provider-event evidence, and makes obligation satisfaction depend on the exact successful refund. Cancellation resolution independently requires every satisfied ticket claim to carry its exact refund item, every waiver claim to remain non-refund evidence, and every authoritative snapshot ticket to have a cancellation-scoped revocation.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations and passed 67 suites / 456 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, full lint with no errors, production build, and diff checks passed.
- Review-evidence hardening exposes the synthetic attempt ordinal, exact amount/currency, authorization reason, completion event identity/type/digest, named audit actor, structured rejection result and generation, every authoritative snapshot row, per-ticket refund/waiver disposition, cancellation-refund link, and batch range/amount directly in the browser. Rejection request identities are collision-safe UUIDs. Reset/reseed now derives its next generation only from the reserved synthetic organizer, so a matching event identifier owned by an unrelated tenant cannot perturb deterministic generation numbering; a PostgreSQL adversarial case proves that boundary.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 457 tests with integration enabled, including 8 staging refund-console PostgreSQL cases. Prisma format/validation/generation, TypeScript, focused and full lint with no errors, production build, and diff checks passed.
- Pending-refund visibility hardening returns the checked-in refund parent before ticket items may legally exist, so the browser exposes the supervisor approval control from the actual PostgreSQL workflow state rather than an impossible pre-approved item fixture.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 457 tests with integration enabled; Prisma format/validation/generation, TypeScript, full lint with no errors, production build, and diff checks passed.
- Command-target hardening rejects any caller-selected generation, organizer, event, order, payment, ticket, refund, cancellation, obligation, policy, amount, currency, status, or provider identity before a synthetic scenario command can mutate data. PostgreSQL adversarial coverage proves every rejected input leaves the authoritative ordinary scenario without a refund.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 458 tests with integration enabled; Prisma format/validation/generation, TypeScript, full lint with no errors, isolated-preview production build, and diff checks passed.
- Strict command-envelope hardening replaces the finite target-field blacklist with per-action allowlists. Refund, cancellation, supervisor, and reset/reseed commands now reject every unexpected top-level field before mutation, including future or disguised buyer, credential, reservation, payment-provider, financial, and nested target inputs. Scenario actions validate after locking their exact generation so concurrent reseeds cannot misattribute the durable rejection evidence.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 459 tests with integration enabled. Prisma format/validation/generation, TypeScript, focused and full lint with no errors, isolated-preview production build, and diff checks passed.

- Allocation-evidence hardening exposes each ticket's immutable component allocation, refundable decision, liability owner, algorithm version, and allocation-set digest in the staging scenario cards. PostgreSQL coverage proves every deterministic seeded ticket reconciles its expected face-value and fee components to the exact per-ticket total.
- Legacy dispute information requests now commit the locked admin request, notifications, audit record, and `ATTEMPTING` delivery reservations before any email-provider call. Post-commit delivery uses a request/recipient idempotency key and a second serializable transaction to finalize provider evidence plus the visible request history; a failed first commit sends no email, while failed evidence finalization is surfaced for Admin review without undoing the committed request.
- Verification on a fresh disposable PostgreSQL 17 database deployed all 104 migrations with current status and passed 119 non-live suites / 1,034 tests with integration enabled plus the 7-test isolated-preview HTTP smoke suite. Prisma format/validation/generation, TypeScript, full lint with 0 errors and 592 pre-existing warnings, isolated-preview production build, focused route coverage, and diff checks passed.
- Commit `76d1b65` was pushed to the isolated `staging` branch. A new preview could not be created because the Vercel project had exhausted its free daily deployment quota (`api-deployments-free-per-day`); no paid upgrade or production action was attempted. The prior isolated preview remained healthy, and local isolated-preview build/smoke validation covered this checkpoint.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 459 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused and full lint with no errors, isolated-preview production build, and diff checks passed.
- Admission-state hardening revalidates the exact deterministic ticket set and expected `ISSUED`/`CHECKED_IN` state before refund and cancellation commands. A scanned or otherwise drifted ordinary ticket cannot be relabeled as the unscanned scenario, and a voided or incomplete cancellation fixture cannot acquire an authoritative cancellation snapshot. PostgreSQL adversarial coverage proves both failures roll back without refund or cancellation parents.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 460 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused and full lint with no errors, isolated-preview production build, and diff checks passed.
- Persona-state hardening revalidates the exact reserved organizer or admin user inside every serialized refund-console transaction. A session snapshot cannot authorize a command after the persona is banned, unverified, or moved to a different role; organizer membership remains independently required. PostgreSQL adversarial coverage proves stale organizer and supervisor identities fail before refund or approval evidence is written.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 461 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused and full lint with no errors, isolated-preview production build, and diff checks passed.
- Purchase-state hardening revalidates the reserved organizer and exact synthetic event, ticket type, committed reservation, paid order, immutable line/components, successful payment attempt, and per-ticket allocation evidence inside every serialized refund/cancellation command. PostgreSQL adversarial coverage proves an event lifecycle or ticket-type drift fails closed before a refund parent can be written.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 462 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused and full lint with no errors, isolated-preview production build, and diff checks passed.
- Synthetic-buyer hardening revalidates the reserved buyer identity, verification/moderation state, disabled capabilities, and buyer binding across the reservation, order, and successful payment attempt before every refund/cancellation command. Admin reset/reseed restores the reserved non-login buyer persona deterministically; PostgreSQL coverage proves a capability drift fails closed without creating a refund and that the next reseed safely repairs the persona.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 463 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused and full lint with no errors, isolated-preview production build, and diff checks passed.
- Admission-evidence hardening revalidates every deterministic ticket binding, issuance key, credential identity/payload digest, expected admission state, and exact accepted-scan evidence before each refund or cancellation command. Extra append-only scan evidence therefore fails closed instead of silently changing the meaning of a synthetic scenario.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 464 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with no errors, isolated-preview production build, and diff checks passed.
- Workflow-evidence hardening revalidates the server-derived refund and cancellation command envelopes before any later approval, preparation, completion, or replay decision. A forged in-scope parent with the reserved request key can no longer inherit the synthetic console workflow; PostgreSQL adversarial coverage proves mismatched requester, digest, reason, snapshot, or financial evidence fails before approval, refund-item, obligation, or completion writes.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 465 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with no errors, isolated-preview production build, and diff checks passed.
- Derived-evidence hardening requires the checked-in approval to belong to the reserved staging supervisor and requires its exact refund item/allocation materialization before completion. Cancellation waiver and completion commands now revalidate the one cancellation-refund link, exact refund item allocations, two deterministic obligations, permitted intermediate status, and reserved-supervisor waiver evidence; extra or substituted preparation rows fail closed before further mutation.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 466 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with no errors, isolated-preview production build, and diff checks passed.
- Terminal-evidence hardening revalidates the exact synthetic provider attempt/event identity, authorization, amount/currency, admission revocation, ticket terminal state, cancellation batch and per-ticket disposition on completion and every replay. Appended or substituted terminal evidence can no longer inherit a successful or resolved console workflow; a PostgreSQL adversarial case proves contaminated provider-event history fails closed.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 467 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with no errors, isolated-preview production build, and diff checks passed.
- Audit-provenance hardening makes every synthetic transition depend on one exact console-authored audit record with the expected reserved actor, aggregate binding, reason, bounded state snapshot, private request identity, and empty network metadata. Missing, duplicate, or substituted workflow audit evidence now fails closed before a checked-in refund or cancellation can advance, and completed refund/cancellation replays revalidate the entire transition chain.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 468 tests with integration enabled. The focused console suite passed 18 PostgreSQL cases, including duplicate terminal-audit and missing activation-audit adversarial coverage. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview production builds, and diff checks passed.
- Ordinary-refund origin hardening refuses to adopt a pre-existing incomplete refund parent, even when an external writer forges every deterministic command field and reserved request identity. Because the console's ordinary refund is one atomic transaction, only a fully revalidated terminal replay can be legitimate; PostgreSQL coverage proves the forged parent remains unmodified with no items, revocation, attempt, or completion audit.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 469 tests with integration enabled. The focused console suite passed 19 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview production builds, and diff checks passed.
- Synthetic-payment evidence hardening requires the deterministic successful payment fixture to remain free of provider-event and reconciliation-exception rows. Appended external-looking payment evidence can no longer be silently adopted as the origin of a console refund; PostgreSQL adversarial coverage proves both contaminations fail before a refund parent is written.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 470 tests with integration enabled. The focused console suite passed 20 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview production builds, and diff checks passed.
- Synthetic-buyer identity hardening revalidates the reserved buyer's non-login password marker, exact name/address profile, and clear ban state in addition to its contact, role, verification, and capability fields. PostgreSQL adversarial coverage proves externally changed identity data fails closed before refund mutation.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 471 tests with integration enabled. The focused console suite passed 21 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview production builds, and diff checks passed.
- Synthetic-tenant metadata hardening revalidates the reserved organizer's exact identity, synthetic contact surface, approval provenance, and complete absence of payment-provider configuration, plus every scenario event's exact synthetic description, venue, schedule, contact, policy, approval, and ticket-type metadata. Reset/reseed restores the reserved synthetic organizer safely; PostgreSQL adversarial coverage proves contaminated merchant/payment and attendee-contact metadata fail before refund mutation.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 472 tests with integration enabled. The focused console suite passed 22 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview production builds, and diff checks passed.
- Synthetic purchase-timeline hardening revalidates one exact seed-time anchor across event approval, reservation commitment/reconciliation, order processing/payment, and provider creation/terminal evidence. It also requires the fixed reservation expiry and absence of release, expiry, or payment-failure markers; PostgreSQL adversarial coverage proves reservation timeline contamination fails before refund mutation.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 473 tests with integration enabled. The focused console suite passed 23 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused and full lint with 0 errors, default and isolated-preview production builds, and diff checks passed.
- Synthetic-ownership provenance hardening revalidates both reserved staging personas, the exact self-invited active OWNER membership, and the organizer/event creator and approver bindings before every refund or cancellation mutation. The synthetic organizer must carry no encrypted business identity or provider configuration; reset/reseed refuses a stale organizer persona and deterministically restores the reserved membership and empty business identity. PostgreSQL adversarial coverage proves a substituted membership inviter and contaminated business number fail closed before refund mutation.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 475 tests with integration enabled. The focused console suite passed 25 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused and full lint with 0 errors, default and isolated-preview production builds, and diff checks passed.
- Synthetic tenant-access hardening requires the refund organizer to have exactly one self-invited active reserved OWNER, with no invitations or event-staff assignments. Unexpected access grants fail closed before any refund or cancellation mutation, and reseed refuses to normalize a contaminated access boundary implicitly. PostgreSQL adversarial coverage exercises extra membership, invitation, and assignment grants independently.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 476 tests with integration enabled. The focused console suite passed 26 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused/full lint with no errors, default and isolated-preview production builds, and diff checks passed.
- Synthetic delivery-intent hardening requires the reserved refund organizer to have no outbox messages before scenario mutation or reseed. Unexpected pending or historical delivery intent therefore fails closed instead of being inherited by the no-side-effect console; PostgreSQL adversarial coverage proves the contaminated organizer cannot refund or reseed until the foreign intent is removed explicitly.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 477 tests with integration enabled. The focused console suite passed 27 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview production builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Synthetic delivery-routing hardening rejects outbox intent bound to the reserved organizer or any reserved synthetic aggregate, even when a foreign writer omits the optional organizer relation. PostgreSQL adversarial coverage proves an organizer-less event aggregate blocks both refund mutation and reseed until the foreign delivery intent is removed explicitly.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 478 tests with integration enabled. The focused console suite passed 28 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Opaque workflow delivery-routing hardening resolves the reserved organizer's generated refund and cancellation identifiers before checking the outbox. A delivery intent that omits organizer routing can no longer target either opaque workflow parent and then allow supervisor approval, cancellation preparation, or reseed to proceed; PostgreSQL adversarial coverage proves each contaminated workflow remains unmodified until the foreign intent is removed explicitly.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 479 tests with integration enabled. The focused console suite passed 29 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Nested workflow delivery-routing hardening also resolves the reserved organizer's opaque refund-obligation identifiers before checking the outbox. An organizer-less delivery intent targeting the checked-in waiver obligation now blocks supervisor approval and reseed before any new evidence can be written; PostgreSQL adversarial coverage proves the contaminated obligation remains unapproved until the foreign intent is removed explicitly.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 480 tests with integration enabled. The focused console suite passed 30 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Allocation delivery-routing hardening resolves the opaque per-component purchase-allocation identifiers materialized for every reserved synthetic order before checking the outbox. An organizer-less delivery intent targeting immutable financial allocation evidence now blocks refund mutation and reseed instead of being inherited by the no-side-effect console; PostgreSQL adversarial coverage proves the refund remains absent until the foreign intent is removed explicitly.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 481 tests with integration enabled. The focused console suite passed 31 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Refund-item delivery-routing hardening resolves the opaque line-item identifiers created under reserved synthetic refunds before checking the outbox. An organizer-less delivery intent targeting a checked-in refund item now blocks supervisor approval and reseed before approval evidence can be written; PostgreSQL adversarial coverage proves the refund remains unapproved until the foreign intent is removed explicitly.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 482 tests with integration enabled. The focused console suite passed 32 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Refund-allocation delivery-routing hardening resolves the opaque per-component identifiers materialized beneath every reserved synthetic refund item before checking the outbox. An organizer-less delivery intent targeting checked-in refund allocation evidence now blocks provider completion and reseed before revocation or provider-attempt evidence can be written; PostgreSQL adversarial coverage proves both side effects remain absent until the foreign intent is removed explicitly.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 483 tests with integration enabled. The focused console suite passed 33 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Refund-attempt delivery-routing hardening resolves the opaque provider-attempt identifiers created beneath completed reserved synthetic refunds before checking the outbox. An organizer-less delivery intent targeting completed synthetic provider evidence now blocks all later console actions and reseed; PostgreSQL adversarial coverage proves an unrelated cancellation cannot activate while that foreign intent remains.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 484 tests with integration enabled. The focused console suite passed 34 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Refund-provider-event delivery-routing hardening resolves the opaque provider-event identifiers stored beneath every completed reserved synthetic refund before checking the outbox. An organizer-less delivery intent targeting terminal synthetic provider evidence now blocks later console actions and reseed; PostgreSQL adversarial coverage proves an unrelated cancellation cannot activate while that foreign intent remains.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 485 tests with integration enabled. The focused console suite passed 35 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Cancellation-batch delivery-routing hardening resolves the opaque batch identifiers created beneath completed reserved synthetic cancellations before checking the outbox. An organizer-less delivery intent targeting terminal synthetic batch evidence now blocks reseed, so a contaminated immutable generation cannot be followed by a newly active console generation.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 486 tests with integration enabled. The focused console suite passed 36 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Cancellation-snapshot delivery-routing hardening resolves the opaque snapshot-ticket identifiers created when a reserved synthetic cancellation activates. An organizer-less delivery intent targeting immutable cancellation scope now blocks preparation and reseed before refund or obligation evidence can be written.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 487 tests with integration enabled. The focused console suite passed 37 PostgreSQL cases. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Cancellation-refund-link delivery-routing hardening resolves the opaque provenance-link identifiers joining reserved synthetic cancellations to their refunds. An organizer-less delivery intent targeting that immutable link now blocks waiver approval and reseed before further cancellation evidence can be written.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 488 tests with integration enabled. The focused console suite passed 38 PostgreSQL cases. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Checked-in approval delivery-routing hardening resolves the opaque supervisor-approval identifiers created beneath reserved synthetic checked-in refunds. An organizer-less delivery intent targeting that approval now blocks provider completion and reseed before revocation or provider-attempt evidence can be written.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 489 tests with integration enabled. The focused console suite passed 39 PostgreSQL cases. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Waiver-approval delivery-routing hardening resolves the opaque supervisor-approval identifiers created beneath reserved synthetic cancellation obligations. An organizer-less delivery intent targeting that approval now blocks cancellation completion and reseed before revocation, provider-attempt, or resolution evidence can be written.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 490 tests with integration enabled. The focused console suite passed 40 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, and diff checks passed.
- Admission-revocation delivery-routing hardening resolves the opaque revocation identifiers created beneath completed reserved synthetic refunds and cancellations. An organizer-less delivery intent targeting terminal admission invalidation evidence now blocks all later console actions and reseed, preventing a contaminated immutable generation from progressing or being replaced.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 491 tests with integration enabled. The focused console suite passed 41 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Cancellation-batch-ticket delivery-routing hardening resolves the opaque per-ticket resolution identifiers created beneath terminal reserved synthetic cancellation batches. An organizer-less delivery intent targeting that final reconciliation evidence now blocks reseed, preventing a contaminated immutable generation from being replaced.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 492 tests with integration enabled. The focused console suite passed 42 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, isolated health, private-cache, and unauthenticated-boundary smoke checks passed.
- Audit-event delivery-routing hardening resolves the opaque immutable evidence identifiers created beneath reserved synthetic workflows. An organizer-less delivery intent targeting an audit event now blocks later console actions and reseed before any successor mutation or generation can be created.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 493 tests with integration enabled. The focused console suite passed 43 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, and diff checks passed.
- Membership delivery-routing hardening resolves the opaque identifier of the reserved synthetic organizer's sole owner membership. An organizer-less delivery intent targeting that access-control aggregate now blocks refund mutation and reseed before any new financial or immutable workflow evidence can be created.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 494 tests with integration enabled. The focused console suite passed 44 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, and diff checks passed.
- Buyer-identity delivery-routing hardening resolves the opaque identifier of the disabled reserved synthetic buyer. An organizer-less delivery intent targeting that console-owned identity now blocks refund mutation and reseed before any new financial or immutable workflow evidence can be created.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 495 tests with integration enabled. The focused console suite passed 45 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, and diff checks passed.
- Reserved-persona delivery-routing hardening resolves the opaque user identifiers of the synthetic administrator and organizer in addition to the disabled buyer. Organizer-less delivery intent targeting either authorizing persona now blocks refund mutation and reseed; independent PostgreSQL cases prove no refund and no next-generation event can be created.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 497 tests with integration enabled. The focused console suite passed 47 PostgreSQL cases. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, and diff checks passed.
- Refund-policy delivery-routing hardening rejects organizer-less outbox intent targeting the reserved policy version inherited by every synthetic allocation, refund, cancellation, and revocation. PostgreSQL coverage proves the contaminated policy blocks refund mutation and reseed before any new financial or immutable workflow evidence can be created.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 498 tests with integration enabled. The focused console suite passed 48 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, and diff checks passed. The deployment build preserves its verified PostgreSQL URL instead of substituting the legacy local development SQLite URL.
- Delivery-intent tenant-isolation hardening replaces the global synthetic identifier-prefix heuristic with exact identifiers resolved through the reserved organizer's complete purchase, payment, admission, refund, cancellation, access, and audit graph. A foreign tenant's lookalike event and organizer-less outbox intent no longer create a cross-tenant denial of service, while every reserved aggregate remains fail-closed.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 499 tests with integration enabled. The focused console suite passed 49 PostgreSQL cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, and diff checks passed.
- Primary-health cache hardening makes both fail-closed and enabled isolated-preview responses explicitly `private, no-store`; deployment smoke verification had shown the successful health response was otherwise emitted with a public revalidation policy. Route coverage proves both outcomes remain non-cacheable.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 500 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, the default deployment build, and diff checks passed.
- Reserved-identity authentication hardening prevents the synthetic organizer, reviewer, and disabled refund buyer emails and phone numbers from entering ordinary registration, login, or password-reset flows. The access-token-only console login also rotates any stale password hash and restores the synthetic legal-consent markers, capabilities, and verification-token boundary whenever it provisions a persona.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 67 suites / 508 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, and local HTTP smoke checks passed. Local and deployed smoke checks proved known-password login remains rejected by both reserved email and phone, password recovery returns its private non-enumerating no-delivery response, reserved email/phone registration is rejected with private non-cacheable responses, and the isolated health/session routes remain available.
- Reserved-profile hardening prevents access-token console personas from mutating their managed name, contact, or address fields through the ordinary account profile route. Ordinary accounts also cannot claim a reserved staging phone in a profile update, closing the remaining path that could displace a synthetic persona or make a reserved phone eligible for an external verification flow. Both refusals are private and non-cacheable.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 68 suites / 510 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, and diff checks passed.
- Ordinary authorization guards now reject every reserved staging persona with a private, non-cacheable console-only response. The synthetic organizer and refund buyer cannot enter ordinary buyer, seller, messaging, notification, or account mutation flows, and the synthetic reviewer cannot use the application's ordinary administrative routes; staging-console routes continue to use their separate access-token, environment, and persona checks.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 69 suites / 516 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, and local HTTP smoke checks passed. The smoke checks proved the staging console and state route remain available while authenticated synthetic access to ordinary buyer and administrative routes receives the private console-only refusal.
- Direct-session boundary hardening closes the ordinary routes that intentionally bypass the shared authorization guards. Reserved console personas now receive the same private console-only refusal from session introspection and password changes, cannot enter seller-onboarding provider status, and cannot use non-production verification or seller-approval helpers. Existing error contracts for ordinary users remain unchanged, and the provider boundary rejects the reserved persona before any Stripe client or account request can be created.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 70 suites / 521 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, and authenticated local HTTP smoke checks passed. Smoke coverage proved the staging console, state, and health routes remain available and private while session introspection, password mutation, seller-provider status, and debug mutation routes fail closed for the reserved reviewer.
- Legacy email-verification hardening conceals reserved staging identities from the unauthenticated token-issuance route and refuses even a matching legacy verification token before account mutation. Both refusals are private and non-cacheable, and neither path can update the reserved persona or invoke email delivery.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 71 suites / 523 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default production build, isolated-preview deployment build, diff checks, and authenticated local HTTP smoke checks passed. Smoke coverage proved the console, state, and private health routes remain available while the legacy verification request conceals the reserved reviewer without issuing a token or invoking delivery.
- Console command-envelope hardening gives every organizer, event, ticket-type, and persona-switch command an explicit top-level field allowlist. Unexpected provider, identity, tenant, financial, target, or nested inputs now fail before persona provisioning, session replacement, service construction, or domain mutation; rejected persona-switch input also leaves the current session intact.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 71 suites / 534 tests with integration enabled. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview builds, diff checks, and authenticated local HTTP smoke checks passed. The local smoke proved unknown, refund, domain, and persona envelopes receive private non-cacheable refusals before unrelated setup while the authenticated reviewer session remains intact. An exact-revision remote deployment was not performed because the isolated staging project reached its free daily preview quota. Earlier isolated-preview deployments and CLI-authenticated health/session smoke results are historical boundary evidence only, not deployment evidence for `f3e749b`, `563152d`, or this documentation tip.
- Managed-persona integrity hardening gives the reserved organizer and administrator one shared exact profile predicate across session authorization, refund/reseed transaction authorization, and access-token restoration. Drift in identity, phone, address, disabled capabilities, staging legal versions, seller linkage, moderation state, or verification/reset-token state now invalidates the session and fails closed inside the synthetic workflow transaction before mutation. The separately disabled refund buyer continues to use its existing exact synthetic-buyer predicate and restoration path.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 71 suites / 541 tests with integration enabled. The focused refund-console PostgreSQL suite passed 49 cases, including organizer capability drift and administrator phone drift without workflow mutation. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, diff checks, and authenticated local HTTP smoke checks passed. The smoke proved a drifted managed organizer becomes unauthenticated to both session and state routes while ordinary account access remains blocked.
- Managed-identity survivability hardening keeps a staging-owned account out of every ordinary authorization, login, password-recovery, email-verification, profile, session-introspection, and seller-provider boundary even if its reserved email is externally changed. The shared classifier recognizes the reserved phone or immutable staging legal-version markers in addition to the email, while ordinary accounts retain their existing access.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 71 suites / 547 tests with integration enabled. Five focused route/helper suites passed 43 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, and diff checks passed. Authenticated local HTTP smoke checks proved an email-drifted organizer is rejected privately by ordinary session introspection, becomes unauthenticated to the staging console/state routes, and cannot escape into ordinary account access.
- Authorization-response hardening makes every remaining ordinary referral, messaging, review-mutation, notification, notification-preference, price-alert, and waitlist handler return the shared authorization guard's refusal verbatim. A managed staging persona can no longer have its private `STAGING_CONSOLE_ONLY` response replaced by a generic cacheable authentication response at those 20 route boundaries.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 71 suites / 567 tests with integration enabled. The focused boundary suite passed 28 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment builds, and diff checks passed. Authenticated local HTTP smoke checks proved all 20 affected methods return `403 STAGING_CONSOLE_ONLY` with `Cache-Control: private, no-store` before request validation or data access while primary health and console state remain available.
- Access-token persona recovery now resolves the organizer and reviewer by both reserved email and reserved phone inside one serializable transaction. A drifted email can therefore be restored on the original user row instead of colliding with its still-reserved phone, while split ownership of the two coordinates or a coordinate belonging to the other managed persona fails closed without repurposing either account.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 71 suites / 571 tests with integration enabled. The PostgreSQL case proved an email-drifted organizer is restored to the same user identity; focused tests also cover split-coordinate and cross-persona conflicts. Prisma format/validation/generation/status, TypeScript, focused and full lint with 0 errors, default and isolated-preview deployment builds, diff checks, and authenticated local HTTP smoke checks passed. The smoke proved the drifted session becomes unauthenticated, access-token login restores the canonical persona, and the restored session can read the private staging state again.
- Access-token session replacement now restores the selected managed persona, revokes every prior bearer for that user, and creates the sole replacement session inside one serializable transaction before setting its browser cookie. A stale session issued before identity restoration can no longer become staging-console-authorized again when the canonical persona fields are restored; a failed restoration also no longer deletes the caller's current session first.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 72 suites / 574 tests with integration enabled. A dedicated PostgreSQL case proved two stale persona sessions are replaced by exactly one new session, while authenticated local HTTP smoke proved the first browser session becomes actorless immediately after a second access-token login and only one database session remains. Prisma format/validation/generation/status, TypeScript, focused and full lint with 0 errors, default and isolated-preview deployment builds, and diff checks passed.
- Persona-switch session hardening also revokes the caller's current bearer inside the successful restoration transaction when the selected persona differs. Overwriting one browser's cookie can no longer leave its prior organizer or reviewer session usable elsewhere, while a failed restoration still preserves the caller's existing session.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 72 suites / 576 tests with integration enabled; the two focused staging PostgreSQL suites passed 52 cases. Prisma format/validation/generation/status, TypeScript, focused and full lint with 0 errors, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated local HTTP smoke proved an admin-to-organizer switch leaves zero administrator sessions and one organizer session, the copied prior administrator bearer becomes actorless, the replacement bearer remains authorized, and health/session responses stay private and non-cacheable.
- Managed-session isolation now makes each successful access-token persona selection the sole surviving session across the complete staging-managed identity set, including the disabled buyer and identities recognized only by immutable staging legal-version markers after contact drift. The same transaction also revokes the caller's overwritten bearer, while its exact relational predicate preserves ordinary user sessions.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 72 suites / 576 tests with integration enabled; the two focused staging PostgreSQL suites passed 52 cases. PostgreSQL coverage proved administrator, organizer, disabled-buyer, and contact-drifted managed sessions are removed while an ordinary session survives. Prisma format/validation/generation/status, TypeScript, focused and full lint with 0 errors, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated local HTTP smoke proved an admin-to-organizer switch leaves only the organizer bearer usable, leaves zero administrator and buyer sessions, and preserves private non-cacheable health/session responses.
- Password-recovery isolation now covers both legacy reset stores as well as the current reset-token flow. A matching token for any managed staging identity is rejected as private, non-cacheable data before password, session, token-use, or legacy attempt-counter mutation; a request for the reserved identity remains concealed without creating a reset token or invoking email delivery.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 73 suites / 580 tests with integration enabled; the focused password-recovery suite passed 4 cases. Prisma format/validation/generation/status, TypeScript, focused and full lint with 0 errors, default and isolated-preview deployment-path builds, and diff checks passed. Local HTTP smoke proved managed request, link-validation, current completion, and legacy completion boundaries remain private and non-cacheable, while matching disposable reset records retained unused state and a zero legacy attempt count.
- Password-reset race hardening revalidates the one-time credential and the complete staging-managed identity predicate in the same write transaction that changes a password and revokes sessions. If access-token persona restoration wins while bcrypt is running, both the current reset-token path and legacy verification-code path now roll back the credential claim and return the private invalid-token response instead of overwriting the restored persona password or deleting its replacement console bearer.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 73 suites / 582 tests with integration enabled; the focused staging-console and password-recovery suites passed 34 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment-path builds, diff checks, and local HTTP smoke checks passed. The smoke checks proved the isolated primary health and session boundaries remain private and available and a managed password-reset request remains concealed without external delivery.
- Authenticated password-change race hardening now rechecks both the caller's observed password hash and the complete staging-managed identity predicate in the credential write transaction. If access-token persona restoration wins while bcrypt is running, the password update affects zero rows, the transaction preserves every replacement console bearer, and the route returns the private console-only refusal; an ordinary concurrent password change receives the existing invalid-password response instead of being misclassified as staging.
- Account-deletion race hardening now rechecks the observed password hash and complete staging-managed identity predicate in the destructive user statement. Persona restoration that wins while bcrypt is running receives the private console-only refusal without deleting the managed user or its replacement bearer; an ordinary concurrent credential change remains an invalid-credentials conflict.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 75 suites / 589 tests with integration enabled. The focused password-change and account-deletion race suites passed 7 cases before the full run. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors, default and isolated-preview deployment-path builds, diff checks, and authenticated local HTTP smoke checks passed. The smoke proved isolated health, admin session, and state remain private and available while a managed reviewer is refused at the ordinary account-deletion boundary without deleting the persona or its sole console bearer.
- Profile-update race hardening now performs the ordinary profile mutation under a serializable transaction with the complete staging-managed identity exclusion in the write predicate. If persona restoration wins after the route's initial authorization check, the profile stays exact and the caller receives the private profile-locked refusal rather than overwriting managed identity or contact fields.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 75 suites / 591 tests with integration enabled; the focused password, deletion, and profile race suites passed 12 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 617 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated local HTTP smoke proved the managed reviewer remains exact and its console bearer remains usable after an ordinary profile mutation attempt is refused privately.
- Buyer-wallet race hardening revalidates the complete staging-managed identity inside a serializable transaction and creates and attaches an ordinary buyer wallet in one managed-excluding nested write. Persona restoration that wins after the outer authorization check rolls back Seller creation and receives the private console-only refusal instead of attaching an ordinary seller aggregate to the restored persona.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 76 suites / 596 tests with integration enabled; the focused buyer-wallet race suite passed 5 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 616 existing warnings, default and isolated-preview deployment-path builds, diff checks, and authenticated local HTTP smoke checks passed. The smoke proved the managed reviewer is refused privately at the ordinary buyer-wallet boundary and retains a null seller link.
- Community-comment race hardening serializes every comment creation against access-token persona restoration by locking and revalidating the author inside the write transaction. A restoration that wins before the lock or produces a serialization abort receives the private console-only refusal without content residue; an ordinary moderation change made before the write retains the existing commenting-disabled response.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 77 suites / 601 tests with integration enabled; the focused community-comment race suite passed 5 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 616 existing warnings, default and isolated-preview deployment-path builds, diff checks, and authenticated local HTTP smoke checks passed. The smoke proved the managed reviewer is refused privately at the ordinary community-comment boundary and the managed comment count remains zero. An exact-revision isolated-preview deployment was attempted after pushing `1a037d6` but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Forum-content race hardening serializes both thread-plus-first-post creation and reply creation against access-token persona restoration by locking and revalidating the author inside the write transaction. A restoration that wins before the lock or produces a serialization abort receives the private console-only refusal without forum residue; an ordinary commenting restriction made before either write retains the existing refusal.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 78 suites / 608 tests with integration enabled; the focused forum-content race suite passed 7 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 615 existing warnings, default and isolated-preview deployment-path builds, diff checks, and authenticated local HTTP smoke checks passed. The smoke proved the managed reviewer is refused privately at both ordinary forum creation boundaries, the private staging state remains available, and the managed forum-content count remains zero.
- Review-mutation race hardening serializes create, update, and delete against access-token persona restoration by locking and revalidating the reviewer inside the write transaction. Review persistence and denormalized seller rating/count changes now commit atomically; a restoration that wins before the lock or causes a serialization abort receives the private console-only refusal without review or seller-summary residue.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 79 suites / 616 tests with integration enabled; the focused review-mutation race suite passed 8 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 615 existing warnings, default and isolated-preview deployment-path builds, diff checks, and authenticated local HTTP smoke checks passed. The smoke proved the managed reviewer is refused privately before ordinary review validation, the private staging state remains available, and the managed review count remains zero. An exact-revision isolated-preview deployment was attempted after pushing `8f39711`, but Vercel refused it at the free daily deployment quota; no historical preview is treated as deployment evidence for this checkpoint.
- Notification-preference race hardening serializes ordinary radius-setting updates, preference upserts, and ownership-checked preference deletion against access-token persona restoration by locking and revalidating the user inside the write transaction. A restoration that wins before the lock or causes a serialization abort receives the private console-only refusal without preference or managed-profile residue.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 80 suites / 623 tests with integration enabled; the focused notification-preference race suite passed 7 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 615 existing warnings, default and isolated-preview deployment-path builds, diff checks, and authenticated local HTTP smoke checks passed. The smoke proved the managed reviewer is refused privately at the ordinary notification-radius boundary, the private staging session and state remain available, and the managed preference count remains zero. Exact revision `2f743b7` deployed successfully to the isolated Vercel preview, where protected health and anonymous staging-session smoke checks returned private, non-cacheable healthy responses.
- Notification-state race hardening serializes bulk read-state updates and scoped notification deletion against access-token persona restoration by locking and revalidating the user inside the write transaction. A restoration that wins before the lock or causes a serialization abort receives the private console-only refusal without mutating managed notification evidence.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 81 suites / 628 tests with integration enabled; the focused notification-mutation race suite passed 5 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 615 existing warnings, default and isolated-preview deployment-path builds, diff checks, and authenticated local HTTP smoke checks passed. The smoke proved the managed reviewer is refused privately at the ordinary notification-state boundary, the private staging state remains available, and the managed notification count remains zero. Exact revision `e44ce06` deployed successfully to the isolated Vercel preview, where protected health and anonymous staging-session smoke checks returned private, non-cacheable healthy responses.
- Price-alert race hardening serializes ticket-scoped and event-query alert creation plus ownership-checked deletion against access-token persona restoration by locking and revalidating the user inside the write transaction. Ticket availability, duplicate detection, ownership checks, and their writes now share the same serializable boundary; a restoration that wins before the lock or causes a serialization abort receives the private console-only refusal without alert residue.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 82 suites / 633 tests with integration enabled; the focused price-alert race suite passed 5 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 614 existing warnings, default and isolated-preview deployment-path builds, diff checks, and authenticated local HTTP smoke checks passed. The smoke proved the managed reviewer is refused privately at the ordinary price-alert boundary while the private staging health and state boundaries remain available. Exact revision `ecc3983` deployed successfully to the isolated Vercel preview, where protected health and anonymous staging-session smoke checks returned private, non-cacheable healthy responses.
- Message-mutation race hardening serializes conversation read-state changes, sends, conversation creation, and sender-owned soft deletion against access-token persona restoration by locking and revalidating the user inside the write transaction. Message creation and conversation timestamp updates now commit atomically; a restoration that wins before the lock or causes a serialization abort receives the private console-only refusal before message, conversation, notification, or WebSocket residue. Read-state updates are additionally constrained to conversations containing the caller, and a foreign user can no longer leave an empty order conversation before failing participant authorization.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 83 suites / 642 tests with integration enabled; the focused message-mutation race suite passed 9 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 613 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated local HTTP smoke proved primary health, staging session, and staging state remained private and available while the managed reviewer received the private console-only refusal at the ordinary message boundary. Exact revision `fc6212e` was pushed to staging; its isolated-preview deployment was attempted, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Waitlist-mutation race hardening serializes event availability checks, duplicate detection, entry creation, and ownership-checked cancellation against access-token persona restoration by locking and revalidating the user inside the write transaction. A restoration that wins before the lock or causes a serialization abort receives the private console-only refusal before waitlist or confirmation-notification residue.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 84 suites / 647 tests with integration enabled; the focused waitlist-mutation race suite passed 5 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 613 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated local HTTP smoke proved primary health, staging session, and staging state remained private and available while the managed reviewer received the private console-only refusal at the ordinary waitlist boundary. Exact revision `c8b85be` was pushed to staging; its isolated-preview deployment was attempted, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Referral-mutation race hardening serializes referral-code generation, claims, and admin completion against access-token persona restoration by locking and revalidating every participating user in deterministic order. Referral creation and signup notification now commit atomically; completion additionally locks the referral and commits its state, seller reward, access-token transaction, and completion notification together. Reward persistence now targets the referrer's actual linked seller identity instead of assuming a user ID is also a seller ID. A restored managed participant or serialization abort receives the private console-only refusal before referral, reward, or notification residue.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 85 suites / 654 tests with integration enabled; the focused referral-mutation race suite passed 7 cases. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 613 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated local HTTP smoke proved primary health, staging session, and staging state remained private and available while the managed reviewer received the private console-only refusal at the ordinary referral boundary. Exact revision `b718625` was pushed to staging; its isolated-preview deployment was attempted, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Catalog-request race hardening serializes existing-preference detection, provider-backed catalog resolution/cache work, automatic preference/request fulfillment, clarification/pending request persistence, and optional admin-email delivery against access-token persona restoration by locking and revalidating the requester for the complete operation. A restoration that wins before the lock or causes a serialization abort receives the private console-only refusal before resolver, request, preference, or email activity.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 86 suites / 658 tests with integration enabled; the focused catalog-request race and resolver suites passed 7 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 613 existing warnings, default and isolated-preview deployment-path builds, diff checks, and isolated local HTTP smoke checks passed. The smoke proved protected primary health and anonymous staging state remained private and available while the catalog mutation boundary refused an unauthenticated caller. No provider lookup or email delivery was invoked during validation. Exact revision `8ac4659` was pushed to staging; its isolated-preview deployment was attempted, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Spotify-operation race hardening serializes connection inspection and disconnection, encrypted-token refresh and provider-backed artist loading, matched-preference and unmatched-request imports, optional admin-email delivery, and OAuth callback token exchange/account storage against access-token persona restoration. The Spotify helpers now carry the same transaction client through token and connected-account persistence. A restoration that wins before the lock or causes a serialization abort receives the private console-only refusal before provider or persistence activity.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 87 suites / 664 tests with integration enabled; the focused Spotify race suite passed 6 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 613 existing warnings, default and isolated-preview deployment-path builds, diff checks, and authenticated isolated local HTTP smoke checks passed. The smoke proved protected primary health and anonymous staging state remained private and available while the managed reviewer received private console-only refusals at the Spotify connection, artist-import, and callback boundaries. No Spotify/provider lookup or email delivery was invoked during validation. Exact revision `fb2dc98` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Verification-operation race hardening serializes email and phone code issuance, resend throttling, confirmation attempts, verification-state changes, and email/SMS delivery against access-token persona restoration. All reads and writes now use the same serializable transaction and locked current user identity; confirmation code consumption and destination verification are fully atomic. A restoration that wins before the lock or causes a serialization abort receives the private console-only refusal before verification residue or delivery.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 88 suites / 673 tests with integration enabled; the focused verification-operation race suite passed 9 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 612 existing warnings, default and isolated-preview deployment-path builds, diff checks, and authenticated isolated local HTTP smoke checks passed. The smoke proved primary health and staging session remained private and available while the managed reviewer received private console-only refusals at all four email/phone send/confirm boundaries. Email and SMS provider credentials were explicitly absent and no delivery was invoked during validation. Exact revision `ee77230` was pushed to staging; its isolated-preview deployment was attempted with the isolated staging project identity, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Review-translation race hardening serializes the authorized review lookup and provider-backed translation request against access-token persona restoration. The locked current seller binding replaces the potentially stale session relation, and a restoration that wins before the lock or causes a serialization abort receives the private console-only refusal before review disclosure or provider activity. Unrelated database failures remain unmasked while provider failures retain the existing translation-failed response.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 88 suites / 678 tests with integration enabled; the focused review-translation suite passed 6 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 612 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health and staging session remained private and available while the managed reviewer received the private console-only refusal at the translation boundary. A fake local provider key made any accidental request fail visibly; no provider request was invoked. Exact revision `99d3b67` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Pricing-recommendation boundary hardening returns the ordinary authorization guard's response instead of continuing after a refusal, then serializes the authenticated recommendation reads against access-token persona restoration. Ticket and seller queries now use the same transaction client and the locked current seller relationship; the previous route incorrectly supplied a user identifier where a seller identifier was required. A restored managed identity or restoration serialization abort receives the private console-only refusal before pricing data access.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 89 suites / 683 tests with integration enabled; the focused recommendation boundary and existing pricing suites passed 13 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 611 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Isolated local HTTP smoke proved primary health and staging session remained private and available, an anonymous recommendation request was rejected, and the managed reviewer received the private console-only refusal without executing ticket queries. Exact revision `2afab60` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Seller-onboarding status race hardening serializes the current seller binding, read-only provider account inspection, capability persistence, seller approval, and user selling enablement against access-token persona restoration. Provider-derived seller and user changes now commit atomically through the same transaction client; a restored managed identity or restoration serialization abort receives the private console-only refusal before provider access or seller mutation.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 90 suites / 688 tests with integration enabled; the focused seller-onboarding race suite passed 5 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 611 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health and staging session remained private and available while the managed reviewer received the private console-only refusal at the seller-onboarding boundary. A fake local Stripe key made any accidental request fail visibly; no provider request or money movement was invoked. Exact revision `f0aaf40` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Listing-maintenance race hardening serializes ticket update and withdrawal behind the current user identity lock, revalidates current seller approval, and uses the locked seller relationship plus one transaction client for listing, event, and provider-derived persistence. A restored managed identity or restoration serialization abort receives the private console-only refusal before ticket lookup, provider inspection, image/OCR work, or mutation; an ordinary approval downgrade is rejected independently.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 91 suites / 693 tests with integration enabled; the focused listing-mutation race suite passed 5 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 611 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health and staging session remained private and available while the managed reviewer received private console-only refusals at both ticket-update and withdrawal boundaries. Provider, email, payment, and money-movement credentials were absent and no external side effect was invoked. Exact revision `fcc4da3` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Listing-creation race hardening serializes the current seller binding, duplicate inventory checks, provider-backed listing evidence, receipt-confirmed catalog persistence, ticket/event creation, automatic verification, and listing notifications against access-token persona restoration. Every database operation now uses the same transaction client, including automatic verification; a restored managed identity or restoration serialization abort receives the private console-only refusal before provider, image/OCR, email, or listing activity, while an ordinary seller approval downgrade remains independently refused.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 92 suites / 696 tests with integration enabled; the focused listing-creation and listing-maintenance race suites passed 8 cases. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 611 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health, staging session, and staging state remained private and available while the managed reviewer received the private console-only refusal at the ticket-creation boundary and retained zero managed listings. Provider, image-search, OCR, email, payment, and money-movement credentials were absent and no external side effect was invoked. Exact revision `a2d1bdf` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Ticket-purchase race hardening serializes current buyer capability, verification, wallet binding, access-token balance, idempotency replay, ticket reservation, and order persistence against access-token persona restoration. The route now uses the wallet reloaded beneath the identity lock instead of the potentially stale guarded relationship; a restored managed identity or restoration serialization abort receives the private console-only refusal before order or ticket access, while ordinary capability and wallet changes retain explicit refusals.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 93 suites / 700 tests with integration enabled; the focused purchase, fee, and live-boundary suites passed 15 cases. A direct PostgreSQL helper smoke proved the user row lock, current wallet resolution, and managed-marker refusal. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 610 existing warnings, default and isolated-preview deployment-path builds, diff checks, and authenticated isolated local HTTP smoke checks passed. The HTTP smoke proved primary health and staging session remained private and available while the managed reviewer received the private console-only refusal at the ticket-purchase boundary. Provider, email, payment, and money-movement credentials were absent and no external side effect was invoked. Exact revision `8dfb094` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Multi-ticket checkout race hardening now runs current buyer capability, verification, wallet binding, idempotency replay, ticket reads, order creation, reservations, sold-out access-token holds, and hydrated response reads beneath the same serialized identity lock used by the single-ticket purchase boundary. The checkout route authorizes the current locked wallet rather than the potentially stale session relationship; a restored managed identity or restoration serialization abort receives the private console-only refusal before order or ticket access, while ordinary wallet, capability, and idempotency refusals retain their explicit contracts.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 94 suites / 704 tests with integration enabled when the live-boundary suite was explicitly directed to the isolated local server; the focused checkout, purchase, and fee suites passed 12 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 609 existing warnings, default and isolated-preview deployment-path builds, diff checks, and authenticated isolated local HTTP smoke checks passed. The smoke proved primary health and staging session remained private and available while the managed reviewer received the private console-only refusal at multi-ticket checkout and retained zero managed orders. Provider, email, payment, and money-movement credentials were absent and no external side effect was invoked. Exact revision `32991ed` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Live-boundary test hygiene now defaults an unspecified `LIVE_BASE_URL` to localhost instead of the production domain. Isolated staging validation must select its disposable local server or separately approved preview explicitly, and a missing selection can no longer direct regression probes at production.
- Payment-intent race hardening now serializes current buyer capability, verification, wallet binding, order ownership, reservation expiry cleanup, provider activity, and payment persistence against access-token persona restoration. The route authorizes the wallet reloaded beneath the identity lock rather than the potentially stale session relationship; a restored managed identity or restoration serialization abort receives the private console-only refusal before order or provider access. New provider requests use one stable order-scoped idempotency key so an ambiguous local commit cannot create a second intent identity on retry.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 95 suites / 708 tests with integration enabled when the live-boundary suite was explicitly directed to the isolated local server; the focused payment-intent, checkout, purchase, and payment suites passed 32 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 607 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health and staging state remained private and available while the managed reviewer received the private console-only refusal at the payment-intent boundary and the payment count remained zero. Provider, email, payment, and money-movement credentials were absent and no external side effect was invoked. Exact revision `bfe4035` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Admin order-capture race hardening serializes current administrator identity, verification, role, order and reservation validation, manual payment persistence, and the paid-order transition against access-token persona restoration. A restored managed identity or restoration serialization abort receives the private console-only refusal before order or payment mutation, while an ordinary role downgrade remains independently refused.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 96 suites / 712 tests with integration enabled after installing the live regression suite's required synthetic seller fixture; the focused admin-capture suite passed 4 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 606 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health and staging state remained private and available while the managed reviewer received the private console-only refusal at the admin-capture boundary and the payment count remained zero. Provider, email, payment, and money-movement credentials were absent and no external side effect was invoked. Exact revision `272ffb6` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Admin order-delivery race hardening serializes current administrator identity, verification, role, paid-order and payment validation, reservation validation, ticket sale, and the delivered-order transition against access-token persona restoration. A restored managed identity or restoration serialization abort receives the private console-only refusal before order or ticket mutation, while an ordinary role downgrade remains independently refused.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 97 suites / 716 tests with integration enabled after installing the live regression suite's required synthetic seller fixture; the focused delivery and capture suites passed 8 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 605 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health and staging state remained private and available while the managed reviewer received the private console-only refusal at the delivery boundary and delivered-order and sold-ticket counts remained zero. Provider, email, payment, and money-movement credentials were absent and no external side effect was invoked. Exact revision `944887c` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Admin order-completion race hardening serializes current administrator identity, verification, role, delivery and buyer-confirmation evidence, successful payment, sold-ticket validation, access-token finalization, seller metrics, launch attribution, pending internal payout creation, and the completed-order transition against access-token persona restoration. A restored managed identity or restoration serialization abort receives the private console-only refusal before order or payout mutation, while an ordinary role downgrade remains independently refused.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 98 suites / 720 tests with integration enabled when the live-boundary suite was explicitly directed to the isolated local server after installing its required synthetic seller fixture; the focused completion suite passed 4 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 604 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health, staging session, and staging state remained private and available while the managed reviewer received the private console-only refusal at the completion boundary and ordinary order and payout counts remained zero. Provider, email, payment, and money-movement credentials were absent and no external side effect was invoked. Exact revision `83ff561` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Admin transfer-proof review race hardening serializes current administrator identity, verification, role, paid-order and pending-review evidence, the single-winner review transition, seller delivery evidence and notification, buyer confirmation delivery, administrator activity delivery, and audit evidence against access-token persona restoration. A restored managed identity or restoration serialization abort receives the private console-only refusal before review mutation or delivery, while an ordinary role downgrade remains independently refused.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 99 suites / 724 tests with integration enabled when the live-boundary suite was explicitly directed to the isolated local server after installing its required synthetic seller fixture; the focused transfer-proof review suite passed 4 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 604 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health, staging session, and staging state remained private and available while the managed reviewer received the private console-only refusal at the transfer-proof review boundary and ordinary order, email-delivery, and notification counts remained zero. Provider, email, payment, and money-movement credentials were absent and no external side effect was invoked. Exact revision `ea05d9c` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Admin dispute-information-request race hardening serializes current administrator identity, verification, role, the open dispute and its append-only request history, email-delivery evidence, user notifications, the review-state update, and audit evidence against access-token persona restoration. Requests for the same dispute lock the order before reading its history so concurrent administrators cannot overwrite one another. Notification creation now accepts the active transaction client, closing the review-identified rollback leak. A restored managed identity or restoration serialization abort receives the private console-only refusal before dispute reads, mutation, or delivery, while an ordinary role downgrade remains independently refused.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 100 suites / 728 tests with integration enabled when the live-boundary suite was explicitly directed to the isolated local server after installing its required synthetic seller fixture; the focused dispute-information suite passed 4 cases and the expanded notification-focused run passed 16 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 604 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health, staging session, and staging state remained private and available while the managed reviewer received the private console-only refusal at the dispute-information boundary and ordinary order, email-delivery, notification, and audit counts remained zero. Provider, email, payment, and money-movement credentials were absent and no external side effect was invoked. Exact revision `75330a1` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Admin dispute-resolution race hardening serializes current administrator identity, verification, role, and the locked current dispute state before payout release, refund-provider access, review-state mutation, access-token restoration, seller metrics, pending internal payout creation, notifications, dispute-email delivery evidence, and audit evidence. Competing administrators must re-read the order after its row lock and cannot repeat a closed resolution; the existing Stripe refund idempotency key remains stable. Dispute email delivery recording now accepts the active transaction client. A restored managed identity or restoration serialization abort receives the private console-only refusal before dispute reads or provider access, while an ordinary role downgrade remains independently refused.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 103 suites / 736 tests with integration enabled when the live-boundary suite was explicitly directed to the isolated local server after installing its required synthetic seller fixture; the focused dispute-resolution and dispute-email transaction suites passed 7 cases, including a fully mocked synthetic refund. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 602 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health, staging session, and staging state remained private and available while the managed reviewer received the private console-only refusal at the refund-resolution boundary and ordinary order, payment, payout, email-delivery, notification, and audit counts remained zero. Provider, email, payment, and money-movement credentials were absent from local validation, and no provider, email, or money side effect was invoked. Exact revision `ba2ce54` was pushed to staging and its Vercel isolated-preview deployment `dpl_2x8XqhbMzZvMYxkZDqYt2zKe13zF` completed successfully from that revision; remote smoke returned private isolated-preview primary health and a private unauthenticated staging-state refusal.
- Buyer dispute-opening race hardening serializes current user identity and buyer-wallet binding with the locked current order state before ticket selection, review-state mutation, administrator and seller notifications, dispute-email delivery evidence, and audit evidence. Competing transitions must re-read the order after its row lock and cannot open the same dispute twice. A restored managed identity or restoration serialization abort receives the private console-only refusal before order reads or delivery, while an ordinary ban remains independently refused.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 104 suites / 741 tests with integration enabled when the live-boundary suite was explicitly directed to the isolated local server after installing its required synthetic seller fixture; the focused dispute-opening race suite passed 5 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 602 existing warnings, default and isolated-preview deployment-path builds, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health and the staging session remained private and available while the managed reviewer received the private console-only refusal at the dispute-opening boundary and ordinary order, audit, notification, and email-delivery counts remained zero. Provider, email, payment, and money-movement credentials were absent from local validation, and no provider, email, or money side effect was invoked. Exact revision `041a7ba` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Buyer/seller dispute-evidence race hardening serializes current user identity and wallet binding with the locked current dispute state before appending evidence, notifying the counterpart, recording dispute-email delivery evidence, and writing audit evidence. Competing evidence submissions and dispute-closing transitions must re-read the order after its row lock, so no append can overwrite another append or survive a concurrently closed dispute. A restored managed identity or restoration serialization abort receives the private console-only refusal before order reads or delivery, while an ordinary ban remains independently refused.
- Verification on a fresh disposable PostgreSQL 16 database deployed all 55 migrations with current status and passed 105 suites / 746 tests with integration enabled when the live-boundary suite was explicitly directed to the isolated local server after installing its required synthetic seller fixture; the focused dispute-evidence race suite passed 5 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 602 existing warnings, default build, the migration-enabled isolated-preview deployment-path build, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health and the staging session remained private and available while the managed reviewer received the private console-only refusal at the dispute-evidence boundary and ordinary order, audit, notification, and email-delivery counts remained zero. Provider, email, payment, and money-movement credentials were absent from local validation, and no provider, email, or money side effect was invoked. Exact revision `99d92cd` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Buyer dispute-cancellation race hardening serializes current user identity and buyer-wallet binding with the locked current dispute state before ticket finalization, seller metrics, pending internal payout creation, order completion, launch attribution, notification, dispute-email delivery evidence, and audit evidence. Competing evidence submissions and administrator resolutions must re-read the order after its row lock and cannot survive buyer closure. A restored managed identity or restoration serialization abort receives the private console-only refusal before order reads or mutation, while an ordinary ban remains independently refused.
- Verification on a fresh disposable PostgreSQL database deployed all 55 migrations with current status and passed 106 suites / 751 tests with integration enabled when the live-boundary suite was explicitly directed to the isolated local server after installing its required synthetic seller fixture; the focused dispute-cancellation race suite passed 5 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 598 existing warnings, default build, the migration-enabled isolated-preview deployment-path build, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health, staging session, and staging state remained private and available while the managed reviewer received the private console-only refusal at the dispute-cancellation boundary and ordinary order, payout, audit, notification, and email-delivery counts remained zero. Provider, email, payment, and money-movement credentials were absent from local validation, and no provider, email, or money side effect was invoked. Exact revision `c784226` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Admin order-reversal race hardening serializes current administrator identity, verification, role, and the locked current order state before cancellation, ticket restoration, and access-token restoration. Competing order transitions must re-read the order after its row lock and cannot reverse an already changed order. A restored managed identity or restoration serialization abort receives the private console-only refusal before order reads or mutation, while an ordinary role downgrade remains independently refused.
- Verification on a fresh disposable PostgreSQL 17 database deployed all 55 migrations with current status and passed 107 suites / 756 tests with integration enabled when the live-boundary suite was explicitly directed to the isolated local server after installing its required synthetic seller fixture; the focused order-reversal race suite passed 5 cases. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, default build, the migration-enabled isolated-preview deployment-path build, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health, staging session, and staging state remained private and available while the managed reviewer received the private console-only refusal at the reversal boundary and ordinary order, payout, and access-token-transaction counts remained unchanged. Provider, email, payment, and money-movement credentials were absent from local validation, and no provider, email, or money side effect was invoked. Exact revision `f9a0f64` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Seller transfer-proof race hardening serializes current user identity and seller-wallet binding with the locked current order state before proof-provider analysis, proof persistence, buyer notification and reminder delivery, and administrator activity delivery. Competing order transitions must re-read the order after its row lock and cannot accept proof for a completed, disputed, or otherwise changed order. A restored managed identity or restoration serialization abort receives the private console-only refusal before order reads or provider access, while an ordinary ban remains independently refused.
- Verification on a fresh disposable PostgreSQL 17 database deployed all 55 migrations with current status and passed 108 suites / 761 tests with integration enabled when the live-boundary suite was explicitly directed to the isolated local server after installing its required synthetic seller fixture; the focused transfer-proof race suite passed 5 cases with a fully mocked proof review. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, default build, the migration-enabled isolated-preview deployment-path build, and diff checks passed. Authenticated isolated local HTTP smoke proved primary health and the staging session remained private and available while the managed organizer received the private console-only refusal at the transfer-proof boundary and ordinary order, notification, reminder-delivery, and email-delivery counts remained zero. Provider, email, payment, and money-movement credentials were absent from local validation, and no provider, email, or money side effect was invoked. Exact revision `92abfc0` was pushed to staging; its isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Corrective seller transfer-proof delivery boundary `6da64a7` replaces the pre-commit global notification and email calls from `92abfc0` with transaction-client staging of the buyer notification, buyer ReminderDelivery intent, and administrator EmailDelivery intent. Only after the Serializable operation commits does an idempotent conditional claim dispatch buyer and administrator email; a rollback, serialization loser, or repeated dispatcher cannot send from an uncommitted intent. Post-commit provider failure is recorded without misreporting the already committed proof submission as failed.
- Verification on a fresh disposable PostgreSQL 17 database deployed all 55 migrations with current status and passed 109 suites / 765 tests in split integration and live-boundary runs; focused transfer-proof coverage passed 9 cases, including a forced rollback, a genuine PostgreSQL Serializable loser, zero pre-commit external sends, zero rolled-back intent residue, and one-time post-commit claims. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, default build, the migration-enabled isolated-preview deployment-path build, live-boundary HTTP checks, and diff checks passed. Provider, email, payment, and money-movement credentials were absent and all delivery calls were mocked in the focused PostgreSQL boundary; no external side effect was invoked. Exact revision `6da64a7` was pushed to staging.
- Recovery successor `3616372` replaces the request-memory-only handoff with a dedicated persisted TransferProofDeliveryIntent outbox. Buyer and administrator payloads are committed with the proof boundary, administrator intent is unconditional on buyer email, and both the immediate route and scheduled transfer workflow drain recoverable rows through bounded leases, compare-and-claim updates, five-minute exponential retry, and a three-attempt ceiling. Legacy ReminderDelivery and EmailDelivery evidence is retained per attempt. Failed and expired PROCESSING claims recover without duplicate concurrent claims, while a process death between commit and immediate dispatch is recovered by the scheduled drainer.
- Fresh disposable PostgreSQL 17 verification applied all 56 migrations and passed the focused route/outbox suites 13/13, including real rollback and P2034 serialization loss, missing-buyer-email administrator staging, persisted process-gap recovery, repeated-drain idempotency, failed and stale claim recovery, provider exception logging, bounded backoff, and attempt exhaustion. The full integration-enabled regression excluding the separately served live-boundary suite passed 108 suites / 762 tests; TypeScript, Prisma format/validation/generation/status, focused and full lint with 0 errors and 595 existing warnings, default and isolated-preview builds, and diff checks passed. Delivery providers were mocked and no external email, provider, payment, or money side effect occurred. Exact revision `3616372` was pushed to staging.
- Send-level successor `9942938` carries the durable outbox idempotency key through buyer and administrator email generation into Resend's provider-level Idempotency-Key header. Because SendGrid does not provide an equivalent safe deduplication contract here, an expired SendGrid processing lease is moved to explicit RECONCILIATION_REQUIRED instead of being automatically resent; fresh SendGrid attempts enter the same reconciliation state before the external call and become DELIVERED only after recorded success. This closes the provider-acceptance/worker-death duplicate-send window without pretending database claims alone provide external exactly-once delivery.
- Focused provider/outbox validation passed 16/16, including the Resend header contract and an ambiguous stale SendGrid acceptance that produces two reconciliation rows and zero resends. Fresh PostgreSQL 17 full integration regression excluding the separately served live suite passed 109 suites / 764 tests. TypeScript, focused and full lint with 0 errors and 595 existing warnings, the default deployment build, and diff checks passed. All provider calls were mocked and no external email or money side effect occurred. Exact revision `9942938` was pushed to staging.
- Retry-hardening successors `dbc578e`, `3275385`, and `b244349` freeze the administrator email timestamp in the committed payload, derive a fixed 83-character provider idempotency key from the durable intent key, quarantine Resend ambiguity once its 24-hour deduplication window expires, and atomically pin the selected provider on first claim. Every later FAILED or expired PROCESSING recovery honors that recorded provider: a Resend claim can be retried only within its window with the identical key, while ambiguous SendGrid or providerless legacy claims require reconciliation and can never cross providers after configuration changes.
- Fresh disposable PostgreSQL 17 verification applied all 58 migrations and passed the focused route/provider/outbox suites 22/22. Coverage includes rollback and genuine Serializable loss with zero pre-commit sends, unconditional administrator staging without buyer email, persisted-row draining, bounded failure recovery, thrown administrator delivery, lost-completion recovery that preserves SENT buyer and administrator evidence, no-credentials-to-configured-provider recovery without a CONSOLE claim, Resend-to-SendGrid and SendGrid-to-Resend environment flips, fixed payload/key reuse, retry-ceiling reconciliation, and explicit ambiguity quarantine. The full integration-enabled regression excluding the separately served live suite passed 109 suites / 771 tests. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, and diff checks passed. Provider calls were mocked and no external email, payment, or money side effect occurred. Exact implementation revision `48ed2dd` was pushed to staging.
- Exhaustion successor `5aec4b8` promotes both legacy exhausted FAILED delivery rows and newly rejected final attempts to explicit RECONCILIATION_REQUIRED state. The promotion is compare-and-update idempotent, preserves the recorded provider and failure evidence, performs no further send, and surfaces its exact promoted-row count through the scheduler result instead of leaving unrecoverable rows silently stranded outside the drainer query.
- Fresh disposable PostgreSQL 17 verification applied all 58 migrations and passed the focused delivery/route suites 21/21. The full integration-enabled regression excluding the separately served live suite passed 109 suites / 772 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, diff checks, and isolated local HTTP health/session/state smoke passed. The smoke proved private no-store responses, access-token persona login, and authenticated synthetic state access. Provider, Stripe, payment, and webhook credentials were absent, delivery providers were mocked, and no external email, provider, payment, or money side effect occurred. Exact implementation revision `5aec4b8` was pushed to staging. An isolated-preview deployment of clean documentation revision `652c3a3` was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as evidence for this checkpoint.
- Retry-window correction `6ffbf53` moves due FAILED Resend rows to RECONCILIATION_REQUIRED when their 24-hour provider idempotency window has expired. The quarantine compare-and-update now matches either the exact FAILED row and availability boundary or the exact expired PROCESSING lease instead of requiring PROCESSING for both cases. Attempted Resend rows missing their first-attempt timestamp also fail closed to reconciliation; neither case performs another provider call or remains silently stranded in the recoverable scan.
- Fresh disposable PostgreSQL 17 verification applied all 58 migrations with current status and passed the focused delivery suite 17/17. The full integration-enabled regression excluding the separately served live-boundary suite passed 109 suites / 774 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, diff checks, and authenticated isolated local HTTP health/session/state smoke passed. The smoke proved private no-store responses, access-token persona login, and authenticated synthetic state access. Provider, Stripe, payment, webhook, and messaging credentials were absent, delivery providers were mocked, and no external email, provider, payment, or money side effect occurred. Exact implementation revision `6ffbf53` was pushed to staging. Its clean documentation revision `6a8160b` deployed successfully to the isolated preview as `dpl_6Y43Yep8HnMrAk68ViywXXpvCmEf`; remote smoke returned private no-store isolated-preview health and a private unauthenticated staging-state refusal.
- Provider-outage retry hardening leaves a due FAILED or expired PROCESSING delivery untouched when its pinned provider credential is temporarily unavailable. A configuration rotation can no longer invoke an unavailable provider, cross to another provider, clear the prior failure, or consume the bounded retry budget; once the pinned provider returns within Resend's idempotency window, the worker reclaims the same durable identity and key. Expired or malformed ambiguity still moves to reconciliation independently of provider availability.
- Fresh disposable PostgreSQL 17 verification applied all 58 migrations with current status and passed the focused delivery suite 18/18. The full integration-enabled regression excluding the separately served live-boundary suite passed 109 suites / 775 tests, and the isolated local live-boundary suite passed 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, diff checks, and authenticated isolated local HTTP health/session/state smoke passed. The smoke proved private no-store responses, access-token persona login, authenticated synthetic state access, and the unauthenticated state refusal. Provider, Stripe, payment, webhook, and messaging credentials were absent, delivery providers were mocked, and no external email, provider, payment, or money side effect occurred. Exact implementation revision `2837106` and validation revision `16374c8` were pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Claim-ownership hardening `2dc981d` gives every transfer-proof outbox lease a fresh compare-and-set token and records a separate durable dispatch boundary. A worker that dies before that boundary can be reclaimed without consuming the provider-attempt budget, while a late worker loses its token before provider access and cannot dispatch. Review correction `3f7bdf7` atomically couples buyer ATTEMPTING evidence to that dispatch claim, so a late worker also cannot overwrite the replacement worker's SENT evidence. Post-dispatch SendGrid ambiguity still quarantines, safe Resend recovery still reuses the same pinned provider identity and key, and missing or unsupported provider evidence fails closed. The migration conservatively marks every legacy PROCESSING row as dispatch-started so an old ambiguous claim can never be reinterpreted as a proven pre-dispatch crash.
- Fresh disposable PostgreSQL 17 verification applied all 59 migrations with current status and passed the focused delivery suite 21/21, including a real overlapping expired-lease reclaim that fenced the late worker, preserved final SENT evidence, and produced one provider call with one consumed attempt. The full integration-enabled regression excluding the separately served live-boundary suite passed 109 suites / 778 tests. A separate upgrade-path fixture proved a legacy PROCESSING row received conservative claim and dispatch evidence. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder, and email-delivery counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked, and no external email, provider, payment, or money side effect occurred. Exact implementation revisions `2dc981d` and `3f7bdf7` were pushed to staging. The isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Concurrent-drainer successor `cdb6781` atomically selects and claims bounded, deterministic batches with PostgreSQL `FOR UPDATE SKIP LOCKED`. Valid intents pinned to a temporarily unavailable provider are excluded before the SQL limit, while malformed or ambiguous provider evidence remains actionable for quarantine, so neither row locks nor an unavailable-provider prefix can starve later deliverable rows. Provider calls remain outside the short claim transaction and behind the per-lease token; attempt counts still advance only at the dispatch boundary. Fresh disposable PostgreSQL 17 verification applied all 59 migrations and passed the focused delivery suite 25/25, including two overlapping drainers partitioning four intents, oldest-first selection, locked-prefix skipping, and unavailable-provider-prefix skipping without changing the prefix or consuming its retry budget. The full integration-enabled regression excluding the separately served live-boundary suite passed 109 suites / 782 tests. Prisma format/validation/generation/status, TypeScript, focused and full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, and diff checks passed. Delivery providers were mocked, no external send occurred, and the disposable database was removed.
- Outcome-evidence successor `c4b346c` fences both buyer and administrator delivery evidence behind the exact active outbox claim. Provider success is recorded only after a conditional ownership assertion in the same transaction as the evidence write, while rejected and thrown outcomes atomically transition the owned outbox row with their failure evidence. A worker returning after its dispatched lease was reclaimed can no longer overwrite the replacement worker's final SENT evidence. Fresh disposable PostgreSQL 17 verification applied all 59 migrations with current status and passed the focused delivery suite 27/27, including overlapping post-dispatch buyer and administrator workers whose late rejected results were fenced after replacement success. The full integration-enabled regression excluding the separately served live-boundary suite passed 109 suites / 784 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, diff checks, and isolated local HTTP health/session/state smoke passed. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked, no external side effect occurred, and the disposable database was removed. An isolated-preview deployment was attempted from the correctly linked repository root after the clean validation revision was pushed, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Atomic-success successor `54e4765` closes the remaining success-side persistence gap: the exact owned outbox claim now transitions to DELIVERED in the same transaction that writes matching buyer ReminderDelivery or administrator EmailDelivery SENT evidence. A crash, transaction rollback, or lease handoff can no longer leave durable SENT evidence while the outbox remains PROCESSING. Loss-injection coverage proves ownership loss commits neither half, after which Resend recovery remains pinned to the same provider identity and idempotency key while ambiguous SendGrid acceptance still quarantines. Fresh disposable PostgreSQL 17 verification applied all 59 migrations with current status and passed the focused delivery suite 27/27 and the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 784 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, payment, and payout counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked, and no external side effect occurred. The implementation revision was pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Provider-identity successor `bf56b14` refuses to record an accepted buyer or administrator delivery when the sender reports a provider different from the exact provider pinned by the owned outbox claim. The ambiguous acceptance moves directly to reconciliation without SENT evidence or a cross-provider retry, preserving the provider identity on which retry safety and idempotency depend. Fresh disposable PostgreSQL 17 verification applied all 59 migrations with current status and passed the focused delivery suite 29/29 and the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 786 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, payment, and payout counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked, and no external side effect occurred. The implementation revision was pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Delivery-envelope successor `2192bdb` validates every claimed buyer and administrator payload before spending an attempt or crossing the provider boundary, including exact ISO timestamps, positive ticket counts, required descriptive fields, and the administrator mailbox binding. Malformed or recipient-divergent rows move directly to reconciliation without provider, reminder, or email-delivery evidence. Fresh disposable PostgreSQL 17 verification applied all 59 migrations with current status and passed the focused delivery suite 30/30 and the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 787 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, payment, and payout counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked, and no external side effect occurred. The implementation and validation revisions were pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Delivery-identity successor `ed8cd75` binds the provider idempotency identity to the exact persisted order, normalized six-hour window, delivery kind, and recipient before dispatch. A changed recipient, kind, window, order, or durable key can no longer send a divergent envelope under a previously reserved provider identity; malformed rows move directly to reconciliation without consuming an attempt or writing delivery evidence. Fresh disposable PostgreSQL 17 verification applied all 59 migrations with current status and passed the focused delivery suite 31/31, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 788 tests, and the isolated local live-boundary suite at 7/7. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, payment, and payout counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked, and no external side effect occurred. The implementation revision was pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Full-envelope successor `1374b9b`, with upgrade-fixture corrections `fdeb463` and `8438dad`, preserves the established provider idempotency key while binding every new transfer-proof delivery to a canonical full-envelope digest and freezing the persisted order, kind, recipient, payload, durable key, identity version, digest, pinned provider, and first-attempt evidence at the PostgreSQL boundary. Migration 60 grandfathers only preexisting rows as version 1, requires all later inserts to carry version 2 digest evidence, and keeps legacy attempted rows on their original provider key. A genuine 59-to-60 upgrade fixture preserved byte-for-byte keys for seeded PENDING and ambiguous PROCESSING Resend rows and rejected a new legacy-identity insert after migration. Fresh disposable PostgreSQL 17 verification at exact corrective tip `8438dad` applied all 60 migrations with current status and passed the focused delivery suite 34/34 and the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 791 tests; the previously recorded isolated local live-boundary suite remained 7/7. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, and diff checks passed. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked, and no external side effect occurred. The exact corrective implementation revisions were pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Lifecycle-integrity successor `4fa4939` makes every committed transfer-proof delivery status carry a coherent evidence shape at the PostgreSQL boundary. PENDING rows have no provider or attempt evidence; PROCESSING rows require an owned, chronological lease and an attempt-consistent dispatch boundary; FAILED and DELIVERED rows require pinned providers and matching attempt evidence; and reconciliation rows cannot retain an active claim. Migration 61 takes a short write lock, preserves provider, attempt, first-attempt, and possible delivered-at evidence, and moves every incoherent legacy row to RECONCILIATION_REQUIRED without inferring provider success. Its upgrade fixture proves coherent PENDING and DELIVERED rows remain unchanged, ambiguous PROCESSING and providerless FAILED rows are quarantined, SQL nulls fail closed, and impossible later transitions are rejected. Fresh disposable PostgreSQL 17 verification applied all 61 migrations with current status and passed the focused delivery suite 36/36, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 793 tests, and the isolated local live-boundary suite at 7/7. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, order, payment, payout, primary-payment-attempt, and primary-refund-attempt counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked, and no external side effect occurred. The implementation and validation revisions were pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Transition-integrity successor `aa2d9da` restricts transfer-proof delivery updates to the implemented lifecycle graph, makes attempt evidence monotonic, permits attempt increments only at the exact owned PROCESSING dispatch boundary, and prevents DELIVERED or RECONCILIATION_REQUIRED outcomes from being resurrected or having their terminal evidence rewritten. Existing recovery fixtures now bypass triggers only while installing synthetic historical states, leaving every exercised runtime transition subject to the database boundary. Fresh disposable PostgreSQL 17 verification applied all 62 migrations with current status and passed the focused delivery suite 37/37, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 794 tests, and the isolated local live-boundary suite at 7/7. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, order, payment, payout, primary-payment-attempt, and primary-refund-provider-attempt counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked, and no external side effect occurred. The implementation revision was pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Forward-migration correction `5448ca8` preserves the already-deployed migration 62 checksum and ships provider immutability at the dispatch boundary as migration 63. A pinned provider can no longer change in the same update that increments the attempt count, and an explicit 62-to-63 upgrade fixture proves the tightening applies to an existing PROCESSING row while an unchanged-provider dispatch remains valid. Fresh disposable PostgreSQL 17 verification applied all 63 migrations with current status and passed the focused delivery suite 39/39, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 796 tests, and the isolated local live-boundary suite at 7/7. Prisma format/validation/generation/status, TypeScript, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, order, payment, payout, primary-payment-attempt, and primary-refund-provider-attempt counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked, and no external side effect occurred. The exact corrective revision was pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- First-attempt-integrity successor `eef85c8` preserves migrations 62 and 63 and forward-replaces the transition function in migration 64. Every future first dispatch must now persist `firstAttemptAt` as the exact `dispatchStartedAt` instant while later retries retain their original first-attempt evidence. A genuine 63-to-64 upgrade fixture proves the formerly permitted divergent timestamps are rejected after the forward migration and that an exact first-dispatch timestamp remains valid. Fresh disposable PostgreSQL 17 verification applied all 64 migrations with current status and passed the focused delivery suite 41/41, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 798 tests, and the isolated local live-boundary suite at 7/7. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-provider-event, and primary refund-provider-event counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. The implementation revision was pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Claim-handoff-integrity successor `fc83f4a` preserves every deployed transition migration and forward-replaces the transition function in migration 65. A dispatched claim can no longer erase or rewrite its dispatch evidence under the same active lease, and a competing owner cannot take over early; reset is permitted only with a distinct claim token at or after the prior logical lease expiry. The narrow Resend accepted-result recovery transition remains available without erasing dispatch evidence. A genuine 64-to-65 upgrade fixture proves the formerly permitted same-claim reset is rejected after the forward migration while an expired-lease successor remains valid. Fresh disposable PostgreSQL 17 verification applied all 65 migrations with current status and passed the focused delivery suite 43/43, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 800 tests, and the isolated local live-boundary suite at 7/7. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-provider-event, and primary refund-provider-event counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. The implementation revision was pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Replay-handoff-integrity successor `c6c164b` preserves all 65 deployed migrations and forward-replaces the transition function in migration 66. An expired claim that crossed SendGrid's provider boundary can no longer be reset for replay, and a Resend successor claim is allowed only while its original 24-hour provider idempotency identity remains active; safe pre-dispatch handoffs remain available. A genuine 65-to-66 upgrade fixture proves the previously accepted dispatched SendGrid handoff is rejected after the forward migration, while current-schema coverage proves an in-window Resend handoff remains valid and an expired Resend identity is refused. Fresh disposable PostgreSQL 17 verification applied all 66 migrations with current status and passed the focused delivery suite 45/45, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 802 tests, and the isolated local live-boundary suite at 7/7. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-provider-event, and primary refund-provider-event counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. The exact implementation revision was pushed to staging. An isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Attempt-identity successor `113e960` preserves every deployed transition migration and adds migration 67 to freeze the provider and first-attempt timestamp once any transfer-proof delivery attempt exists. Neither a standalone update nor a later claim handoff can rewrite the evidence that determines provider replay eligibility. A genuine 66-to-67 upgrade fixture proves both formerly permitted rewrites are rejected while the established SendGrid replay fence and original attempt identity remain intact. Fresh disposable PostgreSQL 17 verification applied all 67 migrations with current status and passed the focused delivery suite 46/46, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 803 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-provider-event, and primary refund-provider-attempt counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. The exact implementation revision was pushed to staging. Vercel deployment `56i7D49XdPuwZvGhKffCqSwKLewU` successfully applied migration 67 and deployed the exact implementation to the protected isolated preview; CLI-authenticated remote smoke returned private, no-store isolated-preview health and the private unauthenticated staging-state refusal.
- Claim-acquisition successor `ccbaeb3` preserves all 67 deployed migrations and forward-replaces the transition function in migration 68. A PENDING or FAILED delivery must now be due and acquire a live pre-dispatch claim before its attempt counter and dispatch marker can advance; a retry can no longer combine claim acquisition with a dispatch marker while leaving its attempt count unchanged. A genuine 67-to-68 upgrade fixture proves the formerly permitted combined retry transition is rejected while the implemented claim-then-dispatch sequence remains valid. Fresh disposable PostgreSQL 17 verification applied all 68 migrations with current status and passed the focused delivery suite 48/48, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 805 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-provider-event, and primary refund-provider-attempt counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. The exact implementation revision was pushed to staging. Its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Dispatch-bound-completion successor `457cbdd` preserves all 68 deployed migrations and forward-replaces the transition function in migration 69. DELIVERED evidence must now descend from the current PROCESSING row's recorded provider dispatch, retain its attempt count, and carry a completion timestamp at or after that dispatch. An already-attempted FAILED intent can therefore no longer acquire a pre-dispatch retry claim and be promoted directly to DELIVERED without crossing the provider boundary again. A genuine 68-to-69 upgrade fixture proves the formerly permitted shortcut is rejected after the forward migration while the implemented dispatch-then-completion sequence remains valid. Fresh disposable PostgreSQL 17 verification applied all 69 migrations with current status and passed the focused delivery suite 50/50, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 807 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed and approved the migration and tests with no actionable findings. The exact implementation revision was pushed to staging. Its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Dispatch-bound-failure successor `0f6e847` preserves all 69 deployed migrations and forward-replaces the transition function in migration 70. FAILED evidence must now descend from the current PROCESSING row's recorded provider dispatch, retain its attempt count, and schedule the next retry strictly after that dispatch. An already-attempted FAILED intent can therefore no longer acquire a pre-dispatch retry claim and return directly to FAILED, nor can a dispatched rejection make itself eligible before its dispatch boundary. A genuine 69-to-70 upgrade fixture proves the formerly permitted undispatched failure is rejected after the forward migration while the implemented dispatch-then-failure sequence remains valid. Fresh disposable PostgreSQL 17 verification applied all 70 migrations with current status and passed the focused delivery suite 52/52, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 809 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed and approved the migration and tests with no actionable findings. The exact implementation revision was pushed to staging. Its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Failure-evidence successor `0362026` preserves all 70 deployed migrations and forward-replaces the transition function in migration 71. Once a retryable FAILED outcome is committed, its provider, attempt identity, rejection error, and durable retry schedule cannot be rewritten by a same-state update; the evidence remains frozen until the due row acquires a pre-dispatch retry claim or is escalated for reconciliation. A genuine 70-to-71 upgrade fixture proves the formerly permitted failure rewrite is rejected after the forward migration while the implemented due claim remains valid. Fresh disposable PostgreSQL 17 verification applied all 71 migrations with current status and passed the focused delivery suite 54/54, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 811 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed and approved the migration and tests with no actionable findings. The exact implementation revision was pushed to staging. Its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Pending-schedule successor `5494f13` preserves all 71 deployed migrations and adds migration 72 to freeze a newly staged PENDING intent's durable `availableAt` boundary until claim acquisition. A same-state update can no longer accelerate or postpone provider delivery, while metadata-only updates and the existing due PENDING-to-PROCESSING claim path remain valid. The batch fixture now establishes synthetic ordering at initial staging rather than rewriting protected state, and a genuine 71-to-72 upgrade fixture proves the formerly permitted schedule rewrite is rejected after the forward migration. Fresh disposable PostgreSQL 17 verification applied all 72 migrations with current status and passed the focused delivery suite 56/56, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 813 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-provider-event, and primary refund-provider-attempt counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed and approved the migration and tests with no actionable findings. The exact implementation revision was pushed to staging. Its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Processing-evidence successor `ea86eb5` preserves all 72 deployed migrations and adds migration 73 to bind every active PROCESSING row's retry schedule and error evidence to the implemented dispatch, expired-lease handoff, and recovery transitions. Dispatch cannot rewrite `availableAt` or `lastError`; a successor owner must preserve the schedule and clear stale errors; and the same-owner collapsed-lease recovery shape is now limited to Resend attempts one and two with retained dispatch evidence and a post-dispatch retry schedule. Standalone active-evidence rewrites and forged SendGrid recovery therefore fail at PostgreSQL, while the implemented pre-dispatch handoff and accepted-Resend persistence-loss recovery remain valid. A genuine 72-to-73 upgrade fixture proves the previously permitted SendGrid recovery rewrite is rejected after the forward migration. Fresh disposable PostgreSQL 17 verification applied all 73 migrations with current status and passed the focused delivery suite 58/58, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 815 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed and approved the migration and tests with no actionable findings. The exact implementation revision was pushed to staging. Its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Reconciliation-evidence successor `665ad04` preserves all 73 deployed migrations and adds migration 74 to bind every PROCESSING- or FAILED-to-RECONCILIATION_REQUIRED escalation to its durable source evidence. Terminal quarantine can no longer fabricate a provider, attempt count, first-attempt timestamp, or delivered timestamp; FAILED escalation preserves its frozen retry boundary, while PROCESSING escalation may retain its existing schedule or record only the implemented deterministic post-dispatch boundary for attempts one through three. Current-schema coverage proves fabricated delivery and failed-schedule evidence are rejected while pre-dispatch quarantine and exhausted-failure escalation remain valid, and a genuine 73-to-74 upgrade fixture proves the previously permitted fabricated terminal delivery timestamp is rejected after the forward migration. Fresh disposable PostgreSQL 17 verification applied all 74 migrations with current status and passed the focused delivery suite 60/60, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 817 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed and approved the migration and tests with no actionable findings. The exact implementation revision was pushed to staging. Its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Claim-schedule successor `367c2d6` preserves all 74 deployed migrations and adds migration 75 to bind PENDING- and FAILED-to-PROCESSING claim acquisition to the durable `availableAt` schedule recorded by its source row. A worker may record its actual processing and lease boundary but can no longer accelerate, postpone, or replace the initial-delivery or retry schedule while taking ownership. Current-schema coverage proves both pending and retry schedule rewrites fail while the implemented retry claim remains valid, and a genuine 74-to-75 upgrade fixture proves the formerly permitted claim-time rewrite is rejected after the forward migration.
- Fresh disposable PostgreSQL 17 verification applied all 75 migrations with current status and passed the focused delivery suite 62/62, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 819 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, and payout counts remained zero in the smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed and approved the migration and tests with no actionable findings. The disposable server and database were removed. The exact implementation and validation revisions were pushed to staging; a protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Claim-lease successor `0d4946e` preserves all 75 deployed migrations and adds migration 76 to bind every PENDING or FAILED claim and every expired PROCESSING handoff to the implemented exact 15-minute lease. The claim start must also be no more than 30 seconds ahead of the database's explicit timezone-independent UTC statement clock, so a forged future start cannot retain ownership for an arbitrary interval. Initial intents now persist the service's captured staging time as their durable availability boundary. Current-schema coverage rejects short, long, and future-dated pending, retry, and handoff leases; a genuine 75-to-76 upgrade fixture proves the previously permitted future claim is rejected after the forward migration and proves a current UTC claim remains valid under a non-UTC PostgreSQL session.
- Fresh disposable PostgreSQL 17 verification applied all 76 migrations with current status and passed the focused delivery suite 64/64, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 821 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim's review identified and verified corrections for the future-start and timezone-coercion boundaries, then approved the migration and tests with no actionable findings. The disposable server and databases were removed. The exact implementation and validation revisions were pushed to staging; a protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Dispatch-clock successor `398fbdc` preserves all 76 deployed migrations and adds migration 77 to bind every provider-attempt increment's `dispatchStartedAt` boundary to the exact `processingAt` clock captured by its owned claim. A forged later dispatch instant inside an otherwise valid lease can no longer shift first-attempt identity, retry scheduling, completion evidence, or provider-idempotency timing. Current-schema coverage rejects divergent first and retry dispatch clocks while preserving the implemented exact-clock transition, and a genuine 76-to-77 upgrade fixture proves the formerly permitted divergent timestamp is rejected after the forward migration.
- Fresh disposable PostgreSQL 17 verification applied all 77 migrations with current status and passed the focused delivery suite 66/66, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 823 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed and approved the migration and tests with no actionable findings. The disposable server and databases were removed. The exact implementation and validation revisions were pushed to staging; a protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Retry-schedule successor `38f5365` preserves all 77 deployed migrations and adds migration 78 to bind retryable provider rejection and accepted-Resend persistence recovery to the dispatcher's exact exponential schedule: five minutes after attempt one and ten minutes after attempt two. A writer can no longer accelerate or postpone the next replay after crossing the provider boundary. Current-schema coverage rejects early and late FAILED schedules and forged recovery schedules while preserving both valid recovery branches; a genuine 77-to-78 upgrade fixture installs migration 77, proves its formerly permitted divergent schedule, then proves migration 78 rejects that schedule and accepts the implemented boundary.
- Fresh disposable PostgreSQL 17 verification applied all 78 migrations with current status and passed the focused delivery suite 68/68 and the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 825 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim identified the upgrade-fixture predecessor gap, verified its correction and the explicit attempt-two recovery case, then approved the migration and tests with no actionable findings. The disposable server and databases were removed. The exact implementation and validation revisions were pushed to staging; a protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Claim-clock successor `a0fd116` preserves all 78 deployed migrations and adds migration 79 to require every new or handed-off delivery claim to own a lease that remains live at PostgreSQL's timezone-independent statement clock. An attempted Resend claim must also remain strictly inside its original 24-hour idempotency window at that database boundary. Historical `processingAt` evidence can therefore no longer create an already-expired owner or move a retry back inside an elapsed replay window. Current-schema coverage rejects stale pending leases and expired-window Resend claims while preserving current pending and in-window retry claims; a genuine 78-to-79 predecessor-stack fixture proves the stale lease was formerly permitted, then proves migration 79 rejects both unsafe shapes and accepts the implemented current claim under a non-UTC database session.
- Fresh disposable PostgreSQL 17 verification applied all 79 migrations with current status and passed the focused delivery suite 70/70, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 827 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed and approved the migration and tests with no actionable findings. The disposable server and databases were removed. The exact implementation and validation revisions were pushed to staging. The protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Dispatch-window successor `80067ad` preserves all 79 deployed migrations and adds migration 80 to require every provider-attempt increment to occur while its owned worker lease remains live at PostgreSQL's timezone-independent statement clock. An attempted Resend retry must also cross the provider boundary strictly inside its original 24-hour idempotency window; acquiring a claim just before that window closes can no longer authorize a later dispatch. Current-schema coverage rejects an expired lease and an elapsed Resend replay window while preserving an in-window retry, and a genuine 79-to-80 upgrade fixture proves both formerly permitted dispatches are rejected after the forward migration under a non-UTC PostgreSQL session.
- Fresh disposable PostgreSQL 17 verification applied all 80 migrations with current status and passed the focused delivery suite 72/72, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 829 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed and approved the migration and tests with no actionable findings. The disposable server and database were removed. The exact implementation and validation revisions were pushed to staging. The protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Delivery-clock successor `ab53e79` preserves all 80 deployed migrations and adds migration 81 to bind successful delivery evidence to the exact clock captured when the owned worker crossed the provider boundary. The existing completion guard rejects missing or earlier evidence; the new trigger rejects a later `deliveredAt`, closing the remaining range to exact equality without weakening claim ownership, provider pinning, or terminal lifecycle enforcement. Current-schema coverage rejects a forged later completion and preserves the exact-clock transition, while a genuine 80-to-81 upgrade fixture proves the formerly permitted forward timestamp is rejected after the forward migration.
- Fresh disposable PostgreSQL 17 verification applied all 81 migrations with current status and passed the focused delivery suite 74/74, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 831 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed and approved the migration and tests with no actionable findings. The disposable server and database were removed. Exact implementation revision `ab53e79` and validation revision `4e6c8ce` were pushed to staging. A protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Result-lease successor `f1e1659` preserves all 81 deployed migrations and adds migration 82 to require the dispatching worker's lease to remain live at PostgreSQL's timezone-independent statement clock when it records successful delivery, retryable provider failure, or accepted-Resend persistence-recovery evidence. An expired worker can no longer authorize a durable outcome or retry merely because no successor has acquired the row yet. `RECONCILIATION_REQUIRED` remains deliberately outside this restriction so an ambiguous stale dispatch can still be quarantined without claiming delivery or authorizing replay. Current-schema coverage rejects all three unsafe expired-lease result shapes while preserving live completion and expired-lease reconciliation; the genuine forward-upgrade fixture applies the actual migration 79→80→81 predecessor chain, proves the former completion gap, then proves migration 82 closes it without blocking quarantine.
- Fresh disposable PostgreSQL 17 verification applied all 82 migrations with current status and passed the focused delivery suite 76/76, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 833 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim's review identified the predecessor-stack and explicit quarantine-coverage gaps, verified both corrections, and approved the migration and tests with no remaining actionable findings. The disposable server and database were removed. Exact implementation revision `f1e1659` and validation revision `b8fbce9` were pushed to staging. A protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Append-only-history successor `5454605` preserves all 82 deployed migrations and adds migration 83 to prevent either row deletion or table truncation from erasing durable transfer-proof delivery authorization, provider identity, retry, result, and reconciliation evidence. The PostgreSQL suite now confines synthetic cleanup bypass to a transaction-local replication-role override, rejects both erasure paths on the current schema with the staged rows preserved, and uses the real migration 79→80→81→82 predecessor stack to prove DELETE and TRUNCATE were formerly permitted before the forward migration closes both gaps.
- Fresh disposable PostgreSQL 17 verification applied all 83 migrations with current status and passed the focused delivery suite 78/78, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 835 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed the trigger scope, test-only cleanup isolation, deletion/truncation coverage, predecessor stack, and application call sites and approved the migration and tests with no actionable findings. The disposable server and database were removed. Exact implementation revision `5454605` and validation revision `e4d6914` were pushed to staging. A protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- History-metadata successor `44f5831` preserves all 83 deployed migrations and adds migration 84 to make transfer-proof delivery row identity and chronology database-owned. Fresh inserts receive one PostgreSQL UTC trigger-execution clock regardless of caller-supplied timestamps; later updates preserve `id` and `createdAt` and assign `updatedAt` as the greatest of that trigger clock, the immutable creation time, and one millisecond after its previous value. Ordinary lock waits and future-dated or internally divergent legacy metadata therefore cannot move durable history backward. Current-schema coverage rejects identity replacement and forged timestamps and exercises a real two-client row-lock wait. The genuine 83-to-84 non-UTC upgrade fixture proves existing divergent timestamps survive migration unchanged and the first subsequent write restores monotonic chronology.
- Fresh disposable PostgreSQL 17 verification applied all 84 migrations with current status and passed the focused delivery suite 81/81, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 838 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim identified and verified corrections for statement-start clocks, future legacy update clocks, and divergent legacy creation/update chronology, then approved the migration and tests with no remaining actionable findings. The disposable server and database were removed. Exact implementation revision `44f5831` and validation revision `9108952` were pushed to staging. A protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Pending-origin successor `25443ce` preserves all 84 deployed migrations and adds migration 85 to require every new transfer-proof delivery intent to originate as the canonical staged authorization: `PENDING`, provider-free, attempt zero, and without claim, dispatch, retry, result, reconciliation, or error evidence. A direct writer can no longer fabricate a terminal or in-progress row at creation and then rely on the append-only guards to preserve false history. Current-schema coverage rejects pending-with-evidence, processing, failed, delivered, and reconciliation origins with no residue. The genuine 84-to-85 fixture proves a coherent terminal origin was formerly accepted, the forward migration rejects it, the canonical pending origin remains valid, and pre-existing history is untouched.
- Fresh disposable PostgreSQL 17 verification applied all 85 migrations with current status and passed the focused delivery suite 83/83, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 840 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim reviewed the replacement insert guard, fail-closed null handling, current-schema cases, and forward-upgrade evidence and approved the migration and tests with no actionable findings. The disposable server and database were removed. Exact implementation revision `25443ce` and validation revision `d6fadd9` were pushed to staging. A protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Origin-clock successor `2b0506a` preserves all 85 deployed migrations and adds migration 86 to make a new transfer-proof delivery's initial schedule database-owned. On insert, one PostgreSQL volatile UTC clock replaces caller-supplied `availableAt`, `createdAt`, and `updatedAt`, making the canonical pending authorization immediately due while preventing either backdated authorization or a future-dated recovery suppression. Existing rows receive no backfill. Current-schema coverage normalizes both past and future forgeries to one clock and proves the resulting row can acquire a valid database-clock claim. The genuine 85-to-86 non-UTC fixture proves the predecessor accepted a forged future schedule, the forward migration normalizes both future and past inputs, and every legacy history clock remains byte-for-byte unchanged.
- Fresh disposable PostgreSQL 17 verification applied all 86 migrations with current status and passed the focused delivery suite 85/85, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 842 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim independently identified the origin-schedule gap, reviewed the single-clock trigger, trigger ordering, test seams, current-schema claim proof, and forward-upgrade fixture, and approved the migration and tests with no actionable findings; his optional byte-for-byte legacy-clock evidence polish was incorporated and reverified. The disposable servers and databases were removed. Exact implementation revision `2b0506a` and validation revision `060d6da` were pushed to staging. A protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Order-provenance successor `bfe0eda` preserves all 86 deployed migrations and adds migration 87 to bind every new transfer-proof delivery intent to one existing ordinary marketplace `Order`. The foreign key restricts parent deletion and rekeying so append-only delivery authorization and provider-attempt history cannot be orphaned later. It is added `NOT VALID` for immutable legacy compatibility and automatically validates on databases with no historical orphan; any legacy orphan remains intact for review while every new orphan is rejected, and the forward fixture proves validation can complete after restoring the missing parent without deleting delivery evidence. Prisma exposes the same restricted relation, and the focused suite now stages delivery work only for real synthetic order parents.
- Fresh disposable PostgreSQL 17 verification applied all 87 migrations with current status and passed the focused delivery suite 88/88, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 845 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim identified the missing order provenance, reviewed the foreign-key design and legacy handling, required the upgrade fixture to restore the missing parent rather than erase append-only history, and approved the corrected migration and tests with no remaining actionable findings. The disposable server and database were removed. Exact implementation revision `bfe0eda` and validation revision `a9f8d39` were pushed to staging. A protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Order-subject successor `994ce05` preserves all 87 deployed migrations and adds migration 88 so a self-consistent delivery envelope cannot borrow an unrelated existing order. Every new intent locks an eligible paid transfer-proof Order, its authoritative seller and optional buyer identities, and a stable nonempty OrderItem set. Buyer and administrator kinds, recipients, participant fields, buyer first name, ticket count, proof type, and confirmation deadline must match that locked snapshot. The parent and child lock order serializes concurrent item insertion, deletion, and reparenting before authorization; legacy delivery history remains byte-for-byte untouched. `completedAt` and the buyer window remain immutable digest-bound envelope metadata because the legacy Order has no structured authoritative source for those clocks.
- Fresh disposable PostgreSQL 17 verification applied all 88 migrations with current status and passed the focused delivery suite 92/92, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 849 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, authenticated isolated local HTTP health/session/state smoke, and exact-preview health/state smoke passed; transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim identified the cross-order subject, payload, and concurrent item-set gaps, reviewed each correction, and approved the final trigger, lock order, forward-upgrade evidence, and tests with no remaining actionable findings. Exact implementation revision `994ce05` was pushed to staging and deployed successfully to the protected isolated preview as `dpl_GbKcDwatSJKiagWS1MUKQPUkDTEA`; remote smoke returned private no-store isolated-preview health and a private unauthenticated staging-state refusal.
- Delivery-identity authority successor `d9753c7` preserves all 88 deployed migrations and adds migration 89 to bind every new transfer-proof delivery's durable idempotency key and SHA-256 digest to its exact canonical order, normalized six-hour window, kind, recipient, and fixed-shape payload before insertion. The atomic upgrade preflight rejects any noncanonical version-2 history instead of leaving a poisoned canonical key in place, while genuine version-1 provider history remains untouched. PostgreSQL owns the accepted flat payload grammar, canonical safe-integer ticket count, exact UTC clocks, key derivation, and digest recomputation. The application independently compares the row returned by each idempotent upsert, so a surviving legacy collision fails loudly and rolls back its notification rather than suppressing legitimate delivery. Current-schema coverage rejects forged keys, digests, extra fields, and legacy collisions; proves Node/PostgreSQL agreement across null, quote, backslash, newline, and Unicode values; and proves concurrent distinct envelopes in one window produce one committed canonical pair and one transactional loser. The genuine 88-to-89 fixture proves a poisoned version-2 row blocks the atomic upgrade without mutation, reviewed repair permits reapplication, valid version-1 history remains byte-for-byte intact, and only canonical new identity is accepted.
- Fresh disposable PostgreSQL 17 verification applied all 89 migrations with current status and passed the focused delivery suite 97/97, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 854 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed. The isolated smoke responses were private and non-cacheable, unauthenticated state access was refused, and transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts were zero after synthetic regression residue was cleared. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim identified the legacy collision, canonicalization, concurrency, and silent-upsert gaps, verified every correction, and approved the migration, application defense, and tests with no remaining actionable findings. The disposable databases were removed. Exact implementation revision `d9753c7` was pushed to staging and deployed successfully to the protected isolated preview as `dpl_G1r4yTryAyqVqfSn9QKnzcxcxSYt`; CLI-authenticated remote smoke returned private no-store isolated-preview health and a private unauthenticated staging-state refusal.
- Delivery-clock authority successor `26c386f` preserves all 89 deployed migrations and adds migration 90 so the accepted Order deadline is the only durable transfer-proof delivery clock. Buyer `windowStart` is the normalized six-hour bucket of `disputeWindowEndsAt - 24 hours`, administrator `completedAt` is that exact instant, and caller-supplied processing time remains only a scheduling input. The upgrade takes an early write-conflicting table lock through commit, then atomically preflights all version-2 history; invalid current history blocks deployment while version-1 provider evidence remains byte-for-byte untouched. The later-named insert trigger preserves established order-subject lock and error ordering before enforcing both clocks. Runtime staging now creates the in-app notification only after the canonical intent upserts, so concurrent retries serialize before notification absence is checked. Current-schema coverage rejects self-consistent forged clocks, proves different caller clocks collapse to one durable envelope and one notification after retry, and the genuine two-session 89-to-90 fixture proves a concurrent invalid writer cannot pass between preflight and trigger installation.
- Fresh disposable PostgreSQL 17 verification applied all 90 migrations with current status and passed the focused delivery suite 99/99, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 856 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed. Local and exact-preview smoke responses were private and non-cacheable, unauthenticated state access was refused, and transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim identified the migration preflight race, verified the early table-lock correction and real two-session fixture, and approved the final migration, application change, and tests with no actionable findings. Exact implementation revision `26c386f` was pushed to staging and deployed successfully to the protected isolated preview as `dpl_kjpEZ3vmbPrMAbTsdrtRBmDiXbti`; CLI-authenticated remote smoke returned private no-store isolated-preview health and a private unauthenticated staging-state refusal.
- Identity-revalidation successor `2514c6c` preserves all 90 deployed migrations and adds migration 91 to close the migration-89 snapshot race without rewriting shipped history. Migration 89's identity preflight could finish while an already-running permissive insert remained invisible, after which that writer could commit a clock-correct but malformed version-2 key or digest; migration 90's locked clock-only audit would accept it. The successor takes an immediate write-conflicting table lock and atomically recomputes the fixed payload shape, normalized identity window, durable key, canonical-envelope SHA-256, and authoritative Order clock for every version-2 row. Invalid current history blocks deployment without mutation, while version-1 provider evidence remains byte-for-byte untouched. The genuine two-session 89-to-90-to-91 fixture reproduces the missed in-flight insert, proves migration 90 accepts its correct clock, proves migration 91 rejects it and rolls back, proves the new early lock waits for and observes a concurrent corrupting writer, and proves reviewed repair permits clean reapplication.
- Fresh disposable PostgreSQL 17 verification applied all 91 migrations with current status and passed the focused delivery suite 100/100, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 857 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local HTTP health/session/state smoke passed. Local and exact-preview smoke responses were private and non-cacheable, unauthenticated state access was refused, and transfer-proof, reminder-delivery, email-delivery, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim independently identified the migration-89 identity race, reviewed the successor lock, helper dependencies, validation ordering, rollback behavior, and forward-upgrade evidence, and approved the final migration and tests with no actionable findings. Exact implementation revision `2514c6c` was pushed to staging and deployed successfully to the protected isolated preview as `dpl_7qDdp9ZnLnBPqxy5UhYCNejVArE1`; CLI-authenticated remote smoke returned private no-store isolated-preview health and a private unauthenticated staging-state refusal.
- Buyer-notification idempotency successor adds migration 92 so transfer-proof confirmation notifications no longer deduplicate unrelated orders merely because the same buyer receives them in one six-hour window. New notifications use a fixed SHA-256 identity over the notification namespace, exact Order, buyer User, and authoritative delivery window. A global nullable unique key serializes retries while preserving all legacy notification history as unkeyed NULL rows. Runtime reuse verifies the complete canonical user, type, link, message, and key and fails the surrounding transaction if a preexisting row owns the identity with different content, so poisoned history cannot silently suppress either delivery intent or the buyer notification. PostgreSQL coverage proves distinct same-window orders create distinct notifications, concurrent retry still converges to one notification, collision handling rolls back both canonical delivery intents, and the forward-only migration leaves legacy rows untouched while enforcing uniqueness for new keyed rows.
- Fresh disposable PostgreSQL 17 verification applied all 92 migrations with current status and passed the focused delivery suite 103/103, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 860 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its required synthetic seller fixture. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state smoke passed. Local and exact-preview smoke responses were private and non-cacheable, unauthenticated state access was refused, and transfer-proof, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts were zero after synthetic regression residue was cleared. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent, delivery providers were mocked in tests, and no external side effect occurred. Jim independently reviewed the canonical key scope, concurrent retries, legacy preservation, collision rollback, application ordering, and migration lock behavior and approved the staging successor with no blocking finding; the ordinary unique-index build remains an explicit maintenance-window or concurrent-index consideration before any future busy production rollout. Exact implementation revision `fcb2ade` was pushed to staging and deployed successfully to the protected isolated preview as `dpl_27khjUT8owWu8Ff4NSHTUbXV5QwQ`; CLI-authenticated remote smoke returned private no-store isolated-preview health and a private unauthenticated staging-state refusal.
- Transfer-proof snapshot-binding successor adds migration 93 and makes an accepted seller proof a one-shot boundary. Once an Order has its delivery deadline, repeated seller submissions are rejected before upload or provider analysis. Version-2 delivery intents now reverse-bind their immutable recipient, transfer type, ticket count, and deadline snapshots to the source Order, OrderItem membership, and active participant identity. Forward migration preflight rejects existing drift atomically; database guards prevent later source drift, while terminal delivery history does not freeze ordinary participant profile maintenance. The migration locks Order, OrderItem, User, and delivery-intent tables in a route-compatible sequence, and two-session coverage proves both participant-update preflight closure and forward progress for the application's Order-before-intent transaction order.
- Fresh disposable PostgreSQL 17 verification applied all 93 migrations with current status and passed the focused transfer-proof delivery and route suites at 114/114, the full integration-enabled regression excluding the separately served live-boundary suite at 109 suites / 865 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/transfer-proof boundary smoke passed. Local and exact-preview health responses were private and non-cacheable, unauthenticated state access was refused, and the unauthenticated transfer-proof route stopped at its CSRF boundary. Transfer-proof delivery-intent, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, and outreach credentials were absent from local verification, delivery providers were mocked in tests, and no external side effect occurred. Jim identified the initial route lock-order and participant-preflight races, reviewed the corrected reverse guards and two-session fixtures, and explicitly approved the final migration and tests with no actionable findings. Exact implementation revision `f044796` was pushed to staging and deployed successfully to the protected isolated preview as `dpl_59dFP7AwQGy2daC3ccGQUvzjzKXn`; CLI-authenticated remote smoke returned private no-store isolated-preview health and a private unauthenticated staging-state refusal.
- Human-review outbox successor `f38656e`, with clock-skew regression revision `12fce5a`, preserves all 93 prior migrations and adds migration 94 so seller human-review requests commit their immutable Order snapshot, audit, and frozen delivery envelope before any provider work. Current identity and the Order row are serialized together; a non-null accepted-proof deadline refuses replacement before mutation or delivery. The resumable delivery worker uses a stable SHA-256 provider key and PostgreSQL UTC-owned acquisition, exact 15-minute leases, dispatch, retry, completion, accepted-Resend replay, quarantine, and final reconciliation evidence; caller clocks cannot advance work. Fresh disposable PostgreSQL 17 verification applied all 94 migrations with current status and passed the focused transfer-proof delivery/review/route suites at 129/129, the full integration-enabled regression excluding the separately served live-boundary suite at 111 suites / 887 tests, and the isolated local live-boundary suite at 7/7 after installing its required synthetic seller fixture. Prisma validation/generation/status, TypeScript, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, and source diff checks passed. Delivery providers were mocked in the focused database tests, and no provider I/O occurred. Jim reviewed the identity/Order race, rollback boundary, provider handoff, ambiguity recovery, lifecycle trigger, database clocks, malformed-envelope quarantine, expired claims, and caller-clock skew and explicitly approved the corrected checkpoint with no remaining blocker. Exact validation revision `12fce5a` was pushed to staging and deployed successfully to protected isolated preview `dpl_2EaAqsqJZ5bkjxyBsxbc13RUXiSb`; CLI-authenticated smoke returned private no-store isolated-preview health, a private unauthenticated staging-state refusal, and the human-review POST stopped at the cross-origin boundary without route work.
- Human-review envelope successor `62f0bf8` preserves the prior 94 migrations and adds migration 95. Because migration 94 did not retain authoritative render inputs, its forward preflight refuses any unreconciled historical review row instead of grandfathering unverifiable content. New review requests persist an exact seller, email, deterministic first-ticket title, and allowlisted application-origin snapshot; PostgreSQL reconstructs and authenticates the complete subject, text, HTML, request identity, and UTF-8 length-framed SHA-256 digest while holding the Order, Seller, User, OrderItem, and Ticket snapshot. The snapshot and rendered envelope are immutable. Direct inserts with a forged subject, text body, HTML body, or extra payload field fail even with the legitimate request and a recomputed digest. Worker selection also excludes unavailable-provider prefixes before its bounded SQL limit while retaining stale rows that can be quarantined without provider access, so pinned outages cannot starve later deliverable work. Fresh disposable PostgreSQL 17 verification applied all 95 migrations with current status and passed the focused transfer-proof delivery/review/route suites at 135/135, the full integration-enabled regression excluding the separately served live-boundary suite at 111 suites / 893 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/human-review boundary smoke passed. Local health was private and non-cacheable, unauthenticated state access was refused, the human-review POST stopped at the cross-origin boundary, and review-delivery intent, transfer-delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero in the dedicated smoke database. Provider, Stripe, payment, webhook, messaging, outreach, OCR, and phone credentials were absent from local validation; delivery providers were mocked in tests, and no external side effect occurred. Jim independently reviewed Node/PostgreSQL render and digest parity, deterministic item selection, payload exactness, preflight race behavior, immutable fencing, and provider-prefix eligibility and approved the checkpoint with no actionable blocker. Exact implementation revision `62f0bf8` was pushed to staging and deployed successfully to protected isolated preview `dpl_HprJBA18K1oWTQjeqXJqQATyEv5x`; CLI-authenticated remote smoke returned private no-store isolated-preview health, a private unauthenticated staging-state refusal, and the human-review POST stopped at the cross-origin boundary without route work.
- Human-review environment-binding successor `b4f6426` preserves all 95 prior migrations and adds migration 96. The review link origin is no longer selected per row from a cross-environment allowlist: PostgreSQL derives one fixed origin from the exact `primary_preview` identity or the narrowly recognized disposable-test identities, rejects near-match archive, production-copy, and backup names, and refuses an unknown database before an empty-outbox upgrade can install. The migration takes a write-conflicting table lock before its origin preflight, so an in-flight predecessor insert is observed after the lock wait and blocks atomically. Application staging derives the same fixed origin internally and validates every configured application-origin value, so precedence cannot conceal disagreement. Current-schema coverage rejects a fully rerendered and redigested staging envelope in the test database, near-match identities, and unknown identity; forward fixtures prove both concurrent cross-environment history and a separate unknown empty database roll back without migration residue. Fresh disposable PostgreSQL 17 verification applied all 96 migrations with current status and passed the focused review-delivery/origin/route suites at 32/32, the full integration-enabled regression excluding the separately served live-boundary suite at 112 suites / 899 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its required synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/human-review boundary smoke passed. Local primary health was private and non-cacheable, unauthenticated staging-state access was refused privately, the human-review POST stopped at the cross-origin boundary, and review-delivery intent, transfer-delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent from local validation; delivery providers were mocked in database tests, and no external side effect occurred. Jim identified the caller-selected origin, unknown-empty-database, and broad near-match classification gaps, reviewed the corrected exact mapping, unconditional locked preflight, Node/PostgreSQL parity, and real concurrent/unknown-database fixtures, and explicitly approved the final diff with no actionable finding. Exact implementation revision `b4f6426` was pushed to staging and deployed successfully to protected isolated preview `dpl_5Aw5nT35FJhYcfD1ivdSLVrd9TMV`; the deployment confirmed the authoritative `primary_preview` database identity and all 96 migrations current. CLI-authenticated remote smoke returned private no-store isolated-preview health, a private unauthenticated staging-state refusal, and the human-review POST stopped at the cross-origin boundary without route work.
- Scheduler recovery-isolation successor `2c343fd` decouples accepted-proof delivery, human-review delivery, and ordinary transfer reminders at their shared authenticated cron boundary. Each component is attempted sequentially even when a predecessor fails, while the run still fails closed with one aggregate incident and structured per-component audit evidence. Focused coverage proves accepted-proof failure cannot starve review delivery or reminders, review-delivery failure cannot starve reminders, successful response compatibility, and authentication before all work. Fresh disposable PostgreSQL 17 verification applied all 96 migrations with current status and passed 113 suites / 903 tests with integration enabled while excluding the separately served live-boundary suite. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/unauthenticated-cron smoke passed. Local primary health was private and non-cacheable, the scheduler refused an unauthenticated request, and accepted-proof delivery-intent, review-delivery-intent, ordinary order, payment, and payout counts remained zero. Provider, Stripe, webhook, OCR, and messaging credentials were absent, no external provider was invoked, the disposable database and server were removed, and Jim approved the failure-isolation diff with no actionable finding. Exact validation revision `adbb6f2` was pushed to staging and deployed successfully to the protected isolated preview as `dpl_Ft1to4S949vstL6vQM6ZvNySqaq5`; CLI-authenticated remote smoke returned private no-store isolated-preview health, a private unauthenticated staging-state refusal, and an unauthenticated scheduler refusal without invoking recovery work. The admin review decision path still requires a dedicated durable seller-decision outbox; a direct post-commit seller send is not an acceptable final boundary.
- Admin-decision delivery successor `2b97322` adds migration 97 and moves transfer-proof review decisions fully behind the durable transfer-proof delivery lifecycle. APPROVE, REJECT, and REQUEST_INFORMATION commit the Order decision, seller notification, immutable seller email envelope, accepted-proof buyer/Admin work when applicable, and audit evidence in one serializable transaction; no provider is invoked before commit. A stable request decision identity, PostgreSQL statement clock, environment-bound origin, exact recipient, actor, action, note, and frozen subject/text/HTML are authenticated at insertion, while the existing provider pinning, 15-minute claims, bounded retries, Resend replay fence, ambiguity quarantine, append-only history, and authenticated cron recovery apply to the new decision kind. The seller user id is part of the exact fixed-shape, digested, PostgreSQL-authorized envelope and is the only notification recipient source; active decision rows freeze the seller's first name. Seller-decision email is FIFO per order by authenticated decision time and row id: every older nonterminal row, including `RECONCILIATION_REQUIRED`, blocks later seller-decision email until human reconciliation resolves it. Fresh disposable PostgreSQL 17 verification applied all 97 migrations with current status and passed the focused PostgreSQL delivery suite at 117/117 plus the route race suite at 5/5. The full integration-enabled regression excluding the separately served live-boundary suite passed 113 suites / 912 tests, and the isolated local live-boundary suite passed 7/7 after installing and removing its synthetic seller fixture. Prisma validation/generation/status, TypeScript, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, and source diff checks passed. Provider delivery was mocked in database tests and no external email, provider, payment, or money side effect occurred. Validation revision `9b7c4ac` was pushed to staging and deployed successfully to protected isolated preview `dpl_DqBMisG46NpGefS94dZt8RR9LdJR`; the deployment confirmed the authoritative `primary_preview` database identity and all 97 migrations current. CLI-authenticated remote smoke returned private no-store isolated-preview health, a private unauthenticated staging-state refusal, a cross-origin admin-review refusal before route work, and an unauthenticated scheduler refusal.
- Seller-decision runtime envelope-shape hardening successor `5b27469` requires the worker to observe exactly the same 11 canonical payload fields that PostgreSQL authenticated at insertion. A redigested row with either an injected field or a missing nullable `sellerFirstName` is quarantined before provider dispatch, closing the runtime fail-closed boundary even under synthetic trigger-bypass corruption. Fresh disposable PostgreSQL 17 verification applied all 97 migrations with current status and passed the focused delivery suite at 119/119 and the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 914 tests. Prisma format/validation/generation/status, TypeScript, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP smoke passed. Health was private and non-cacheable, unauthenticated staging-state and scheduler access were refused, the admin-review route stopped at the cross-origin boundary, and delivery-intent, notification, order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero. Provider and payment credentials were absent and no external side effect occurred.
- Seller-decision database-clock recovery successor `00bed8c` removes the scheduler caller's clock from due-row selection, stale-claim and Resend-window decisions, lease acquisition, dispatch, completion, retry, and delivery-evidence timestamps. Each acquired row carries one PostgreSQL-owned worker clock through the existing exact-clock transition fences. An expired final `PROCESSING` claim at the three-attempt budget now becomes explicit `RECONCILIATION_REQUIRED` work exactly once inside the acquisition transaction instead of remaining permanently ineligible for both exhaustion handling and candidate selection; no fourth provider attempt is authorized. Fresh disposable PostgreSQL 17 verification applied all 97 migrations with current status and passed the focused delivery suite at 122/122, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 917 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed. Health was private and non-cacheable, unauthenticated staging state and scheduler access were refused, the admin-review route stopped at the cross-origin boundary, and transfer-proof delivery intent, review-delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery providers were mocked in database tests and no external side effect occurred. Jim identified the caller-clock and expired-final-claim gaps, reviewed both corrections and their seller-decision regressions, and approved the final diff with no actionable issue. Exact implementation revision `00bed8c` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Seller-decision schedule successor `caf9ba2` adds migration 98 and binds every new decision email's initial `availableAt` to its database-authenticated `decidedAt`, preventing a caller from silently delaying an otherwise valid durable decision envelope. The per-order FIFO selector now compares canonical fixed-width UTC text and falls back to immutable creation evidence for malformed restored rows instead of casting untrusted JSON directly; a poisoned decision is therefore quarantined before provider dispatch without aborting the shared delivery scan. Fresh disposable PostgreSQL 17 verification applied all 98 migrations with current status and passed the focused delivery suite at 124/124, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 919 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed. Health was private and non-cacheable, unauthenticated staging state and scheduler access were refused, the admin-review route stopped at the cross-origin boundary, and transfer-proof delivery intent, review-delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery providers were mocked in database tests and no external side effect occurred. Jim reviewed the migration lock and preflight, schedule binding, timestamp semantics, trigger composition, poisoned-payload fallback, FIFO behavior, and regressions and approved the diff with no actionable finding. Exact implementation revision `caf9ba2` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel again refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Seller-decision origin-clock composition successor `123870b` adds migration 99 after finding that the alphabetically earlier generic history-metadata trigger replaced a new decision's `availableAt` with `clock_timestamp()` before migration 98's exact schedule trigger compared it with the route's database-authenticated `statement_timestamp()`. In deployed database-clock mode that composition could reject every otherwise valid seller-decision insert. The metadata trigger now leaves seller-decision availability for the later exact-envelope schedule trigger to authorize while retaining PostgreSQL-owned creation and update history; all other transfer-proof intent kinds preserve their existing database-origin schedule. A dedicated regression exercises the real database-origin function and proves both the exact decision schedule and later database-owned history, while the forged-schedule rejection remains intact. Fresh disposable PostgreSQL 17 verification applied all 99 migrations with current status and passed the focused delivery suite at 125/125, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 920 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed. Health remained private and non-cacheable; staging state and the scheduler refused unauthenticated access, the admin-review route stopped at the CSRF boundary, and transfer-proof delivery intent, review-delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery providers were mocked in database tests and no external side effect occurred. Jim reviewed the migration preflight, trigger ordering, exact schedule rejection, history-clock separation, regression, and final diff and approved it with no actionable finding. Exact implementation revision `123870b` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel again refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Seller-decision runtime-origin hardening requires the worker to independently derive the one application origin allowed by its current isolated environment before dispatch. A structurally valid seller-decision row whose origin, rendered link, and digest were all rewritten through synthetic trigger-bypass corruption is now quarantined before provider I/O instead of becoming a phishing-capable delivery. Fresh disposable PostgreSQL 17 verification applied all 99 migrations with current status and passed the focused delivery suite at 126/126 and the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 921 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed. Health was private and non-cacheable, staging state was unavailable to an anonymous caller, the scheduler refused unauthenticated access, the admin-review route stopped at the cross-origin boundary, and transfer-proof delivery intent, review-delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery was mocked in database tests and no external side effect occurred. Jim reviewed the exact worker change and adversarial regression and approved them with no actionable finding. Exact implementation revision `b60c701` and validation record `93e5447` were pushed to staging; no protected-preview evidence is claimed because the subsequent combined deployment attempt was refused at Vercel's free daily quota.
- Human-review runtime-origin hardening applies the same independent environment check to the durable support-review worker. The database trigger already rejected cross-environment writes, but a structurally valid row whose origin, rendered review links, and full-envelope digest were all rewritten through synthetic trigger-bypass corruption could previously remain self-consistent at runtime; it is now quarantined before provider I/O. Fresh disposable PostgreSQL 17 verification applied all 99 migrations with current status and passed the focused review-delivery suite at 22/22, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 922 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed. Health was private and non-cacheable, staging state was unavailable to an anonymous caller, the scheduler refused unauthenticated access, the admin-review route stopped at the cross-origin boundary, and review-delivery intent, transfer-proof delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery was mocked in database tests and no external side effect occurred. Jim reviewed the exact two-file worker and adversarial regression diff and approved it with no actionable finding. Exact implementation revision `4df7582` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Accepted-proof runtime envelope-shape hardening successor `db46993` requires both buyer-confirmation and administrator-activity workers to observe exactly the same canonical payload keys that PostgreSQL authenticated at insertion, including required key presence for nullable buyer name and email snapshots. A trigger-bypass-corrupted row whose injected field and full-envelope digest remain self-consistent is now quarantined at attempt zero before either provider path and cannot be reclaimed. Fresh disposable PostgreSQL 17 verification applied all 99 migrations with current status and passed the focused delivery suite at 128/128, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 924 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed. Health was private and non-cacheable, staging state was unavailable to an anonymous caller, the scheduler refused unauthenticated access, the admin-review route stopped at the cross-origin boundary, and review-delivery intent, transfer-proof delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery providers were mocked in database tests and no external side effect occurred. Jim reviewed the exact two-file worker and adversarial PostgreSQL regression diff and approved it with no actionable finding. Exact implementation revision `db46993` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Runtime subject-revalidation successor `e146609` binds every identity-version-2 transfer delivery's final authorization check to the same transaction that records its owned dispatch boundary. Buyer confirmation re-locks the parent order, current guarded buyer identity, and deterministically ordered item membership before comparing the recipient, name, exact deadline, and ticket count; administrator activity additionally re-locks the seller identity and compares both participant addresses and the frozen proof type; seller decisions re-lock the seller identity and require the exact decision id, action, note, clock, and decider to remain in durable append-only review history. The lock sequence mirrors insert-side authorization, while identity-version-1 recovery remains explicitly grandfathered. Self-consistently redigested trigger-bypass corruption is quarantined at attempt zero before provider I/O, and a real two-session race proves a buyer-profile change waits behind runtime validation/dispatch ownership, is then rejected by the active-intent reverse guard, and cannot redirect the provider recipient. Fresh disposable PostgreSQL 17 verification applied all 99 migrations with current status and passed the focused delivery suite at 133/133, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 929 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed. Health was private and non-cacheable, staging state was unavailable to an anonymous caller, the scheduler refused unauthenticated access, the admin-review route stopped at the cross-origin boundary, and review-delivery intent, transfer-proof delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery providers were mocked in database tests and no external side effect occurred. Jim reviewed the final lock order, legacy behavior, adversarial cases, and two-session race and approved the exact two-file diff with no actionable finding. Exact implementation revision `e146609` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Seller-decision FIFO-clock hardening successor `00a7143` adds migration 100 and prevents a regex-shaped but impossible restored `decidedAt` value from controlling per-order predecessor ordering. The database helper parses the untrusted timestamp inside an exception-safe boundary, requires an exact canonical UTC round trip, and otherwise uses immutable creation evidence; the worker compares the resulting clock and immutable id as one deterministic tuple. A redigested later decision with an impossible month can no longer place itself before an earlier valid decision and permanently strand that valid delivery after quarantine. Fresh disposable PostgreSQL 17 verification applied all 100 migrations with current status and passed the focused delivery suite at 134/134, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 930 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed. Health was private and non-cacheable, staging state was unavailable to an anonymous caller, the scheduler refused unauthenticated access, the admin-review route stopped at the cross-origin boundary, and transfer-proof delivery intent, review-delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, and primary refund-provider-attempt counts remained zero. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery providers were mocked in database tests and no external side effect occurred. Jim identified the impossible-clock ordering gap and approved the exact migration, worker, and adversarial regression diff with no actionable finding. Exact implementation revision `00a7143` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- FIFO-clock parser and quarantine-order correction `a6ff30a` adds migration 101 and replaces the migration-100 exception block with PostgreSQL's non-throwing `pg_input_is_valid` predicate before the exact UTC millisecond round trip. The corrected adversarial regression creates the impossible decision first, gives its canonical successor a later decision clock, and proves immutable creation fallback selects and quarantines the poisoned predecessor at attempt zero without provider work; its terminal reconciliation status then deliberately remains the explicit gate that prevents a later decision from being silently dispatched. Fresh disposable PostgreSQL 17 verification applied all 101 migrations with current status and passed the focused delivery suite at 134/134, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 930 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma validation/generation/status, TypeScript, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/session/state/scheduler/admin-review boundary smoke passed; transfer-proof delivery intent, review-delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery providers were mocked in database tests and no external side effect occurred. Jim explicitly approved the exact revision with no actionable SQL, timestamp, FIFO/quarantine, adversarial, or upgrade-coverage finding. Exact implementation revision `a6ff30a` was pushed to staging and deployed successfully to protected isolated preview `dpl_3PqeaVJwhLjS5fRPsUWN4vs1NWNd`; the build applied migration 101 to the authoritative `primary_preview` database. CLI-authenticated remote smoke returned private no-store isolated-preview health, a private unauthenticated staging-state refusal, an unauthenticated scheduler refusal, and a cross-origin admin-review refusal before route work.
- Durable-history FIFO successor `e32d064` removes the remaining seller-decision ordering dependency on restored payload clocks. A trigger-bypass-corrupted earlier intent can no longer use a syntactically valid, self-consistently redigested far-future `decidedAt` value to make a later real decision appear first: predecessor authorization now compares only PostgreSQL-owned immutable `(createdAt, id)` history, while the payload clock remains independently required for exact envelope and durable-decision validation. The adversarial regression proves the poisoned head is quarantined at attempt zero without provider work and its reconciliation state continues to gate the canonical successor. Fresh disposable PostgreSQL 17 verification applied all 101 migrations with current status and passed the focused delivery suite at 135/135, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 931 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/session/state/scheduler/admin-review boundary smoke passed; transfer-proof delivery intent, review-delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery providers were mocked in database tests and no external side effect occurred. Jim explicitly approved the exact two-file diff with no actionable ordering, legacy, concurrency, or test-adequacy finding. Exact implementation revision `e32d064` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Human-review runtime-subject successor `ae8b77a` closes the remaining provider-redirection gap in the durable human-review outbox. A trigger-bypass-corrupted row can no longer self-consistently replace the fixed Support recipient, idempotency key, and envelope digest and reach provider I/O: dispatch now re-locks the parent Order and requesting seller identity, requires the exact durable request id/time/actor, requires the fixed Support mailbox, and records the owned dispatch boundary in that same transaction. The frozen, insertion-authenticated render snapshot deliberately remains valid across ordinary seller profile changes. Fresh disposable PostgreSQL 17 verification applied all 101 migrations with current status and passed the focused review-delivery suite at 24/24, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 933 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed; review-delivery intent, transfer-proof delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after cleanup. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery providers were mocked in database tests and no external side effect occurred. Jim identified and verified the correction for mutable rendered-snapshot sources, then approved the exact two-file implementation diff with no remaining actionable finding. Exact implementation revision `ae8b77a` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Live-lease dispatch successor `358b6a3` prevents runtime subject locking from carrying an acquired transfer-proof delivery past its worker lease and into provider I/O. Both buyer and non-buyer dispatch transactions now read a fresh database-owned clock after completing their parent, participant, item-membership, and durable-decision locks, then require the exact acquired lease to remain strictly live in the same compare-and-set that records the dispatch boundary. A real blocked-parent-lock race advances the database clock after acquisition and proves both stale buyer and administrator workers leave attempt, dispatch, projection, and provider evidence untouched; the next drain exclusively reclaims and delivers both pre-dispatch claims once, and a third drain is empty. Fresh disposable PostgreSQL 17 verification applied all 101 migrations with current status and passed the focused transfer-delivery suite at 136/136, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 934 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed; transfer-proof delivery intent, review-delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after cleanup. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery providers were mocked in database tests and no external side effect occurred. Jim requested the complete reclaim/no-op proof and then approved the exact two-file implementation diff with no remaining actionable finding. Exact implementation revision `358b6a3` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Obsolete human-review successor `1736a01` revalidates the review eligibility state under the same parent-order lock used for dispatch authorization. If an administrator has already approved or rejected the request, the support-review worker now quarantines the stale envelope at attempt zero instead of sending obsolete review links; the terminal row preserves reconciliation evidence and cannot be reclaimed. Fresh disposable PostgreSQL 17 verification applied all 101 migrations with current status and passed the focused review-delivery suite at 26/26 and the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 936 tests. Prisma format/validation/generation/status, TypeScript, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed; review-delivery intent, transfer-proof delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after cleanup. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery was mocked in database tests and no external side effect occurred. Jim approved the exact two-file implementation diff with no actionable state-semantics, lock-order, quarantine, or coverage finding. Exact implementation revision `1736a01` and validation record `1b3961e` were pushed to staging; the protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Human-review live-lease successor `bf95293` prevents parent-order and seller-identity locking from carrying an acquired support-review delivery beyond its worker lease and into provider I/O. After runtime subject validation completes, the dispatch compare-and-set now reads a fresh database clock and requires the exact acquired lease to remain strictly live. A PostgreSQL-backed blocked-parent-lock regression injects a post-lock clock beyond the lease, proves the stale worker leaves attempt, dispatch, provider, and email evidence untouched, then proves an expired pre-dispatch claim is reclaimed and delivered exactly once while a third drain is empty. Fresh disposable PostgreSQL 17 verification applied all 101 migrations with current status and passed the focused review-delivery suite at 27/27, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 937 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed; review-delivery intent, transfer-proof delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after cleanup. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery was mocked in database tests and no external side effect occurred. Jim approved the exact two-file implementation diff with no actionable finding. Exact implementation revision `bf95293` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Unsupported-review-provider recovery successor `2efc997` brings the human-review drainer in line with the accepted-proof worker for restored legacy evidence. An expired pre-dispatch claim carrying a non-null provider outside the current `RESEND | SENDGRID` allowlist is now selected ahead of provider-specific dispatch branches, preserved under its recorded identity, and moved to `RECONCILIATION_REQUIRED` without falling through to the currently configured provider or performing provider I/O. The PostgreSQL regression temporarily removes and then restores the provider allowlist constraint only in the disposable database to model a restored historical table, proves the unsupported row becomes explicit terminal reconciliation work, and proves a later drain cannot reclaim it. Fresh disposable PostgreSQL 17 verification applied all 101 migrations with current status and passed the focused review-delivery suite at 28/28, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 938 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed; review-delivery intent, transfer-proof delivery intent, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after cleanup. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; delivery was mocked in database tests and no external side effect occurred. Jim reviewed and approved the exact two-file implementation and restored-schema fixture with no actionable correction, then accepted the complete validation evidence. Exact implementation revision `2efc997` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Failed unsupported-review-provider successor `2aacaa6` adds migration 102 so restored attempt evidence can complete the recovery path introduced by `2efc997`. Migration 94 previously admitted `FAILED -> RECONCILIATION_REQUIRED` only after the third attempt, so a due attempt-one row with an unsupported pinned provider was selected by the worker and then aborted the entire acquisition transaction at the history trigger. The new narrow trigger split admits only that exact legacy transition, preserves every source field except status, the exact provider-derived reconciliation explanation, and the previously unconstrained update timestamp, and leaves every supported-provider and other history transition under the existing guard. The PostgreSQL regression proves altered source scheduling is rejected, the canonical failed row becomes terminal without provider I/O, and a later drain is empty. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused review-delivery suite at 29/29, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 939 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, isolated local HTTP health/state/scheduler/admin-review boundary smoke, and CLI-authenticated exact-deployment smoke passed; both disposable databases retained zero review-delivery intent, transfer-proof delivery intent, email-delivery, notification, ordinary order, payment, payout, and primary provider-attempt/event residue after cleanup. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent from the verification processes; delivery was mocked in database tests and no external side effect occurred. Jim approved the exact migration and regression diff with no actionable transition-safety, trigger-coverage, or test-adequacy finding. Exact implementation revision `2aacaa6` was pushed to staging and deployed successfully to protected isolated preview `dpl_GLUR7hSVhT6vJj12zdPP87ZiE41j`; the deployment confirmed the authoritative `primary_preview` database current at all 102 migrations. Remote smoke returned private no-store isolated-preview health, a private unauthenticated staging-state refusal, an unauthenticated scheduler refusal, and a cross-origin admin-review refusal before route work.

- Exhausted unsupported-review-provider successor `508a556` closes the remaining ordering gap between legacy-provider quarantine and generic retry exhaustion. A restored `FAILED` row at the three-attempt boundary previously reached the generic promotion first and aborted because migration 102 correctly requires the exact provider-derived reconciliation explanation. The worker now performs a database-clock-owned pre-pass limited to exhausted failed rows with non-null unsupported providers, changes only status, the authenticated provider-derived explanation, and update time, then leaves ordinary exhaustion and the existing attempt-one acquisition path unchanged. The PostgreSQL regression reproduces the former transaction abort, proves explicit terminal reconciliation without provider I/O, and proves provider, attempt count, first-attempt identity, and schedule remain intact. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused review-delivery suite at 30/30, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 940 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed; review-delivery intent, transfer-proof delivery intent, email-delivery, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after cleanup. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent from local validation; delivery was mocked in database tests and no external side effect occurred. Jim independently reviewed the exact two-file diff and approved its transition safety, scope, trigger compatibility, concurrency behavior, and coverage with no actionable finding. The implementation revision was pushed to staging and deployed successfully to protected isolated preview `dpl_2Xtb9wC5ZG6bYpcK5pGvAnJJNRKs`; the build confirmed the authoritative `primary_preview` database current at all 102 migrations. CLI-authenticated exact-deployment smoke returned private no-store isolated-preview health, a private unauthenticated staging-state refusal, an unauthenticated scheduler refusal, and a cross-origin admin-review refusal before route work.

- Concurrent unsupported-review-provider successor `a9da3ce` fences the inter-statement restore race left between the dedicated unsupported-provider pre-pass and generic exhausted-failure promotion. Generic exhaustion now admits only null or currently supported provider evidence, so PostgreSQL rechecks and excludes a row that changes to an unsupported provider while the update waits; the row remains failed for the next dedicated provider-derived quarantine instead of aborting the drainer with the wrong reconciliation explanation. The deterministic PostgreSQL regression injects that provider change after the pre-pass and immediately before generic exhaustion, proves the first drain preserves the failed evidence without provider I/O, then proves the next drain records the exact terminal provider-derived explanation. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused review-delivery suite at 31/31, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 941 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed; review-delivery intent, transfer-proof delivery intent, email-delivery, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after cleanup. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent from local verification; delivery was mocked in database tests and no external side effect occurred. Jim independently approved the exact two-file diff with no actionable Prisma-filter, transition-safety, concurrency, or coverage correction. Exact implementation revision `a9da3ce` was pushed to staging and deployed successfully to protected isolated preview `dpl_8twf5wwk7uy1NGL7w5BxcPdVtYi4`; the build confirmed the authoritative `primary_preview` database current at all 102 migrations. CLI-authenticated exact-deployment smoke returned private no-store isolated-preview health, a private unauthenticated staging-state refusal, an unauthenticated scheduler refusal, and a cross-origin admin-review refusal before route work.

- Concurrent review-envelope ownership successor `973c446` closes both privileged-restore windows around the human-review provider boundary. Dispatch now preserves the established parent-order and seller lock order, re-locks the acquired intent, revalidates its current canonical envelope, and compares every immutable identity, rendered-body, payload, digest, request-clock, and creation field before incrementing the provider attempt. The dispatch, successful-result, and failed-result compare-and-sets all reuse that exact acquired-envelope predicate. If a self-consistent replacement lands after the dispatch transaction commits, the worker cannot attribute `DELIVERED`, `FAILED`, or `EmailDelivery` evidence to content it did not send; retained lifecycle ownership is instead atomically quarantined as explicit reconciliation work, regardless of provider acceptance or rejection, and cannot replay. Deterministic PostgreSQL regressions inject a redigested replacement both after acquisition and from inside accepted and rejected provider callbacks, proving pre-dispatch refusal, post-dispatch reconciliation, no false delivery record, and no second provider call. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused review-delivery suite at 34/34, the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 944 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed; review-delivery intent, transfer-proof delivery intent, email-delivery, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after cleanup. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent from local verification; delivery providers were mocked in database tests and no external side effect occurred. Jim identified the post-dispatch evidence-attribution window, verified the exact-predicate and terminal-reconciliation correction, and approved the amended two-file implementation diff with no remaining actionable lock-order, ownership, replay, or coverage finding. Exact implementation revision `973c446` was pushed to staging and deployed successfully to protected isolated preview `dpl_8F38ZEMtRGhufWPYey6KpZDSBQVV`; its build confirmed the authoritative `primary_preview` database current at all 102 migrations. CLI-authenticated exact-deployment smoke returned private no-store isolated-preview health, a private unauthenticated staging-state refusal, an unauthenticated scheduler refusal, and a cross-origin admin-review refusal before route work.

- Rejected-result provider-identity successor `165ddde` treats every returned provider identity as dispatch evidence, including rejected results, across buyer confirmation, administrator activity, seller decision, and human-review delivery. A rejection attributed to a provider other than the pinned provider now becomes terminal `RECONCILIATION_REQUIRED` work without writing a falsely attributed failed `ReminderDelivery` or `EmailDelivery` projection, and a later drain cannot replay it. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the combined focused delivery suites at 174/174 and the full integration-enabled regression excluding the separately served live-boundary suite at 113 suites / 948 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed; review-delivery intent, transfer-proof delivery intent, email-delivery, notification, ordinary order, payment, and payout counts remained zero after cleanup. Provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent from validation; delivery providers were mocked in database tests and no external side effect occurred. Jim identified the analogous three-branch transfer-delivery gap, reviewed the amended four-file diff, and approved it with no remaining actionable finding. Exact implementation revision `165ddde` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Provider-credential normalization successor `e4e7c71` gives provider selection, pinned-provider availability checks, and the sender one shared credential-cleaning boundary. Empty, quote-only, quote-wrapped-whitespace, repeated-quote, and nested-quote values are all unavailable after dequoting and a final trim; they can no longer pin or consume a Resend attempt that the sender cannot execute, and a valid SendGrid credential may be selected for genuinely new work without changing any already pinned provider identity. Helper coverage proves the malformed-empty classes and provider precedence, while PostgreSQL-backed transfer-proof and human-review cases prove the selected provider, attempt, and terminal delivery evidence agree with the sender. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused helper/delivery suites at 186/186, the full integration-enabled regression excluding the separately served live-boundary suite at 114 suites / 960 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP health/state/scheduler/admin-review boundary smoke passed; transfer-proof delivery intent, review-delivery intent, reminder-delivery, email-delivery, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero. Real provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; provider outcomes were mocked and no external side effect occurred. Jim identified the quoted-whitespace fixed-point gap, reviewed the corrected shared helper and deterministic regressions, and approved the final diff with no remaining actionable finding. Exact implementation revision `e4e7c71` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Provider-evidence alignment successor `f022a19` extends the normalized credential boundary to every remaining shared email-provider inspection. Legacy transfer reminders now select one normalized provider, persist that identity before dispatch, and pass the same provider explicitly to the sender. Dispute, administrator information-request, and Stripe webhook delivery projections persist the provider returned by the completed attempt instead of inferring it from mutable environment state; repeated dispute projections also refresh the provider identity. Verification health remains externally generic while applying sender-equivalent normalization, the debug Resend client rejects malformed-empty credentials, and successful verification responses derive their development-delivery marker from returned CONSOLE evidence rather than a post-send configuration read.
- Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed seven focused provider/evidence suites at 36/36, the full integration-enabled regression excluding the separately served live-boundary suite at 115 suites / 965 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 595 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP primary-health, staging-state, scheduler-authorization, and verification-health smoke passed; transfer-proof delivery intent, review-delivery intent, reminder-delivery, email-delivery, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after cleanup. Real provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; provider results were mocked and no external side effect occurred. Jim identified the post-send verification-classification gap, reviewed the amended fifteen-file implementation diff and its route-level evidence assertions, and approved it with no remaining actionable finding. Exact implementation revision `f022a19` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Provider-setup containment successor `a2de749` moves SendGrid client initialization inside the sender's provider error boundary. A synchronous setup failure can no longer reject `sendEmail` before pinned provider evidence reaches durable delivery callers; it now resolves as a bounded `SENDGRID`/`PROVIDER_ERROR` result, performs no send, and cannot fall through to another provider. Provider catch bindings also use `unknown` with bounded non-Error fallback text. The deterministic sender regression proves the setup exception, exact provider evidence, diagnostic, and absence of provider dispatch. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused sender suite at 3/3, the full integration-enabled regression excluding the separately served live-boundary suite at 115 suites / 966 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with zero errors, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP primary-health, staging-state, scheduler-authorization, admin-review origin, and verification-health smoke passed. Real provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; the provider failure was mocked and no external side effect occurred. Jim independently reviewed the behavioral two-file diff, reran the focused suite, and approved it with no actionable behavior, coverage, typing, lint, or adjacent provider-evidence finding. Exact implementation revision `a2de749` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Returned-provider evidence successor `b1d0569` makes provider identity mandatory on every `EmailSendResult` and removes ambient credential configuration from post-attempt evidence classification. The shared evidence helper now preserves only the provider returned by the completed sender boundary; a wrapper-level unexpected rejection is explicitly `CONSOLE` / `EXCEPTION_WITHOUT_PROVIDER_EVIDENCE`, so a configured Resend or SendGrid key cannot be persisted as historical attempt evidence when no trustworthy provider identity was returned. Focused wrapper and Stripe projection regressions prove the neutral failure result and prove configured Resend state cannot create false Resend delivery evidence. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed eight focused suites at 46/46, the full integration-enabled regression excluding the separately served live-boundary suite at 115 suites / 968 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP primary-health, access-token session/state, scheduler authorization, admin-review origin, and verification-health smoke passed. Transfer-proof delivery intent, review-delivery intent, reminder-delivery, email-delivery, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after cleanup. Real provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; provider outcomes were mocked and no external side effect occurred. Jim identified the unsafe ambient-evidence fallback, reviewed the neutral unattributed-exception correction and the final ten-file diff, and approved it with no remaining actionable finding. Exact implementation revision `b1d0569` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Legacy-reminder provider-evidence successor `46bda60` closes the corresponding gap in the synchronous seller and buyer transfer-reminder wrapper. An unexpected sender rejection now replaces the ambient provider pin with neutral `CONSOLE` / `EXCEPTION_WITHOUT_PROVIDER_EVIDENCE` failure evidence. Every resolved sender result must retain the provider pinned before dispatch; a mismatched returned identity remains a deterministic non-success under the pinned durable provider, cannot become `SENT`, and cannot emit the administrator success notification. The sender catch is narrow, so persistence failure after a known accepted result propagates instead of being relabeled as a providerless exception. Deterministic regressions prove all three boundaries. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused reminder suite at 10/10, the full integration-enabled regression excluding the separately served live-boundary suite at 115 suites / 971 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP primary-health, private access-token session/state, unauthenticated state, scheduler authorization, admin-review origin, and verification-health smoke passed. Transfer-proof delivery intent, review-delivery intent, reminder-delivery, email-delivery, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after cleanup. Real provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; sender outcomes were mocked and no external side effect occurred. Jim identified the resolved-result identity gap, reviewed the corrected two-file diff including its narrowed catch, and approved it with no remaining actionable finding. Exact implementation revision `46bda60` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Dispute-email providerless-exception successor `80530a0` closes the same returned-evidence gap in the synchronous multi-party dispute wrapper. An unexpected sender rejection is now converted to neutral `CONSOLE` failure evidence and persisted through the caller's transaction client with the stable `EXCEPTION_WITHOUT_PROVIDER_EVIDENCE` classification in the durable error field; configured ambient provider state cannot be substituted for an identity the sender never returned. The sender catch remains narrow, so persistence failures still reach the existing settled-result diagnostic path without being relabeled. The deterministic regression proves the exact durable create/update evidence while preserving any pre-existing test environment value. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused dispute-email suite at 2/2, the full integration-enabled regression excluding the separately served live-boundary suite at 115 suites / 972 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP primary-health, staging-state refusal, scheduler-health, verification-health, and admin-review origin smoke passed. Transfer-proof delivery intent, review-delivery intent, reminder-delivery, email-delivery, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero before cleanup. Real provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; sender outcomes were mocked and no external side effect occurred. Jim identified the missing durable classification and environment-restoration gaps, verified both corrections, and approved the amended two-file implementation diff with no remaining actionable finding. Exact implementation revision `80530a0` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Administrator dispute-information providerless-exception successor converts an unexpected sender rejection inside the narrow delivery boundary to neutral `CONSOLE` / `EXCEPTION_WITHOUT_PROVIDER_EVIDENCE` failure evidence. The serializable request can retain its explicit failed-delivery projection, user notification, dispute history, and audit record with a warning, while configured ambient Resend state cannot be substituted for a provider identity that was never returned. Persistence failures after the sender boundary still propagate and cannot be relabeled as provider failures. The deterministic route regression preserves and restores any pre-existing credential environment value and proves the complete warning path. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused administrator/dispute suites at 7/7, the full integration-enabled regression excluding the separately served live-boundary suite at 115 suites / 973 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP primary-health, staging-session/state, scheduler-health, verification-health, and admin-review origin smoke passed. Transfer-proof delivery intent, review-delivery intent, reminder-delivery, email-delivery, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after fixture cleanup. Real provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; the sender failure was mocked and no external side effect occurred. Jim independently reviewed the exact two-file behavioral diff, reran the focused route suite at 5/5, and approved it with no remaining actionable correctness, scope, persistence-boundary, or adjacent evidence finding. Exact implementation revision `d3d4784` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Stripe transactional-email providerless-exception successor `6017b00` prevents unexpected buyer or seller sender rejection from escaping after the payment success transaction, deleting the Stripe event claim, and permitting an ambiguous webhook replay. Only the sender await is contained: missing provider evidence becomes a durable `FAILED` / `CONSOLE` projection with the stable `EXCEPTION_WITHOUT_PROVIDER_EVIDENCE` classification, while resolved provider results remain unchanged and every later persistence failure still reaches the existing 500 response and event-claim release path. Deterministic coverage proves both transactional branches, ambient Resend non-attribution, continued administrator and notification work, retained-claim duplicate replay without a second send, and the separate persistence-failure boundary. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused Stripe suite at 5/5, the full integration-enabled regression excluding the separately served live-boundary suite at 115 suites / 975 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP primary-health, staging-state refusal, scheduler-health, verification-health, and admin-review origin smoke passed. Transfer-proof delivery intent, review-delivery intent, reminder-delivery, email-delivery, notification, ordinary order, payment, payout, primary payment-attempt, primary payment-provider-event, primary refund-provider-attempt, and primary refund-provider-event counts remained zero after fixture cleanup. Real provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; sender outcomes were mocked and no external side effect occurred. Jim reviewed the exact two-file behavioral diff, independently reran the focused suite at 5/5, and approved it with no remaining actionable correction. Exact implementation revision `6017b00` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Catalog-request providerless-exception successor `a335c6d` contains an unexpected sender rejection at the narrow admin-email await inside the serialized ordinary request boundary. The pending catalog request now commits with the stable `EXCEPTION_WITHOUT_PROVIDER_EVIDENCE` error classification instead of rolling back and allowing an ambiguous resubmission, while ambient Resend configuration cannot be substituted for provider identity and later persistence failures still propagate. Deterministic coverage proves the successful request response, exact durable failure projection, and environment restoration. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused catalog-request suite at 5/5, the full integration-enabled regression excluding the separately served live-boundary suite at 115 suites / 976 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP primary-health, staging-state refusal, catalog-request CSRF, scheduler authorization, and verification-health smoke passed. Catalog requests, transfer-proof delivery intents, notifications, ordinary orders, payments, payouts, primary payment attempts, and primary refund-provider attempts remained zero after fixture cleanup. Real provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; the sender rejection was mocked and no external side effect occurred. Jim independently reviewed the exact two-file behavioral diff, reran the focused suite at 5/5, and approved it with no actionable correctness, scope, persistence-boundary, or adjacent provider-evidence finding. Exact implementation revision `a335c6d` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Administrator catalog-clarification providerless-exception successor `caf180e` contains only the buyer-email await before durable clarification-state persistence. An unexpected sender rejection becomes neutral `CONSOLE` / `EXCEPTION_WITHOUT_PROVIDER_EVIDENCE` failure evidence, the exact error is stored while the route returns `emailSent: false`, and configured ambient Resend state cannot be substituted for provider identity. The catalog-request update remains outside the catch, so a later persistence failure still returns 500 and cannot be relabeled as a delivery failure. Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused route suite at 2/2, the full integration-enabled regression excluding the separately served live-boundary suite at 116 suites / 978 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP primary-health, unauthenticated staging-state, catalog-request/admin-review CSRF, scheduler-health, and verification-health smoke passed. Catalog requests, transfer-proof delivery intents, review-delivery intents, reminder deliveries, email deliveries, notifications, ordinary orders, payments, payouts, primary payment attempts, and primary refund-provider attempts remained zero after fixture cleanup. Real provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; the sender rejection was mocked and no external side effect occurred. Jim independently reviewed the exact two-file behavioral diff, reran the focused suite at 2/2, and approved it with no actionable correction; the broader stale-admin and repeated-clarification race remains separate architectural debt. Exact implementation revision `caf180e` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Spotify catalog-email commit-boundary successor `84a59d2` removes the unmatched-artist administrator send from the Serializable import callback. Catalog-request and notification-preference writes must commit before any email attempt; a deterministic mocked post-callback transaction failure proves that an abort after the staged writes performs no send, while successful-path ordering proves the email follows the completed transaction. The post-commit send carries an 80-character idempotency key derived from the user and de-duplicated, sorted stable request IDs, and an unexpected sender rejection is contained as neutral `CONSOLE` / `EXCEPTION_WITHOUT_PROVIDER_EVIDENCE` rather than being falsely attributed from ambient configuration. This bounded correction is not a durable outbox: process death after commit but before send can lose the notification, SendGrid has no equivalent provider deduplication here, and staging-persona restoration between lock release and the post-commit send remains adjacent revalidation work.
- Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused Spotify suite at 8/8, the full integration-enabled regression excluding the separately served live-boundary suite at 116 suites / 980 tests, and the isolated local live-boundary suite at 7/7 after installing its synthetic seller fixture. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and authenticated isolated local primary-health and staging session/state smoke passed. Catalog requests, transfer-proof delivery intents, review-delivery intents, reminder deliveries, email deliveries, notifications, ordinary orders, payments, payouts, primary payment attempts, and primary refund-provider attempts remained zero before the disposable database was removed. Real provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; sender behavior was mocked and no external side effect occurred. Jim withheld the initial exception-only change, required the pre-commit I/O correction, and approved the amended three-file implementation with the residual limitations above explicitly retained. The clean staging revision was pushed, but its protected isolated-preview deployment was refused by Vercel's free daily deployment quota, so no historical preview is treated as evidence for this checkpoint.

- Spotify OAuth callback provider-boundary successor removes both authorization-code exchange and the provider `/me` read from the Serializable ordinary-user callback. Tx1 now commits only a primitive snapshot of exact connection absence or the current connection id, provider identity, and SHA-256 version of its encrypted credential/expiry/update evidence. Only then are the one-time code and bounded provider account envelope read. Tx2 re-locks the current User, refuses a restored managed persona, revalidates the exact connection version, and uses create-on-absence or an exact compare-and-update rather than a blind upsert; a concurrent disconnect, reconnect, refresh, or credential rotation therefore cannot be overwritten. Raw access and refresh tokens remain in memory only until they are encrypted for the fenced final projection. One canonical configuration gate now supplies both the token-exchange redirect URI and terminal account redirect: matching application origins, credential-free HTTPS, the exact callback path, no query/hash, and one recognized isolated-test, isolated-preview, or production identity are mandatory. Request Host, Origin, and forwarded headers cannot select a provider or account redirect. The OAuth state cookie is cleared on every callback terminal response, including unauthenticated refusal. The new callback transactions intentionally do not use the shared post-transaction persona-reclassification read, so current identity is sourced only from their locked transactions.
- At this callback-only checkpoint the adjacent artist-import provider boundary remained an explicit design gate. Its dormant `getSpotifyImportCandidates` / `getSpotifyAccessToken` path was subsequently removed after the durable refresh-command and bounded artist snapshot/read path became the exclusive live artist-import implementation. No remaining exported helper can directly rotate a `ConnectedAccount` credential or suppress one Spotify read branch outside that durable ownership and exact revalidation boundary.
- Spotify artist-import design gate: retaining automatic token refresh requires a durable refresh command; the existing `ConnectedAccount` row alone cannot safely serialize provider ownership or preserve a rotated refresh token when local finalization fails. The database must enforce exactly one command for the connection identity plus the digest of its exact source version (connection id, provider account id, encrypted access-token ciphertext, encrypted refresh-token ciphertext, expiry, and update clock), so concurrent staging cannot create parallel owners for one version. The proposed command owns that immutable source snapshot and digest, actor/provenance, one immutable attempt identity, provider-contact classification, bounded terminal evidence, and database timestamps. Trigger enforcement must reject mutation of command identity, provenance, source snapshot/digest, attempt ownership, or existing evidence; reject delete and truncate; and permit only `PENDING -> ATTEMPTING -> SUCCEEDED | RECONNECT_REQUIRED`, with the owning attempt required for either terminal transition.
- Claiming remains User-first and connection-second inside a Serializable transaction, is allowed only from `PENDING`, and commits before provider I/O. Provider contact may use only the claimed command's frozen connection/version inputs. An authenticated decryption or configuration failure known to occur before any request is classified as `NOT_CONTACTED`, never as ambiguous provider contact; it may terminalize the owning attempt as reconnect-required with that truthful bounded evidence. Timeout, malformed response, provider rejection after contact, rotated-token success that cannot be finalized, and other known post-contact outcomes receive distinct bounded contact/outcome classifications without storing secrets. Process death or any failure before a terminal write physically leaves the row `ATTEMPTING`; it is exposed operationally as no-retry and reconnect-or-manual-reconciliation, and is never described as though it transitioned to `RECONNECT_REQUIRED`. `ATTEMPTING` is never age-reclaimed or automatically resent, and every concurrent or replaying caller observes the durable owner/outcome and performs zero provider I/O.
- Only the owning attempt may finalize. Success must re-lock the command, current User, and exact `ConnectedAccount`, revalidate the complete source version, write the newly encrypted access/optional rotated refresh credential, atomically move a `ConnectedAccount.currentRefreshCommandId` (or equivalent current-generation owner) to this command, and mark the command `SUCCEEDED` in one transaction. Deferred commit-time constraint triggers on the success transition and current-link mutation must reject a newly succeeded command unless that transaction installs its exact result version as the current projection, and must reject a current link unless its command is succeeded, belongs to the same connection identity, and matches that projection's result-version digest. A later valid refresh may atomically supersede the movable link; historical succeeded commands remain immutable evidence but no longer constrain the evolving connection once they are not current. An authorized disconnect may clear the current link only while deleting the locked exact connection, without deleting its command history; reconnect creates a new connection identity/generation with no inherited owner, and old commands cannot bind to it. Command evidence contains no plaintext token, reversible token digest, provider response body, or secret-derived error text; only the current `ConnectedAccount` projection stores encrypted credentials. The PostgreSQL matrix must cover duplicate staging, forbidden field/evidence mutation, delete/truncate and illegal transitions, wrong-attempt finalization, both directions of a partial success/current-link projection commit, source-version drift, known pre-contact failure classification, provider-success finalization failure, stranded-`ATTEMPTING` replay with zero provider I/O, a second successful refresh that supersedes the current owner, and authorized disconnect/reconnect proving historical success neither blocks lifecycle evolution nor attaches to the new identity. A Prisma model, migration constraints/indexes/triggers, and these deterministic concurrency/adversarial tests are required before runtime implementation; schema and runtime behavior remain intentionally unchanged at this checkpoint.
- After that schema is explicitly approved, OAuth exchange/`/me`, token refresh, and artist reads remain three separate boundaries. Artist GET Tx1 returns only the locked ordinary-user identity and exact connection snapshot; any safely finalized refresh and all Spotify/catalog reads happen after commit and reduce to bounded primitives; Tx2 re-locks User first and returns those primitives only if persona and exact connection identity/version are unchanged. Artist POST additionally freezes the bounded client selection and candidate/match envelope before Tx2; Tx2 performs no provider/catalog read and atomically writes only that user's catalog preferences/requests after the same exact revalidation. Disconnect, reconnect, refresh, or persona drift during either read path fails closed; rollback or `P2034` commits zero imports/requests; unmatched-artist email remains post-commit and is built only from committed request ids/names. The alternative that avoids schema work is to remove automatic refresh entirely and return a stable reconnect-required result whenever the snapshotted credential is expired or near expiry.
- Fresh disposable PostgreSQL 17 implementation-tip verification applied all 106 migrations with current status and passed the focused Spotify callback suites at 3 suites / 31 tests, including the real same-row credential-version CAS regression; the non-live integration-enabled regression passed at 126 suites / 1121 tests, and the separately served isolated local live-boundary suite passed at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 587 existing warnings, the default production build, the migration-enabled isolated-preview-path build, source diff checks, and isolated local primary-health, unauthenticated staging-state, and state-clearing unauthenticated Spotify refusal smoke passed. Connected accounts, sellers, catalog requests, orders, payments, payouts, notifications, and seller-account commands were zero after cleanup. Stripe, email, Spotify, messaging, OCR, and phone credentials were absent; every provider outcome was mocked and no external side effect occurred. Jim required the canonical environment-bound URI gate and real PostgreSQL credential-version proof and approved the corrected callback implementation; he selected the durable refresh-command direction but withheld its schema/runtime implementation pending the explicit ownership, transition, projection, and adversarial-test requirements now recorded above. Exact implementation revision `43eb565` and private-response hygiene successor `aa9b039` were pushed to staging; the successor separately passed both focused suites at 29/29, TypeScript, focused lint, and source diff checks. Protected isolated-preview deployment `FfYGVuFX3ze52AJnLBpft4zhfnB4` built revision `43eb565`, applied no pending migrations, and returned private no-store primary health plus private unauthenticated staging-state refusal; its Spotify refusal smoke exposed an inherited public cache header despite correctly clearing OAuth state, which `aa9b039` corrects and covers. The exact-successor deployment retry was refused by Vercel's free daily quota, so that historical preview is not treated as exact-tip evidence and no further retry, paid upgrade, or production action was attempted.

- Ticket-listing email commit-boundary successor `2f41b37` removes both the seller manual-review email and `TICKETS_LISTED` administrator activity email from the Serializable listing callback. The callback now returns a prepared response and a primitive notification envelope derived from the committed listing snapshot; both provider-capable helpers run only after `runOrdinaryListingMutation` resolves. Stable per-listing idempotency keys cover both attempts, each post-commit notification is independently contained, and an unexpected rejection is classified as `EXCEPTION_WITHOUT_PROVIDER_EVIDENCE` without changing the committed route's `201` response or starving the other notification. A deterministic wrapper regression executes the local write and prepared response before raising `P2034`, proving neither notification is attempted when commit resolution fails; ordering coverage proves the local write and commit precede both attempts. This bounded correction is not a durable outbox: process death after commit but before either send can lose the notification, SendGrid does not honor the supplied idempotency keys, and staging-persona restoration between lock release and post-commit delivery remains adjacent revalidation work.
- Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused ticket-listing boundary suite at 5/5, the combined listing suites at 8/8, the full integration-enabled regression excluding the separately served live-boundary suite at 117 suites / 985 tests, and the isolated local live-boundary suite at 7/7 after installing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local primary-health, private staging session/state, seller-search, ticket-create authorization, scheduler-health, and verification-health smoke passed. Catalog requests, transfer-proof delivery intents, review-delivery intents, reminder deliveries, email deliveries, notifications, tickets, orders, payments, payouts, primary payment attempts, primary payment-provider events, primary refund-provider attempts, and primary refund-provider events remained zero before the disposable database was removed. Real email-provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; sender outcomes were mocked and no external side effect occurred. Jim specified the commit-resolution, independent-failure, idempotency, and authoritative-envelope constraints, reviewed the exact implementation diff, and approved it with no required correction; the subsequent hygiene change removed the last pre-lock display fallback and added the resolved-helper-failure regression he suggested. Exact implementation revision `2f41b37` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked staging project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Dispute-open email commit-boundary successor `ccbcc4b` removes the buyer, seller, and support sends from the ordinary buyer's Serializable dispute callback. The callback now returns its committed response and an authoritative primitive email envelope; delivery begins only after `runOrdinaryOrderOperation` resolves, and an unexpected post-commit helper rejection cannot turn the committed dispute into an HTTP failure. Each recipient receives a stable per-order, per-role idempotency key. Before provider I/O, the helper atomically reserves the existing unique `(orderId, emailType, recipient)` identity as `ATTEMPTING`; any existing `SENT`, `FAILED`, or abandoned `ATTEMPTING` evidence is terminal no-send/no-overwrite absent a stronger durable retry model. Final evidence is conditionally written only while that exact identity remains `ATTEMPTING`, with no upsert fallback, so a concurrent terminal result cannot be downgraded. Deterministic regressions prove local write and commit precede delivery, an injected post-callback `P2034` performs zero email work, committed HTTP 200 survives helper rejection, buyer/seller/support keys are distinct, pre-existing terminal evidence suppresses provider I/O, and ownership loss cannot overwrite terminal evidence. This bounded correction is not a durable outbox: process death after commit but before reservation can lose all notifications, death after provider acceptance can leave a terminal no-retry `ATTEMPTING` row requiring manual reconciliation, SendGrid does not honor the supplied provider key, and staging-persona restoration between lock release and post-commit delivery remains adjacent revalidation work.
- Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused dispute suites at 12/12, the full integration-enabled regression excluding the separately served live-boundary suite at 117 suites / 990 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local primary-health, private staging session/state, seller-search, ticket-create managed-persona refusal, scheduler-health, and verification-health smoke passed. Catalog requests, transfer-proof delivery intents, review-delivery intents, reminder deliveries, email deliveries, notifications, tickets, orders, payments, payouts, primary payment attempts, primary payment-provider events, primary refund-provider attempts, and primary refund-provider events remained zero before the disposable database was removed. Real email-provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; sender outcomes were mocked and no external side effect occurred. Jim withheld initial approval until stable delivery identities were reserved before provider I/O and terminal evidence was concurrency-fenced, independently reran the corrected focused suites at 12/12, and approved the exact correction for commit. Exact implementation revision `ccbcc4b` and its validation record were pushed to staging; the protected isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Dispute-evidence email commit-boundary successor `ad1ef1c` removes the buyer, seller, and support sends from the ordinary participant's Serializable evidence callback. The callback now returns its committed response and a primitive envelope built from the locked order plus the transaction-created submission UUID; delivery begins only after `runOrdinaryOrderOperation` resolves, and an unexpected post-commit helper rejection cannot turn the committed submission into an HTTP failure. Both the provider idempotency key and durable `EmailDelivery.emailType` derive from the exact order, committed submission UUID, and recipient role instead of dispatch-time `Date.now()`. Replaying the same prepared envelope therefore reaches the existing pre-provider unique reservation and terminal no-send/no-overwrite fence, while a later legitimate submission receives a distinct identity. Deterministic regressions prove local write and commit precede delivery, an injected post-callback `P2034` performs zero email work, committed HTTP 200 survives helper rejection, same-envelope replay cannot resend, and a new committed submission remains independently deliverable. This bounded correction is not request-level idempotency or a durable outbox: a client retry after a committed but lost response can create a new submission UUID, process death after commit but before reservation can lose notifications, death after provider acceptance can leave a terminal no-retry `ATTEMPTING` row requiring manual reconciliation, SendGrid does not honor the supplied provider key, and staging-persona restoration between lock release and post-commit delivery remains adjacent revalidation work.
- Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused evidence/email suites at 13/13, the full integration-enabled regression excluding the separately served live-boundary suite at 117 suites / 993 tests, and the isolated local live-boundary suite at 7/7 after installing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local primary-health, private staging session/state, seller-search, dispute-evidence managed-persona refusal, scheduler-health, and verification-health smoke passed. Catalog requests, transfer-proof delivery intents, review-delivery intents, reminder deliveries, email deliveries, notifications, tickets, orders, payments, payouts, primary payment attempts, primary payment-provider events, primary refund-provider attempts, and primary refund-provider events remained zero before the disposable database was removed. Real email-provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; sender outcomes were mocked and no external side effect occurred. Jim specified the submission-scoped durable identity and replay constraints, independently reran the focused pair at 13/13, and approved the exact four-file implementation diff with no required correction. Exact implementation revision `ad1ef1c` and validation record `f29da20` were pushed to staging; the protected isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Dispute-cancellation email commit-boundary successor `8b54250` removes the buyer, seller, and support sends from the ordinary buyer's Serializable cancellation callback. The callback now returns its committed response plus a primitive envelope derived from the locked order and dispute snapshot; delivery begins only after `runOrdinaryOrderOperation` resolves, and an unexpected helper rejection cannot turn the committed completion, payout eligibility, notification, audit, or launch attribution into an HTTP failure. Provider keys are stable per order and role (`dispute-cancelled:<orderId>:<role>`), while the existing unique per-order/role `DISPUTE_CANCELLED_*` delivery identity reaches the pre-provider `ATTEMPTING` reservation, terminal P2002 no-send fence, and status-fenced finalization. Deterministic regressions prove local write and commit precede delivery, an injected post-callback `P2034` performs zero email work, committed HTTP 200 survives helper rejection, and buyer, seller, and support identities are exact and distinct. This bounded correction is not a durable outbox: process death after commit but before reservation can lose notifications, death after provider acceptance can leave a terminal no-retry `ATTEMPTING` row requiring manual reconciliation, SendGrid does not honor the supplied provider key, and staging-persona restoration between lock release and post-commit delivery remains adjacent revalidation work.
- Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused cancellation/email suites at 14/14, the full integration-enabled regression excluding the separately served live-boundary suite at 117 suites / 996 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local primary-health, private staging session/state, seller-search, dispute-cancellation managed-persona refusal, scheduler-health, and verification-health smoke passed. Catalog requests, transfer-proof delivery intents, review-delivery intents, reminder deliveries, email deliveries, notifications, tickets, orders, payments, payouts, primary payment attempts, primary payment-provider events, primary refund-provider attempts, and primary refund-provider events remained zero before cleanup. Real email-provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; sender outcomes were mocked and no external side effect occurred. Jim selected this adjacent successor, reviewed the exact three-file implementation diff, independently reran the focused pair at 14/14, and approved broader validation and commit with no required correction. Exact implementation revision `8b54250` and validation record `f5294a9` were pushed to staging; the protected isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Administrator dispute-resolution email commit-boundary successor `2bf2eea` removes only the buyer, seller, and support sends from the privileged Serializable resolution callback. Audit and in-app notification writes remain transactional. Terminal payout release and refund decisions return primitive envelopes derived from the locked order and committed resolution values; delivery begins through global Prisma only after `runOrdinaryAdminOperation` resolves. Stable action-specific provider keys (`dispute-resolved:<orderId>:<role>` and `dispute-refunded:<orderId>:<role>`) retain the deterministic `DISPUTE_RESOLVED_*` and `DISPUTE_REFUNDED_*` reservation identities, P2002 terminal no-send fence, and status-fenced finalization. Four-hundred responses and `KEEP_UNDER_REVIEW` produce no envelope. Deterministic regressions prove commit-before-email ordering, callback-complete `P2034` causes zero email/evidence I/O, post-commit helper rejection preserves the committed 200, resolved and refunded role identities are exact, and same-envelope replay cannot resend or overwrite evidence. This email-only correction does not close the higher-severity Stripe transaction gap: `refunds.create` still runs inside the database transaction, so provider success followed by rollback or process failure can leave external money movement without committed local state. The stable Stripe idempotency key mitigates duplicate refunds but does not reconcile that crash window. The existing non-durable notification limitations also remain: process death before reservation can lose delivery, provider acceptance before finalization can leave terminal `ATTEMPTING` evidence requiring manual reconciliation, SendGrid does not honor the supplied provider key, and staging-persona restoration after lock release remains adjacent revalidation work.
- Fresh disposable PostgreSQL 17 verification applied all 102 migrations with current status and passed the focused resolution/email suites at 19/19, the full integration-enabled regression excluding the separately served live-boundary suite at 117 suites / 1002 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 593 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local primary-health, private staging session/state, seller-search, and dispute-resolution managed-persona refusal smoke passed; scheduler and verification health remained fail-closed stale/degraded without credentials or run evidence. Catalog requests, transfer-proof delivery intents, review-delivery intents, reminder deliveries, email deliveries, notifications, tickets, orders, payments, payouts, audit records, primary payment attempts, primary payment-provider events, primary refund-provider attempts, and primary refund-provider events remained zero after fixture cleanup. Real email-provider, Stripe, payment, webhook, messaging, OCR, and phone credentials were absent; sender and refund outcomes were mocked in tests and no external side effect occurred. Jim specified the narrow boundary and residual-risk constraints, independently reran the focused pair and TypeScript, and approved the exact three-file implementation diff with no required correction. Exact implementation revision `2bf2eea` was pushed to staging; its protected isolated-preview deployment was attempted from the correctly linked repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- Legacy dispute-refund commit-boundary successor `84db8f2` adds a dedicated one-per-order/payment `LegacyDisputeRefundIntent` rather than reusing the isolated primary-ticketing refund domain. The first Serializable transaction locks and revalidates the ordinary order and payment, then commits an immutable authorization snapshot and command digest without calling Stripe or mutating refund state. A one-winner `NOT_SENT` to `ATTEMPTING` claim is persisted before provider I/O; uncertain, reconciliation, succeeded, and failed evidence is never automatically resent. Exact provider refund identity, payment-intent binding, amount, currency, and terminal status are validated before a second current-admin transaction re-locks the intent, order, and payment and atomically finalizes payment, order, ticket, escrow, payout, access-token, audit, and intent state. Provider success followed by local rollback records reconciliation-required evidence with the exact provider refund ID/status while leaving all local refund mutations rolled back. Refund notifications and stable-role emails begin only after that second commit. The database trigger freezes authorization evidence, permits only the defined state transitions, and rejects deletion; application claim and finalization independently reconstruct the digest from stored fields. Exact replay uses the original actor, normalized reason, request context, authorization time, and provider command, while changed authorization or a shape-valid tampered digest fails before provider contact.
- Fresh disposable PostgreSQL 17 verification applied all 103 migrations with current status and passed the focused route/email/PostgreSQL suites at 34/34 and the full integration-enabled regression excluding the separately served live-boundary suite at 118 suites / 1017 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 592 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, and source diff checks passed. Coverage proves authorization rollback produces no provider claim, concurrent callers produce one dispatch winner, a crash after claim is durable and non-resendable, provider timeout requires reconciliation, provider success plus local rollback preserves provider evidence without local refund state, exact success commits every legacy row once, replay cannot resend, mismatched provider evidence cannot finalize, and authorization/digest/history trigger violations fail closed. Stripe, email, messaging, OCR, and phone credentials were absent; all provider outcomes were mocked and no external email, provider, payment, webhook, payout, or money side effect occurred. Jim reviewed the amended six-file diff, independently reran the focused route/email suites and TypeScript, and approved broader exact-tip validation and commit with no required correction.
- Exact implementation revision `84db8f2` and validation record `ae69082` were pushed to staging. The migration-enabled protected isolated preview deployed successfully as `dpl_BZrgcbcVZUTfNoUh1yNUSFBS6jp7`; remote smoke returned private no-store isolated-preview health, private no-store unauthenticated staging-state refusal, and fail-closed origin rejection for an unauthenticated dispute-refund mutation probe. No authenticated dispute action or provider call was attempted.

- Legacy payment-intent commit-boundary successor moves both Stripe retrieval and creation out of `runOrdinaryPurchase`'s Serializable callback. The first transaction locks and revalidates the current buyer wallet, order, every ordered ticket reservation, amount/currency, and any prior payment before committing one immutable `LegacyPaymentIntentCommand`. A one-winner `NOT_SENT` to `ATTEMPTING` claim owns provider I/O; timeouts, mismatched evidence, and provider success followed by local finalization failure become durable non-retryable reconciliation work. The second current-buyer transaction re-locks and revalidates the complete snapshot and alone upserts `Payment` plus exact provider identity/status/amount/currency evidence. The stable `truefantix-order-<orderId>` provider key remains defense in depth. Stripe client secrets are never persisted: immediate success returns the validated transient value, while a later durable success replay performs retrieve-only exact-evidence validation and can neither create another intent nor mutate terminal command history.
- Fresh disposable PostgreSQL 17 verification applied all 104 migrations with current status and passed the focused route/PostgreSQL suites at 20/20, the full integration-enabled regression excluding the separately served live-boundary suite at 119 suites / 1033 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 592 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local primary-health, private unauthenticated staging-state refusal, and fail-closed payment mutation smoke passed. The disposable database was empty after fixture cleanup. Stripe, email, messaging, OCR, and phone credentials were absent; every provider outcome was mocked and no external provider, payment, webhook, payout, email, or money side effect occurred. Jim withheld initial approval until the durable plaintext client secret was removed, then reviewed and approved the retrieve-only immutable replay correction with no remaining actionable finding.
- Exact implementation revision `b6ae2bb` was pushed to staging. Protected isolated-preview deployment `dpl_8qUEcXZnjE2CfzWpUTxaDLnyHvQP` completed successfully; authenticated-bypass remote smoke returned private no-store isolated-preview health, private no-store unauthenticated staging-state refusal, and fail-closed origin rejection for the unauthenticated payment-intent mutation probe. No authenticated checkout or Stripe/provider call was attempted.

### Legacy payout provider-boundary design checkpoint

- The current ordinary payout path is not safe to retry after its provider boundary. It reclaims a five-minute-old `PROCESSING` payout without knowing whether `stripe.transfers.create` crossed the network boundary, and a thrown provider call or a successful transfer followed by a failed local write is rewritten as ordinary retryable `FAILED`. The connected-account instant-payout call has the same ambiguity after the platform transfer has already succeeded. Stable Stripe idempotency keys reduce duplicate risk but are not durable local evidence and do not make these states equivalent.
- The bounded successor will give the platform transfer and connected-account instant payout separate one-per-payout command records and state machines. The command, never legacy `Payout.status`, `attemptCount`, or `lastAttemptAt`, is the sole authority to dispatch provider work. Each record freezes a canonical command digest plus the exact payout, order, payment, seller, connected-account, amount, currency, destination, and source identity needed by its own provider operation. The transfer digest also freezes the authorization provenance that controls audit and result behavior: the exact caller-authorized payout scope, `actorUserId` (nullable only where the existing caller contract permits), `automatic`, and the database authorization time. A later manual request cannot overwrite, complete, or lend its actor identity to an automatic command, and an automatic invocation cannot adopt a manual command; a provenance mismatch returns the existing non-success outward contract with zero provider I/O. A serializable authorization commits `NOT_SENT`; one compare-and-update winner claims `ATTEMPTING` before provider I/O. `ATTEMPTING` is never lease-reclaimed or automatically resent. Provider timeout, unknown outcome, or provider success followed by failed local finalization becomes terminal local `RECONCILIATION_REQUIRED`; authenticated evidence that no provider mutation was attempted may become terminal `FAILED`. `SUCCEEDED`, `FAILED`, and `RECONCILIATION_REQUIRED` are non-retryable without a separately reviewed reconciliation command.
- Transfer success is finalized in one fenced serializable transaction that re-locks and revalidates the immutable command, payout, order, payment, seller, and exact provider result before setting the existing payout `PAID`. The stored evidence includes provider transfer ID, amount, currency, destination, source transaction, funding mode, and settlement currency. If this finalization rolls back, a separate best-effort transaction records the exact provider success as reconciliation evidence while leaving the payout locally unpaid; failure of that evidence write leaves the durable command `ATTEMPTING`, which still cannot be reclaimed. Exact replay of recorded success returns the prior result and performs no provider create.
- Legacy `Payout` becomes a non-authoritative projection that old status-based code cannot reclaim. Command staging and claim must project a non-dispatchable payout state rather than leave `PENDING`, stale `PROCESSING`, or `FAILED`; terminal authenticated pre-provider failure and `RECONCILIATION_REQUIRED` must also remain non-dispatchable and operations-visible. Only a fenced, exact transfer-command success finalization may project `Payout.status = PAID`. No five-minute or other age-based path may create, reclaim, or resend provider work from the legacy row, and coverage must prove every command state has no remaining `Payout.status`-based dispatch path.
- Instant payout begins only after transfer success is durably recorded and is never represented by the transfer command or allowed to downgrade the paid platform transfer. Provider account discovery may conclude `STANDARD_ONLY` without creating an instant-payout command. Once an eligible destination is frozen and the instant command is claimed, ambiguity or local-finalization failure becomes its own `RECONCILIATION_REQUIRED`; a completed transfer may therefore coexist with a separately failed or reconciliation-required instant payout. Both callers derive audit, result, and incident context exclusively from the persisted command provenance. Existing callers retain their current HTTP/result shape: admin processing preserves its authorization and status mapping, but command reconciliation is surfaced as non-retryable operations attention rather than ordinary `STRIPE_FAILED`; buyer confirmation still returns the committed order-completion response and routes transfer or instant-payout reconciliation to operations without presenting it as a retryable provider failure.
- The migration must add database checks/triggers that freeze both command scopes, provenance, and digests, permit only the explicit forward transitions, reject delete/truncate, and prevent provider evidence from being overwritten. PostgreSQL coverage is required for concurrent one-winner claims; crash/timeout after either claim with zero resend; provider transfer success plus local-finalization rollback; exact success replay with zero create; refusal to reclaim stale `ATTEMPTING`; automatic-versus-manual provenance mismatch in both directions with zero provider I/O; every transfer-command state projected to a legacy payout state with no status-based reclaim path; instant-payout ambiguity preserving the completed transfer; digest, transition, update, delete, and truncate violations; and unchanged authorization/response contracts for both the admin payout and buyer-confirmation routes, including reconciliation mapped as non-retryable operations attention. Validation must use only mocked provider outcomes and a disposable isolated database; no Stripe credential, connected account, transfer, payout, webhook, or money movement is authorized.

- The implemented payout-command successor adds separate immutable transfer and instant-payout command rows. Read-only Stripe charge discovery occurs before authorization only for genuinely new work; the committed transfer command freezes and digests the exact provider mutation amount, currency, destination, source transaction, funding mode, settlement currency, payout/order/payment/seller scope, caller provenance, and idempotency key. A staged command dispatches only that stored scope. One compare-and-update claim owns each mutation, `ATTEMPTING` is never reclaimed, authenticated pre-provider refusal becomes terminal `FAILED`, every uncertain provider outcome becomes `RECONCILIATION_REQUIRED`, and complete shape-valid mismatched provider evidence is retained without being accepted as success. Exact fenced finalization is the only path to the legacy paid projections, with a deferred commit-time database constraint preventing either half from committing alone. Instant-payout ambiguity cannot downgrade or resend the completed platform transfer.
- Fresh disposable PostgreSQL 17 verification applied all 105 migrations with current status and passed the focused payout command/service suites at 20/20, the full integration-enabled regression excluding the separately served live-boundary suite at 121 suites / 1054 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 591 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP boundary smoke passed. Transfer-command, instant-command, payout, payment, and order counts were zero after fixture cleanup. Stripe, email, messaging, OCR, and phone credentials were absent; every provider result was mocked and no external provider, payment, webhook, payout, email, or money side effect occurred. Jim identified the missing frozen transfer mutation scope, exact returned-evidence fence, incomplete reconciliation-evidence shape, and partial projection commit gaps; he reviewed the corrected scope, triggers, regressions, and deferred projection constraint and approved the final diff with no remaining actionable finding.
- Exact implementation revision `df0feba` was pushed to staging. Its protected isolated-preview deployment was attempted from the correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

### Legacy seller-onboarding provider-boundary design checkpoint

- The current seller-onboarding start route is not safe under concurrency or an ambiguous provider outcome. Two callers can both observe no seller or no connected-account binding and create separate Stripe Express accounts; a successful `accounts.create` followed by a failed local seller update leaves an untracked provider account; and retry has no durable evidence that the provider boundary was already crossed. The same route silently mutates an existing connected account on every start request, then creates an onboarding link from the request URL rather than an explicitly configured application origin. Start and login-link creation also authorize from a stale guard snapshot instead of the current identity lock already used by onboarding status.
- The bounded successor will create or recover the local seller in Tx1 beneath serializable current `User` and `Seller` locks, revalidate verification, ban state, selling capability state, ordinary-persona/managed-persona exclusion, and the exact seller binding, and commit one immutable account-creation command before provider I/O. The command freezes the actor and authorization time plus the complete provider input: user and seller identities; normalized name, email, phone, address, and country; supported account type; requested capabilities; payout settings; business-profile URL and description; and provider metadata behind a canonical digest. Changed profile data cannot alter or replace an existing `NOT_SENT` or `ATTEMPTING` command; exact replay may use only the stored command. One atomic compare-and-update winner claims `NOT_SENT` as `ATTEMPTING` and alone owns `accounts.create`; the stable Stripe idempotency key is frozen with the command as defense in depth, not as dispatch authority. `ATTEMPTING` is never age-reclaimed or automatically resent. Authenticated evidence that no provider mutation was attempted may become terminal `FAILED`; timeout, unknown outcome, mismatched returned evidence, or provider success followed by local linkage finalization failure becomes `RECONCILIATION_REQUIRED` with no resend.
- Account success finalization will run in a second serializable transaction that re-locks the command, current user, and seller, reconstructs the digest, and validates the exact returned account identity, type, country, metadata, and requested capability scope before atomically binding `Seller.stripeAccountId` and marking the command `SUCCEEDED`. A pre-existing seller account that is not the command's exact finalized provider identity is a hard conflict, never an overwrite. Exact replay of durable success returns the existing binding with zero provider create. Database constraints/triggers must freeze command scope, digest, provider evidence, and idempotency key; permit only explicit forward transitions; reject delete/truncate; and prevent a command from succeeding without the matching seller projection or a seller projection from being installed by an unfinished command.
- Existing-account `stripe.accounts.update(...)` prefill will be removed from the interactive ordinary start request because Stripe-hosted onboarding owns those updates. Any later profile synchronization is a distinct, explicitly authorized provider command with its own immutable requested change and reconciliation state; onboarding start cannot silently rewrite provider account data or swallow an update failure. Account-link and login-link creation remain distinct from account creation: the callback beneath `runOrdinarySellerOnboardingOperation` locks and revalidates the current user plus exact seller/account, performs zero Stripe or other provider I/O, and returns only a primitive authorization snapshot containing `userId`, `sellerId`, `stripeAccountId`, intended link kind, and the canonical configured refresh/return URLs where applicable. The serializable transaction must commit and the wrapper must resolve successfully before the route calls `accountLinks.create` or `createLoginLink` outside the transaction, using exclusively that snapshot; the identity lock is never held across either provider call, and no local mutation follows the ephemeral link call. Start revalidates and targets the newly linked/frozen account or the already-current locked account; login revalidates and targets the current locked account. A persona restoration or account-binding change after the committed snapshot may make an issued link stale but cannot redirect it to another account. Provider link failures remain retryable because they create no account or linkage, and they never mutate seller linkage or imply account success. Marketplace, refresh, and return URLs all derive from one fail-closed configured canonical HTTPS application origin whose environment boundary matches the current deployment; `req.url`, `Host`, `Origin`, forwarded headers, and caller-selected targets are never provider inputs.
- Existing outward behavior remains bounded: a newly finalized or already linked ordinary seller receives a fresh onboarding URL, an ordinary linked seller may receive a dashboard login URL, incomplete verification and missing configuration retain explicit refusals, and command ambiguity is surfaced as non-retryable operations attention rather than an invitation to retry. No route may expose command internals, provider credentials, or an account belonging to another seller. The status route remains read-only at the provider boundary and continues to persist provider-derived capability state beneath the current identity lock.
- PostgreSQL and route coverage must prove concurrent account starts have one winner, one committed command, and at most one provider dispatch; rollback before claim performs zero provider work; crash/timeout after the account claim is durable and non-resendable; exact account success plus Tx2 rollback retains reconciliation evidence without installing a seller binding; exact success replay performs zero create; forged or mismatched provider evidence cannot finalize; linkage ownership loss and a pre-existing different seller binding cannot be overwritten; digest, transition, update, delete, truncate, and projection violations fail closed; a persona restored before authorization causes zero provider I/O; existing-account start performs no `accounts.update`; hostile `Host`, `Origin`, and forwarded-host inputs cannot influence either link type or the business-profile URL; concurrent start/login account-binding changes cannot target another account; and login/start link creation is restricted to the exact authorized seller account. Link-order coverage must prove both ephemeral provider calls observe the authorization commit first and are never reached after an injected Prisma `P2034`, callback rollback, failed snapshot construction, or account-binding revalidation failure; the provider mock must also prove it runs after the transaction wrapper resolves rather than from inside its callback. No-store responses, rate limits, verified-user requirements for start, managed-persona refusal, and existing outward error contracts remain intact. Validation must use only mocked provider outcomes and a disposable isolated database; no Stripe credential, connected account, account update, link, webhook, payment, payout, email, or money movement is authorized.

- The implemented seller-account command freezes and digests the complete provider account input and caller identity, gives one atomic claim ownership of `accounts.create`, never reclaims `ATTEMPTING`, and records uncertain or post-provider local failures as non-resendable reconciliation work. Exact provider evidence and the current ordinary user/seller binding are revalidated before one atomic seller projection and command success. Database triggers freeze command scope and history, enforce forward-only transitions, reject deletion/truncation, and require the command success and seller projection to commit together. Existing-account starts no longer call `accounts.update`; start and login-link routes obtain committed primitive authorization snapshots and only then create an ephemeral provider link from the exact authorized account and canonical configured HTTPS origin.
- Fresh disposable PostgreSQL 17 verification applied all 106 migrations with current status and passed the focused seller/origin/persona/provider suites at 5 suites / 59 tests, the full integration-enabled regression excluding the separately served live-boundary suite at 124 suites / 1079 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, full lint with 0 errors and 591 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local HTTP boundary smoke passed. Seller-account commands, orders, payments, payouts, delivery intents, and notifications were zero after fixture cleanup. Stripe, email, messaging, OCR, and phone credentials were absent; every provider result was mocked and no external provider, link, payment, webhook, payout, email, or money side effect occurred. Jim reviewed the provider-boundary implementation and required explicit `P2034` and callback-rollback link coverage; he approved the corrected route-level gate with no remaining actionable finding.
- Exact implementation revision `f7541bd` and test-only successors `ae77255` and `035c792` were pushed to staging. Protected isolated-preview deployment was attempted from the clean, synchronized, correctly linked staging repository root, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.

- The seller-status synchronization successor removes the remaining provider read from beneath the ordinary-user identity lock. One serializable transaction is now the sole verified ordinary user/seller/account authorization source and commits a primitive `NO_ACCOUNT` or exact linked-account snapshot before Stripe is initialized. Exact, nondeleted, bounded account evidence is validated before the external-account read; both provider results are reduced to bounded primitive readiness evidence. A second serializable User-then-Seller transaction revalidates the current verified ordinary persona and exact unchanged user, seller, and account binding before atomically persisting readiness and promotion-only approval/can-sell state. Provider failure, malformed or mismatched evidence, identity drift, binding drift, serialization loss, or transaction rollback cannot partially persist readiness or repeat provider work inside a transaction callback.
- Fresh disposable PostgreSQL 17 verification applied all 106 migrations with current status and passed the focused seller/origin/persona/provider/PostgreSQL suites at 5 suites / 71 tests, the full integration-enabled regression excluding the separately served live-boundary suite at 124 suites / 1092 tests, and the isolated local live-boundary suite at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 589 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local primary-health, unauthenticated seller-status, and fail-closed staging-state smoke passed. Seller-account commands, orders, payments, payouts, delivery intents, notifications, and sellers were zero after fixture cleanup. Stripe, email, messaging, OCR, and phone credentials were absent; every provider result was mocked and no external provider, link, payment, webhook, payout, email, or money side effect occurred. Jim reviewed the exact five-file behavioral diff, required the sole-Tx1 authorization source, bounded provider envelope, Tx2 revalidation, rollback, and outward-contract proofs, and approved the corrected implementation with no remaining actionable finding.
- Exact implementation revision `3fc9997` was pushed to staging. Its protected isolated-preview deployment was attempted from the clean, synchronized repository root explicitly bound to the existing staging-only Vercel project, but Vercel refused it at the free daily deployment quota, so no historical preview is treated as deployment evidence for this checkpoint.
- Seller-status identity-fence successor `18dd00a` removes the status path from the shared post-transaction managed-persona reclassification read, so Tx1 and Tx2 are now the only current-identity authorization sources. Tx1 commits `NO_ACCOUNT` or the exact primitive user/seller/account snapshot before Stripe initialization; exact nondeleted account evidence is validated before the external-account lookup; provider results are bounded to normalized primitive readiness facts; and Tx2 re-locks User then exact Seller, revalidates verified ordinary identity and the unchanged binding, and uses fenced atomic readiness/promotion writes. The test-only successor `c14e896` explicitly proves a missing current database identity preserves the private no-store 401 contract with zero provider reads or local mutation. A fresh exact-executable-tip rerun after `c14e896` used PostgreSQL 17 with all 106 migrations current and passed the focused five suites at 77/77, the non-live regression at 124 suites / 1098 tests, and the local built-app boundary at 7/7. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 589 existing warnings, the default production build, the migration-enabled isolated-preview-path build, source diff checks, and isolated local primary-health, unauthenticated seller-status, and fail-closed staging-state smoke passed. Seller-account commands, orders, payments, payouts, delivery intents, notifications, tickets, and sellers were zero after synthetic fixture cleanup; Stripe, email, messaging, OCR, and phone credentials were absent, every provider outcome was mocked, and no external side effect occurred. Exact revisions `18dd00a` and `c14e896` were pushed to staging. One protected isolated-preview deployment was attempted from the repository root bound to `truefantix-staging`, but Vercel refused it with `api-deployments-free-per-day` (retry after 24 hours); no deployment was created, no deployment retry was performed, and no historical preview is treated as exact-tip evidence.

- Spotify refresh-command schema successor adds an immutable, generation-bound command history and one movable current-owner projection without wiring any runtime route or provider access. The PostgreSQL boundary freezes the exact connected-account credential version and attempt owner, permits only `PENDING` to `ATTEMPTING` to one terminal state, and requires exact contact, HTTP, result, failure, token-rotation, expiry, and terminal-attempt evidence. Deferred bidirectional constraints make successful ownership and the current connected-account projection commit together, while later successful refreshes may supersede the projection without rewriting prior history. Disconnect/reconnect creates a distinct generation: old commands cannot bind to it and historical connection IDs cannot be reused.
- Fresh disposable PostgreSQL 17 verification applied all 107 migrations with current status and passed the focused command suite at 20/20, the full integration-enabled regression excluding the separately served live-boundary suite at 127 suites / 1143 tests, and the isolated local built-app boundary at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 587 existing warnings, source diff checks, the default production build, the migration-enabled isolated-preview-path build, primary-health smoke, healthy application smoke with a synthetic session secret, and private unauthenticated staging-state refusal passed. Sellers, connected accounts, and Spotify refresh commands were zero after cleanup. No Spotify, Stripe, email, webhook, messaging, OCR, or phone credential was supplied to the validated paths; every provider behavior was mocked, no external provider or other side effect occurred, and no deployment was attempted. Jim reviewed the exact schema-only diff and approved it for validation, commit, and push; runtime wiring, provider access, and deployment remain explicitly unauthorized.
- Spotify refresh-command evidence-matrix correction `ef80aef` closes the remaining `CONTACT_UNCERTAIN` shape gap by forbidding HTTP, result-expiry, and token-rotation evidence together, while retaining the exact failure-code/contact/status matrix, terminal-attempt ownership, and non-null success-expiry equality already enforced by the schema checkpoint. A direct PostgreSQL case proves uncertain contact cannot carry result evidence. Fresh disposable PostgreSQL 17 verification applied all 107 migrations with current status and passed the focused command matrix at 21/21, the full integration-enabled non-live regression at 127 suites / 1,144 tests, and the separately served local live boundary at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, full lint with 0 errors and 587 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, primary-health and application-health smoke, and private unauthenticated staging-state refusal passed. Spotify refresh commands, connected accounts, sellers, orders, payments, payouts, and notifications were zero after cleanup. Runtime routes and Spotify provider behavior were unchanged; no provider credential, provider contact, deployment, production/live data, or external side effect occurred.
- Spotify refresh service successor `ef99b3e` adds an application-only command orchestrator without referencing it from any route, UI, callback, disconnect, artist-import, or existing access-token path. Separate serializable stage and claim transactions lock User, exact ConnectedAccount, then command; revalidate ordinary verified access and the exact credential-version digest; and give one durable PENDING-to-ATTEMPTING owner to provider I/O. Only stage and claim receive bounded serialization/uniqueness retries. Decryption/configuration failures terminate as exact pre-contact evidence, while bounded token response parsing, explicit fetch/body timeouts, provider rejection, malformed success, and transport ambiguity map to the schema's truthful terminal matrix without persisting plaintext tokens, raw bodies, credentials, or provider-derived error text. Exact success encrypts the result before a second User-to-account-to-command transaction atomically installs the new projection, moves current ownership, and terminalizes the command; access restoration, source drift, wrong ownership, or local failure after provider success leaves the credential projection unchanged and records reconciliation-required evidence. Simultaneous first callers converge on one command and one provider dispatch; an ambiguously committed claim is re-read as ATTEMPTING with zero provider I/O; terminal and in-progress replay never resend.
- Fresh disposable PostgreSQL 17 validation applied all 107 migrations with current status and passed the combined refresh schema/service suites at 2 suites / 42 tests, the full integration-enabled non-live regression at 128 suites / 1,165 tests, and the separately served local live boundary at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused and full lint with 0 errors and 587 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, private no-store primary health, private unauthenticated staging-state refusal, and unauthenticated Spotify connection refusal passed. Spotify refresh commands, connected accounts, sellers, orders, payments, payouts, and notifications were zero after fixture cleanup. Jim reviewed the exact unreferenced two-file slice, required verified/banned access fences and genuine stage/claim conflict coverage, and approved the corrected implementation plus broad validation, commit, and push with no remaining actionable finding. All token HTTP behavior was mocked; Spotify, Stripe, email, webhook, messaging, OCR, and phone credentials were absent; no provider contact, deployment, production/live data, or external side effect occurred. Runtime wiring and deployment remain explicitly unauthorized.
- Spotify artist credential-acquisition successor `331dcfb` adds an application-only Tx1/reacquisition boundary without referencing it from any route, UI, callback, disconnect, or legacy artist path. A short Serializable snapshot locks and revalidates the exact verified, non-banned ordinary user and Spotify connection, returns only frozen bounded credential evidence after commit, invokes the durable refresh service only for an expired projection, ignores transient winner tokens, and reacquires and decrypts only the committed successful result projection. Durable in-progress, reconnect, missing-connection, source-drift, rollback, and replay outcomes perform no artist-provider work.
- Spotify artist snapshot successor `478b2df` adds an unreferenced read-only service with issued-credential provenance, fixed Spotify endpoints and query shapes, one deadline spanning both fetch and streamed-body consumption, per-response and aggregate byte budgets, bounded pages/items/strings, strict payload and pagination validation, all-branch failure, immutable token-free artist/match primitives, and a final User-then-connection Serializable fence over ordinary identity, exact provider account, current refresh owner, credential digest, expiry, and remaining validity. The service cancels stalled readers, refuses forged ready envelopes and administrator/managed personas before provider I/O, and returns no candidates after connection or credential drift. Jim withheld approval until the body stream was covered by the explicit deadline, then reviewed the correction and deterministic never-resolving fetch/body cases and approved broader staging validation with no remaining actionable finding.
- Fresh exact-tip disposable PostgreSQL 17 validation applied all 107 migrations with current status and passed the focused credential/read suites at 2 suites / 23 tests and the full integration-enabled non-live regression at 130 suites / 1,188 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 587 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, healthy local application/database smoke, and private unauthenticated Spotify connection and artist-route refusal passed. Connected accounts, Spotify refresh commands, sellers, orders, payments, payouts, notifications, and catalog requests were zero after fixture cleanup. Spotify, Stripe, email, webhook, messaging, OCR, and phone credentials were absent; every provider response was mocked, no route/runtime behavior changed, and no external provider call, deployment, production/live data access, or side effect occurred.
- Spotify artist route-wiring successor replaces the legacy provider and refresh work from the ordinary-user transaction with the committed credential/read boundary and one exact Serializable User-then-connection Tx2 for writes. GET returns only a fully fenced snapshot. POST accepts only a 16 KiB strict object with at most 350 bounded, deduplicated Spotify IDs and a boolean unmatched policy; every selected ID must exist in the authorized snapshot before Tx2. Tx2 revalidates the exact catalog identity of every match and atomically upserts only the locked user's preferences and requests. Drift, serialization loss, stale selections, and catalog replacement cannot partially write or trigger email. Public match primitives retain the existing `type`, `value`, and `label` UI contract, while unmatched email is built only after commit from bounded committed request IDs and names.
- Jim's review initially withheld approval for partial success on unknown selected IDs and the missing `match.label` response field. Mixed known/unknown and wholly unknown selections now fail with zero writes, and the response contract is covered directly; Jim approved the corrected diff with no remaining actionable finding. Fresh disposable PostgreSQL 17 validation applied all 107 migrations with current status and passed the focused route/credential/read suites at 3 suites / 47 tests and the full integration-enabled non-live regression at 130 suites / 1,196 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 587 existing warnings, default and migration-enabled isolated-preview production builds, source diff checks, and isolated local primary-health plus private unauthenticated artist-route smoke passed. All Spotify and email behavior was mocked or refused before use; no real provider credential, provider call, email, payment, money movement, live data, production database, or production environment was used. Exact implementation revision `c2ed449` was pushed to staging. Its protected isolated-preview deployment was attempted from the clean repository root bound to `truefantix-staging`, but Vercel refused it with `api-deployments-free-per-day`; no deployment was created, no retry was performed, and no historical preview is treated as exact-tip evidence.
- Spotify snapshot/delivery forward correction closes the GET-to-POST authorization and unmatched-request replay gaps left in `c2ed449`. GET now returns a bounded five-minute AES-256-GCM opaque snapshot token bound to the exact user, connection generation, provider account, refresh owner, credential/result digest and expiry, normalized artist identity, and catalog evidence; the UI must return that token, and POST decrypts it before a provider-independent Tx2 that revalidates the exact connection and catalog evidence. Missing, oversized, expired, or tampered tokens and duplicate normalized Spotify ids fail closed. Replayed unmatched imports never reopen reviewed terminal requests. Migration 108 adds an atomic durable batch-delivery reservation with unique per-request membership, database-enforced ARTIST ownership/value identity, immutable envelopes and memberships, guarded delete/truncate history, exact lifecycle/evidence shapes, terminal irreversibility, and pre/post-dispatch reconciliation that preserves whether provider contact may have begun. The post-commit drainer claims with `FOR UPDATE SKIP LOCKED`, pins one provider, records dispatch before sending, never replays delivered or ambiguous work, and leaves provider rejection terminal.
- Jim withheld approval until delivery history and linked CatalogRequest identity were enforced in PostgreSQL, then approved the amended snapshot, UI, schema, lifecycle, worker, and adversarial regression diff with no remaining actionable finding. Fresh disposable PostgreSQL 17 validation applied all 108 migrations with current status and passed the focused route/read suites at 2 suites / 44 tests and the full integration-enabled non-live regression at 130 suites / 1,204 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 587 existing warnings, default and migration-enabled isolated-preview production builds, source diff checks, clean post-test database counts, and isolated local primary-health plus private unauthenticated Spotify artist-route smoke passed. Spotify and email outcomes were mocked or refused before use; provider, Stripe, webhook, messaging, OCR, and phone credentials were absent, and no external provider call, email, payment, money movement, live data, production database, or production environment was used. Exact implementation revision `3cee428` was pushed to staging. Its one authorized protected isolated-preview deployment was attempted from the clean repository root bound to `truefantix-staging`, but Vercel refused it with `api-deployments-free-per-day`; no deployment was created, no retry was performed, and no historical preview is treated as exact-tip evidence.
- Spotify catalog-delivery outcome correction makes only an explicit HTTP 4xx response terminal `FAILED`; HTTP 5xx, `NETWORK_ERROR`, `PROVIDER_ERROR`, missing or unknown result evidence, sender exceptions, and provider-identity mismatch remain non-replayable `RECONCILIATION_REQUIRED` work. Persisted provider-result evidence is bounded, while raw provider and exception text is replaced with canonical operational evidence. Fresh disposable PostgreSQL 17 validation applied all 108 migrations with current status and passed the focused artist-read/delivery suite at 27/27 and the full integration-enabled non-live regression at 130 suites / 1,208 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 587 existing warnings, default and migration-enabled isolated-preview production builds, source diff checks, clean post-test database counts, and isolated local private primary-health, private unauthenticated staging-state refusal, and private unauthenticated Spotify artist-route refusal passed. Jim required 5xx quarantine plus direct `NETWORK_ERROR`, `PROVIDER_ERROR`, and HTTP 500 regressions, then approved the corrected exact diff with no remaining actionable finding. All sender outcomes were mocked; Spotify, email, Stripe, webhook, messaging, OCR, and phone credentials were absent, and no external provider call, email, deployment, production/live data, payment, or money movement occurred.
- Spotify catalog-delivery scheduler recovery closes the post-commit process-loss gap: the existing authenticated transfer-reminder scheduler now independently selects at most 100 database-clock-due `PENDING` intents or expired `PROCESSING` claims in deterministic order and delegates them to the existing claim, lease, provider-pinning, idempotency, and terminal-history fences. Unavailable providers leave pristine pending work recoverable; concurrent request and scheduler drains dispatch once; expired claims quarantine before new provider work; and delivered, failed, or reconciliation-required rows never replay. Jim reviewed and approved the exact four-file implementation diff with no actionable correctness, race, authentication, or lifecycle finding. Fresh disposable PostgreSQL 17 validation applied all 108 migrations with current status, passed the focused suites at 2 suites / 34 tests and the full integration-enabled non-live regression at 130 suites / 1,211 tests, and removed the disposable databases afterward. Prisma validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 587 existing warnings, default and migration-enabled isolated-preview production builds, source diff checks, and isolated local private primary-health plus unauthenticated staging-state, scheduler, and Spotify-route refusal smoke passed; the smoke database retained zero Spotify delivery intents. Provider, Spotify, email, Stripe, webhook, messaging, OCR, and phone credentials were absent, all sender outcomes were mocked or refused before provider work, and no external provider call, email, deployment, production/live data, payment, or money movement occurred.
- Spotify catalog-review race hardening adds migration 109 and makes the linked request's current `PENDING` state part of dispatch authorization. The dispatch transaction locks requests in deterministic id order, revalidates their exact owner/type/value/status, and only then records attempt 1. A matching request-to-intent database fence prevents review from crossing an active provider dispatch: review-first work causes attempt-0 quarantine with zero provider call, while dispatch-first work rejects the review transition until delivery finalizes. The migration preflight rejects already-contradictory active-dispatch history without blocking safe pre-dispatch quarantine. Jim reviewed the exact runtime, trigger, migration, and two-session regression diff and approved it with no actionable correctness, deadlock-order, lifecycle, or coverage finding. Fresh disposable PostgreSQL 17 validation applied all 109 migrations with current status and passed the focused artist-read/delivery suite at 31/31 plus the full integration-enabled non-live regression at 130 suites / 1,213 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 587 existing warnings, default and migration-enabled isolated-preview production builds, source diff checks, and isolated local private primary-health plus unauthenticated staging-state, scheduler, and Spotify-route refusal smoke passed; relevant delivery, catalog, connection, notification, order, payment, and payout tables were empty after cleanup. All sender outcomes were mocked or refused before provider work; provider, Spotify, email, Stripe, webhook, messaging, OCR, and phone credentials were absent, and no external provider call, email, deployment, production/live data, payment, or money movement occurred.
- Spotify OAuth reconnect generation compatibility now handles a callback arriving after a successful refresh projection. An exact unchanged refresh-owned `ConnectedAccount` is CAS-deleted and recreated inside the callback's second Serializable, user-locked transaction rather than attempting a trigger-rejected in-place credential rewrite. The new generation is ownerless and receives only the callback evidence; the historical refresh command retains its original connection identity. CAS loss, provider-account uniqueness failure, replacement insertion failure, serialization loss, or deferred-constraint failure rolls the deletion back atomically. Unowned connections retain their existing exact compare-and-update behavior. Jim reviewed the exact three-file behavioral diff and approved it with no actionable concurrency, history-retention, uniqueness, trigger, deferred-constraint, or one-time-exchange finding.
- Fresh disposable PostgreSQL 17 validation applied all 109 migrations with current status and passed the focused callback/race suites at 2 suites / 38 tests, the callback/refresh PostgreSQL suites at 2 suites / 24 tests, and the full integration-enabled non-live regression at 130 suites / 1,215 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 587 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, and isolated local private primary-health plus unauthenticated staging-state and Spotify-route refusal smoke passed. Connected accounts, refresh commands, catalog requests and deliveries, orders, payments, and payouts were zero after cleanup. Spotify, email, Stripe, webhook, messaging, OCR, and phone credentials were absent; no provider call, email, deployment, production/live data, payment, or money movement occurred.
- Spotify snapshot-expiry authorization now carries the authenticated five-minute token deadline into the final Serializable fence and samples its clock only after the User and exact ConnectedAccount are locked and the current credential digest is read. A POST that was valid when parsed but waited past snapshot expiry or credential leeway can no longer write preferences, catalog requests, or delivery work using the stale pre-lock clock. GET also samples a fresh post-fence time before sealing its snapshot. Jim independently reviewed the exact two-file behavior and approved it with no actionable correctness, boundary-comparison, clock-ordering, or coverage finding; validity remains deliberately an admission-time authorization at the final locked fence rather than a promise through transaction commit.
- Fresh disposable PostgreSQL 17 validation applied all 109 migrations with current status and passed the focused Spotify artist read/delivery suite at 32/32 and the full integration-enabled non-live regression at 130 suites / 1,216 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 587 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, isolated local private/no-store unauthenticated primary-health and staging-state refusals, and private/no-store unauthenticated Spotify-route refusal passed. Connected accounts, refresh commands, catalog requests and deliveries, orders, payments, and payouts were zero after cleanup. Spotify, email, Stripe, webhook, messaging, OCR, and phone credentials were absent; no provider call, email, production/live data, payment, or money movement occurred. Exact implementation revision `6a04cab` was pushed to staging. Its protected isolated-preview deployment was attempted once from the correctly linked `truefantix-staging` repository root, but Vercel refused it with `api-deployments-free-per-day`; no deployment was created, no retry was performed, and no historical preview is treated as exact-tip evidence.
- Spotify credential-age hardening moves both the initial unexpired-token clock and the post-refresh reacquisition clock inside their Serializable User-to-connection lock boundaries, after the exact credential or command digest is read. The sixty-second validity boundary now fails closed on equality during acquisition and the final artist fence, so a lock wait cannot authorize artist-provider work with a token that has crossed its leeway or admit a zero-lifetime snapshot. Disposable-PostgreSQL regressions prove clock placement after the three initial and four refreshed locking/digest queries, exact-equality refusal for ordinary and refreshed acquisition with zero artist/catalog reads, and exact-equality `DRIFTED` refusal at the final fence. Jim reviewed the exact four-file correction and approved it with no remaining actionable correctness or coverage finding.
- Fresh disposable PostgreSQL 17 validation applied all 109 migrations with current status and passed the focused credential/read suites at 2 suites / 47 tests and the full integration-enabled non-live regression at 130 suites / 1,221 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 587 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, isolated local private/no-store primary health, private unauthenticated staging-state refusal, and private unauthenticated Spotify-route refusal passed. Connected accounts, refresh commands, catalog requests and deliveries, orders, payments, and payouts were zero after cleanup. The only token-refresh response was mocked; Spotify, email, Stripe, webhook, messaging, OCR, and phone credentials were absent, and no external provider call, email, deployment, production/live data, payment, or money movement occurred.
- Legacy Spotify artist-path retirement removes the dormant exported `getSpotifyAccessToken` and `getSpotifyImportCandidates` surface together with its direct credential rewrite, partial provider-read suppression, unbounded legacy artist parsing, and provider-catalog fallback. The live artist route already uses only the durable refresh-command acquisition and bounded snapshot/read/import boundary, while OAuth callback exchange and its bounded `/me` evidence read remain intact. Repository-wide references prove there is no remaining consumer of the retired exports or helpers. Jim reviewed the deletion, confirmed the remaining callback/start/connection imports, and required the historical implementation note to be corrected before final approval.
- Fresh disposable PostgreSQL 17 validation applied all 109 migrations with current status and passed the focused Spotify callback/operation/refresh/credential/read suites at 7 suites / 130 tests and the full integration-enabled non-live regression at 130 suites / 1,221 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 582 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, isolated local private/no-store primary health, private unauthenticated staging-state refusal, and private unauthenticated Spotify-route refusal passed. Connected accounts, refresh commands, catalog requests and deliveries, orders, payments, and payouts were zero after cleanup. Spotify, email, Stripe, webhook, messaging, OCR, and phone credentials were absent; no external provider call, email, deployment, production/live data, payment, or money movement occurred.
- Spotify OAuth callback provider-read containment replaces the remaining unbounded token and `/me` JSON reads with one fixed-origin streaming boundary. Successful bodies are capped at 64 KiB and must complete inside one ten-second fetch-and-body deadline; timeout, oversize, HTTP rejection, read failure, invalid UTF-8, and parse failure abort the request, cancel any active stream exactly once, and surface only canonical local evidence. Client credentials and the one-time authorization code are bounded before provider contact, and provider response text is never copied into an exception or log. Jim identified the missing universal early-failure stream cancellation, verified the idempotent correction and still-open malformed UTF-8 regression, and approved the exact two-file change with no remaining blocker.
- Fresh disposable PostgreSQL 17 validation applied all 109 migrations with current status and passed the focused OAuth callback boundary at 22/22 and the full integration-enabled non-live regression at 130 suites / 1,226 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 582 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, isolated local private/no-store primary health, private unauthenticated staging-state refusal, and private/no-store unauthenticated Spotify artist and callback refusals passed. Connected accounts, refresh commands, catalog requests and deliveries, orders, payments, and payouts remained zero. Spotify, email, Stripe, webhook, messaging, OCR, and phone credentials were absent; every provider response was synthetic and no external provider call, email, deployment, production/live data, payment, or money movement occurred. Jim approved commit and push to staging but did not authorize a deployment, so no deployment was attempted.
- The immediate callback-read media-type successors reject a nominally successful token or `/me` response unless its case-insensitive media type is exactly `application/json`, with parameters permitted; wrong or missing media types cancel the unread stream and return the same canonical local failure. The focused synthetic provider boundary passed 24/24, the callback response/cookie/no-store contract suite passed 21/21, and TypeScript, targeted lint, and source diff checks passed. The PostgreSQL callback suite was discovered but intentionally skipped without a configured disposable database. No real credentials or provider call were used, and refresh-command and artist-read paths were unchanged.
- Spotify callback authorization hardening now validates the OAuth state cookie before honoring either a success or provider-denial response. A denial with missing or mismatched state clears the cookie and returns the private `invalid_state` redirect without exchanging a code. Successful token evidence must be a non-array object containing bounded access and refresh credentials, Bearer token type, a positive integral expiry, and both requested artist scopes before the callback may contact `/me`; accepted token type casing is normalized to `Bearer`. Malformed, non-refreshable, expired-at-issuance, or insufficient-scope responses therefore cause zero account-provider reads and no persistence. Jim reviewed and approved the exact route, evidence, and regression diff with no actionable correctness, compatibility, or coverage finding.
- Fresh disposable PostgreSQL 17 validation applied all 109 migrations with current status and passed the focused callback/operation suites at 2 suites / 54 tests and the full integration-enabled non-live regression at 130 suites / 1,237 tests. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 582 existing warnings, default and migration-enabled isolated-preview production builds, source diff checks, local application/database health, private/no-store primary health, private unauthenticated staging-state refusal, and private/no-store unauthenticated Spotify callback refusal passed. Connected accounts, refresh commands, catalog requests and deliveries, orders, payments, and payouts remained zero after cleanup. All provider responses were synthetic; Spotify, email, Stripe, webhook, messaging, OCR, and phone credentials were absent, and no external provider call, email, deployment, production/live data, payment, or money movement occurred.

- Spotify authorization-start hardening revalidates the authenticated user under the existing Serializable ordinary-user lock before it generates OAuth state or constructs an external authorization redirect. A restored managed staging persona is refused privately before configuration or redirect work; authentication refusal, managed refusal, missing configuration, and unexpected failure all clear stale OAuth state and return `private, no-store`. The not-configured redirect now derives from the canonical environment-bound application origin instead of request headers. Callback token evidence must include an explicit bounded scope containing both requested artist scopes before the account-provider read; missing or partial scope causes zero `/me` I/O and no persistence. Jim reviewed and approved the exact four-file implementation diff with no actionable correctness, compatibility, race, or coverage finding.
- Fresh disposable PostgreSQL 17 validation applied all 109 migrations with current status and passed the focused callback/operation suites at 2 suites / 58 tests, the full integration-enabled non-live regression at 130 suites / 1,241 tests, and the separately served isolated local live boundary at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 582 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, local application/database health, private/no-store primary health, private unauthenticated staging-state refusal, private state-clearing unauthenticated Spotify start/callback refusals, and authenticated managed-persona Spotify start/connection refusals passed. Connected accounts, refresh commands, catalog-delivery intents, orders, payments, payouts, primary payment attempts, and primary refund-provider attempts remained zero before disposal. Spotify, email, Stripe, webhook, messaging, OCR, and phone credentials were absent; provider behavior was mocked, no provider call or external side effect occurred, and no deployment was attempted because Jim did not authorize one.
- Outreach campaign send hardening replaces the pre-read/unconditional `SENDING` transition with one committed exact-recipient claim. A normalized-email transaction advisory lock is now shared by send claims, public unsubscribe, confirmed resubscribe, administrator suppression, and tracked Resend delivery events before their eligibility mutations. Claims then lock campaign, recipient, and contact in one order, revalidate the exact current PENDING recipient plus suppression, unsubscribe, and recent-contact state, and return only a frozen primitive provider envelope after commit. Campaign completion remains `SENDING` while any PENDING or SENDING recipient exists. Delivery-event handling re-locks and re-reads the exact provider-message recipient after the advisory lock before status-priority comparison, so stale delayed evidence cannot overwrite a concurrently committed higher-priority delivery state. Draft campaign replacement now locks the campaign first and revalidates DRAFT/all-PENDING state before replacing recipients. Jim identified and reviewed both the cross-path deadlock edge and stale webhook status edge, then approved the corrected shared-lock and revalidation boundary with no remaining actionable finding.
- Fresh disposable PostgreSQL 17 validation applied all 109 migrations with current status and passed the focused outreach suites at 4 suites / 28 tests, including 8 real PostgreSQL claim, unsubscribe, settlement, and out-of-order delivery cases; the full integration-enabled non-live regression passed at 131 suites / 1,249 tests and the separately served isolated local live boundary passed at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, focused lint, full lint with 0 errors and 582 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, healthy local application/database smoke, private unauthenticated outreach-administration refusal, and invalid-unsubscribe refusal passed. Outreach campaigns, contacts, recipients, suppressions, and email events plus sellers, connected accounts, refresh commands, orders, payments, and payouts were zero after fixture cleanup. Email, Stripe, Spotify, webhook, messaging, OCR, and phone credentials were absent; delivery behavior was mocked, no external email/provider/payment side effect occurred, and no deployment was attempted.
- Outreach provider-outcome hardening gives every committed recipient claim an immutable unique attempt UUID used as both the Resend idempotency key and a signed-webhook provenance tag. Only explicit HTTP 4xx evidence becomes ordinary `FAILED`; transport failure, HTTP 5xx, and malformed accepted responses become non-resendable `RECONCILIATION_REQUIRED`. Provider acceptance finalizes outside provider I/O, and accepted-but-unpersisted evidence is preserved by a separate best-effort reconciliation transaction. A same-attempt/same-provider webhook may bind the provider identity and delivery state before the send response returns; both success and exception finalizers treat that state as idempotent success rather than false failure or reconciliation. A committed delivery-event replay retains its campaign identity so a failed post-commit settlement can be retried without reapplying delivery side effects. Unknown shared-account webhook traffic is not retained. Identity-mismatched outreach evidence is terminally quarantined with thirty-day detailed retention and a permanent minimal Svix-ID tombstone, so even an expired corrected replay can never bind. Jim required 5xx ambiguity, shared-account scoping, early-webhook idempotence, permanent quarantine replay fencing, and reviewed the final convergence correction with no remaining actionable finding.
- Fresh disposable PostgreSQL 17 validation applied all 110 migrations with current status and passed the focused outreach suites at 4 suites / 34 tests, including real PostgreSQL acceptance-finalization failure, HTTP-500 no-redispatch, tagged early-webhook race, unrelated-event refusal, out-of-order replay, quarantine-expiry/tombstone, claim, unsubscribe, and settlement cases. The full integration-enabled non-live regression passed at 131 suites / 1,255 tests and the separately served isolated local live boundary passed at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, full lint with 0 errors and 582 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, healthy local application/database smoke, private unauthenticated staging-state refusal, unauthenticated outreach-administration refusal, invalid-unsubscribe refusal, and disabled-webhook refusal passed. Outreach campaigns, contacts, recipients, delivery events, quarantines, tombstones, sellers, orders, payments, and payouts were zero after cleanup. Email, Stripe, Spotify, webhook, messaging, OCR, and phone credentials were absent and all provider behavior was mocked, so no external email/provider/payment side effect occurred. Exact implementation revision `f82d631` was pushed to staging and protected isolated-preview deployment `dpl_H639A2KS1eVEQMApzvnASXojzZnZ` completed Ready; authenticated-bypass remote smoke returned private no-store primary health and private no-store unauthenticated staging-state refusal, while the outreach administration route refused unauthenticated access. No authenticated outreach action or provider call was attempted.
- Outbound outreach-provider containment successor `37553c0` gives the Resend call one fixed deadline spanning fetch and successful-response body consumption, refuses redirects, and bounds accepted bodies to 64 KiB of strict UTF-8 with exact JSON media type, object shape, and message identity. Received HTTP 4xx evidence is classified before body parsing as canonical terminal rejection; every other non-2xx, transport failure, timeout, invalid accepted response, or malformed accepted identity is canonical uncertain evidence. Provider response text never reaches thrown or durable evidence. Abort and stream cancellation are idempotent and non-blocking, so even a never-settling provider cancellation cannot delay or change the already authenticated HTTP outcome. Jim required status-first classification and non-blocking cleanup, reviewed the corrected exact three-file diff, and approved it with no remaining actionable finding.
- Fresh disposable PostgreSQL 17 validation applied all 110 migrations with current status and passed all six outreach suites at 58/58 tests, including PostgreSQL route-level rejection and uncertain-outcome no-replay proofs. The full integration-enabled non-live regression passed at 131 suites / 1,269 tests, and the separately served isolated local live boundary passed at 7/7 after installing and removing its synthetic seller fixture. Prisma format/validation/generation/status, TypeScript, full lint with 0 errors and 582 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, private no-store local primary health, unauthenticated outreach-administration refusal, invalid-unsubscribe refusal, and disabled-webhook refusal passed. Checked outreach, seller, order, payment, and payout tables were empty after cleanup. Email, Stripe, Spotify, webhook, messaging, OCR, and phone credentials were absent; provider behavior was mocked, no external provider/email/payment side effect occurred, and no deployment was attempted.
- Public outreach-webhook ingress containment successor `d8bd3f3` removes the unbounded pre-verification `Request.text()` boundary. Configured requests must now present bounded non-empty Svix headers before the route streams at most 256 KiB under one five-second deadline, strictly decodes the exact UTF-8 payload, and invokes signature verification. Timeout, declared or streamed oversize, read/decode failure, invalid signature, and malformed signed tracked evidence all return one private no-store refusal with zero delivery-service call; disabled configuration returns before touching body state. After verification, tracked evidence is reduced to bounded provider-message, normalized-recipient, finite-timestamp, UUID-attempt, status, and event-detail primitives before persistence. Cancellation is idempotent and non-blocking, so provider-controlled cleanup cannot replace or delay the authenticated outcome. Jim reviewed the exact two-file diff and approved it with no remaining actionable ingress, signature-fidelity, cancellation, cache, or envelope-validation finding.
- Fresh disposable PostgreSQL 17 validation applied all 110 migrations with current status and passed the focused webhook boundary at 21/21 plus the full integration-enabled non-live regression at 131 suites / 1,286 tests. The separately served isolated local live boundary passed at 7/7 after its synthetic seller fixture was installed and removed. Prisma format/validation/generation/status, TypeScript, targeted lint, full lint with 0 errors and 582 existing warnings, default and migration-enabled isolated-preview production builds, and source diff checks passed. Local smoke returned private no-store primary health, private unauthenticated staging-state refusal, unauthenticated outreach-administration refusal, invalid-unsubscribe refusal, and private no-store disabled-webhook refusal. Sellers, outreach campaigns, contacts, recipients, delivery events, orders, payments, and payouts were zero after cleanup. Email, Stripe, Spotify, webhook, messaging, OCR, and phone credentials were absent; all provider behavior was synthetic or refused before use, so no external provider/webhook/email/payment side effect or deployment occurred.
- Deterministic outreach delivery projection successor `51373ef` adds nullable selected-event priority, provider occurrence time, and Svix identity to each recipient without inventing provenance for legacy status. Migration 111 accepts initialization only from an exact persisted event strictly above the legacy status tier, then requires every replacement to be lexicographically greater by priority, occurrence time, and Svix ID while its materialized status/error exactly matches that event. Public ingress and the delivery service both require the same bounded canonical ASCII Svix grammar, making the runtime code-unit and PostgreSQL `C`-collation tie-break identical. Once provenance exists, unchanged-tuple status/error rewrites fail at PostgreSQL except the explicit independent `REPLIED` projection with non-null reply time and preserved delivery error. Every authenticated event still persists once and bounce, suppression, contact, and campaign-settlement effects remain independent of which event wins the projection. Jim required the shared Svix ordering and explicit unchanged-provenance lifecycle, then approved the corrected six-file diff with no remaining actionable correctness, ordering, migration, or coverage finding.
- Fresh disposable PostgreSQL 17 validation applied all 111 migrations with current status and passed the focused outreach suites at 3 suites / 68 tests with open-handle detection. The matrix covers both sequential orders and concurrent `DELIVERED`/`FAILED` and `BOUNCED`/`SUPPRESSED` conflicts, occurrence-time and Svix-ID ties, legacy authority, higher/lower priority, duplicate persistence, independent side effects, campaign settlement, non-ASCII refusal, forged evidence, unchanged-tuple rewrite refusal, and the explicit reply transition. The full integration-enabled non-live regression passed at 131 suites / 1,292 tests, and the separately served isolated local live boundary passed at 7/7 after synthetic seller cleanup. Prisma format/validation/generation/status, TypeScript, targeted lint, full lint with 0 errors and 582 existing warnings, default and migration-enabled isolated-preview production builds, source diff checks, private no-store primary health, private unauthenticated staging-state refusal, unauthenticated outreach-administration refusal, invalid-unsubscribe refusal, and private no-store disabled-webhook refusal passed. Sellers, outreach campaigns, contacts, recipients, events, orders, payments, and payouts were zero after cleanup. Provider credentials were absent and no provider/webhook/email/payment call, deployment, production/live access, or external side effect occurred.
- Reply-lifecycle preservation successor `b1d3434` keeps an already materialized `REPLIED` lifecycle visible while later exact delivery evidence advances its independently selected priority, occurrence time, Svix identity, and error detail. PostgreSQL permits this exception only when both old and new states remain `REPLIED` and the exact non-null reply timestamp is unchanged; legacy replies and replies with existing delivery provenance are both covered. Same-provenance status rewrites and forged exact-event advances that replace reply time remain refused. Jim identified the initial missing reply-time equality, reviewed the corrected migration/runtime/PostgreSQL matrix, and approved it with no remaining actionable reply-lifecycle, provenance, concurrency, or coverage finding.
- Fresh disposable PostgreSQL 17 validation applied all 112 migrations with current status and passed the focused outreach claim/delivery suite at 22/22 with open-handle detection. The integration-enabled non-live regression passed at 131 suites / 1,293 tests, and the separately served isolated local live boundary passed at 7/7 after its synthetic seller fixture was removed. Prisma format, generation, migration status, TypeScript, targeted lint, full lint with 0 errors and 582 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, and source diff checks passed. Local smoke returned healthy application and private no-store primary health, private unauthenticated staging-state refusal, unauthenticated outreach-administration refusal, invalid-unsubscribe refusal, and private no-store disabled-webhook refusal. Sellers, outreach campaigns, contacts, recipients, events, quarantines, orders, payments, and payouts were zero after cleanup. Provider credentials were absent; no provider, webhook, email, payment, deployment, production/live access, or external side effect occurred.
- Inbound-reply boundary hardening shares the delivery webhook's bounded signed-ingress reader and now refuses oversized or stalled bodies, malformed UTF-8, overlong or noncanonical Svix headers, malformed tracked envelopes, and unbounded recipient lists before database or provider work. The subsequent received-email lookup uses the fixed Resend origin with redirects refused, one fetch/body deadline, declared and streamed response caps, JSON media and fatal UTF-8 validation, exact returned-email identity binding, bounded nullable stored fields, and bounded attachment metadata reduced to a count. Every retrieval failure remains canonical and precedes reply, recipient, and contact writes; optional post-commit forwarding remains a separately deferred lifecycle.
- Fresh disposable PostgreSQL 17 validation applied all 112 migrations with current status and passed the focused inbound and delivery webhook boundary at 3 suites / 52 tests. The integration-enabled non-live regression passed at 133 suites / 1,323 tests, and the separately served isolated local live boundary passed at 7/7 after its synthetic seller fixture was removed. Prisma format/validation/generation/status, TypeScript, targeted lint, full lint with 0 errors and 582 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, and source diff checks passed. Local smoke returned healthy application/database state, private no-store primary health, private unauthenticated staging-state refusal, and private no-store disabled outbound and inbound outreach webhook refusals. Outreach campaigns, contacts, recipients, replies, delivery events, quarantines, orders, payments, payouts, and sellers were zero after cleanup. Provider credentials were absent; all provider behavior was synthetic or refused before use, and no provider, webhook, forwarding, email, payment, deployment, production/live access, or external side effect occurred. Jim reviewed the bounded provider correction and approved the final boundary and coverage with no remaining actionable finding.
- Durable reply-forwarding successor adds migration 113 and replaces the inline Resend SDK passthrough with an atomic reply-owned intent and a bounded text-only notification. PostgreSQL recomputes the canonical SHA-256 idempotency identity, freezes the exact reply source and forwarding envelope, owns due/lease/dispatch/completion clocks, rejects forged projections and deletion, requires exact pending/attempt-zero origin, binds each terminal state to its allowed evidence class, permits safe expired pre-dispatch reclaim, and sends expired post-dispatch work to reconciliation without replay. The post-commit drainer and authenticated scheduler use deterministic 100-row recovery bounds, exact configuration revalidation, pre-dispatch serialized-request byte authorization, one fenced provider dispatch, fixed-origin bounded request/response handling, and stable terminal failure versus ambiguous-outcome evidence. The administrator readiness signal reuses the same validated destination and sender predicate as intent creation, so malformed forwarding configuration cannot appear ready. Oversized local envelopes quarantine at attempt zero without provider contact; duplicate webhooks and concurrent drains converge on one provider call. The notification explicitly states that it is not MIME passthrough: the bounded HTML body remains in the local reply record, attachment metadata is reduced to a count, and neither HTML-only content nor attachment bytes are forwarded; exact passthrough fidelity remains outside this bounded successor.
- Fresh disposable PostgreSQL 17 validation applied all 113 migrations with current status and passed the focused outreach forwarding, ingress, provider, ordering, and scheduler boundary at 8 suites / 143 tests with open-handle detection. The integration-enabled non-live regression passed at 135 suites / 1,362 tests, and the separately served isolated local live boundary passed at 7/7 after its synthetic seller fixture was removed. Prisma format/validation/generation/status, TypeScript, targeted lint, full lint with 0 errors and 582 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, source diff checks, local application/database health, private unauthenticated primary/staging refusals, and private no-store disabled outbound/inbound outreach webhook refusals passed. Outreach forwarding intents, replies, recipients, contacts, campaigns, orders, payments, payouts, and sellers were zero after cleanup. All provider behavior was synthetic or refused before use; credentials were absent, and no provider, webhook, forwarding, email, payment, deployment, production/live access, or external side effect occurred.
- Reply-forward lease-clock successor adds migration 114 to replace session-timezone-dependent forwarding clocks with PostgreSQL's UTC statement clock, make every `updatedAt` database-owned, require a strictly live owned lease for provider dispatch and every ordinary provider result, and reserve `FORWARD_CLAIM_EXPIRED_AFTER_DISPATCH` for the exact expired dispatched-claim recovery transition. Runtime claim, dispatch, settlement, and recovery predicates use the same clock; successful forwarding projects the exact returned completion clock onto `OutreachReply.forwardedAt`. A delayed provider acceptance or explicit rejection can no longer materialize delivery, failure, or ordinary reconciliation after ownership expires, and the recovery scheduler quarantines that ambiguous dispatch without replay. The genuine 113-to-114 fixture preserves live and expired attempt-zero and attempt-one rows byte-for-byte through the forward migration, proves both sides of the live/expired clock partition, and separately proves non-UTC sessions cannot forge origin, lease, dispatch, update, or completion clocks.
- Fresh disposable PostgreSQL 17 validation applied all 114 migrations with current status and passed the focused forwarding, ingress, webhook, and scheduler boundary at 4 suites / 59 tests with open-handle detection; the final migration-specific suite passed 20/20 after review corrections. A final test-only successor completed the 113-to-114 transition matrix with an explicit expired attempt-one `FAILED` refusal; its focused forward-upgrade test passed 1/1 on a fresh 115-migration database. The integration-enabled non-live regression passed at 135 suites / 1,367 tests, and the separately served isolated local live boundary passed at 7/7 after its synthetic seller fixture was removed. Prisma validation/generation/status, TypeScript, targeted lint, full lint with 0 errors and 582 existing warnings, the default production build, the migration-enabled isolated-preview deployment-path build, diff checks, local application/database health, disabled staging-console and inbound-webhook refusals, and zero-row financial/outreach cleanup checks passed. The protected isolated-preview deployment `dpl_5ED1JsLoMZfDFqdrGyfkDjDzjxXh` reached Ready, applied migration 114 only to the verified `primary_preview` database, and passed authenticated Vercel health, unauthenticated staging-session, and private no-store disabled inbound-webhook smoke checks. Provider credentials were absent from local validation, all provider behavior was synthetic or refused before use, and no email, webhook, forwarding, payment, payout, production/live-domain, or real-data side effect occurred. Jim reviewed the lease fence, UTC clock ownership, runtime races, forward-upgrade evidence, and converse recovery proofs and approved the successor for documentation, exact commit, and clean staging push.

### Spotify artist-import refresh wiring completion record

This section originated as the documentation-only contract for a later separately reviewed replacement. That replacement is now complete: the live artist route uses `readSpotifyArtistSnapshot` and `importSpotifyArtistSnapshot`, the durable refresh-command and committed credential-acquisition boundaries authorize provider reads, and the legacy in-transaction artist path has been retired. The following items remain the implemented boundary and retained invariants, as recorded by the completed implementation and validation notes immediately above.

1. **Route guard and committed identity snapshot.** The existing route-level authentication guard remains the first boundary for GET and POST. POST may parse, deduplicate, freeze, and bound its client selection before Tx1, but no provider, candidate, match, notification, or email envelope is prepared there. After the guard, a short Serializable Tx1 locks the current User and revalidates that exact row as email-verified, phone-verified, non-banned, and ordinary (no managed seller or administrator persona), then locks the exact current Spotify `ConnectedAccount` owned by that User. For an unexpired credential, Tx1 returns the one exact bounded credential snapshot that can authorize the next artist read: user id, connection id/generation, Spotify provider, provider account id, the current encrypted access-token ciphertext, database-generated version digest covering that same locked credential projection, and credential expiry. For an expired credential it returns the same non-secret identity/version/expiry primitives plus `refreshRequired`, but its obsolete ciphertext is not usable. Tx1 never returns a Prisma model, transaction client, refresh credential, command row, plaintext token, or mutable object. Tx1 must successfully commit and its wrapper must resolve before access-token decryption, refresh-service invocation, Spotify or catalog I/O, or any provider/candidate/match/notification/email envelope preparation. A callback success followed by rollback, including `P2034`, performs zero refresh, Spotify, catalog, or email I/O.
2. **Credential-acquisition boundary and durable replay.** Every artist-provider read is authorized by exactly one committed credential snapshot with ciphertext and version evidence from the same locked `ConnectedAccount`; decryption never obtains authority by itself. The unexpired snapshot from Tx1 may be decrypted only after Tx1 commits, and any missing ciphertext or ciphertext/version inconsistency fails closed with zero artist reads. An expired snapshot may invoke only `executeSpotifyRefreshCommand`, outside Tx1. After every `SUCCEEDED` result, whether it is the winning invocation with a transient access token or a terminal replay without one, the caller must take a fresh short Serializable acquisition transaction: lock and revalidate User, lock the exact current connection, lock or read the exact succeeded command as required by the established order, prove ordinary verified access and exact Spotify provider, current `currentRefreshCommandId`, `SUCCEEDED` command ownership, connection id/generation, provider account id, command id, and command result-version digest equal to the database-generated digest of the current linked credential projection, then return that projection's current encrypted access-token ciphertext, authorized result digest, and bounded connection/provider/expiry primitives. That fresh transaction must commit before decryption or artist I/O and is the sole post-refresh authority. The transient winner token is always ignored for artist reads; it cannot substitute for or bypass reacquisition of the committed current projection. A durable `ATTEMPTING` command is surfaced by the service as the existing stable `IN_PROGRESS` no-provider/no-retry outcome; `RECONNECT_REQUIRED` is the existing stable reconnect no-provider/no-retry outcome; `NO_CONNECTION` remains disconnected. Those outcomes perform zero artist reads, and no path may automatically retry them, use the pre-refresh credential, fall back to the legacy refresh helper, or resend an ambiguous or terminal command.
3. **Post-commit provider and catalog reads.** Refresh-service invocation, access-token decryption, followed/top-artist reads, response parsing, deduplication, catalog reads, and provider-to-catalog matching occur outside every database transaction. Immediately before any acquired credential is used, its primitives must still be the exact acquisition result; provider, connection/generation, current owner where applicable, and result-version mismatches fail closed rather than selecting another account or credential. Spotify requests require bounded connect/body timeouts, bounded response bodies and page counts, explicit status/result validation, and no provider-derived error text in logs or client responses. Partial suppression of one Spotify branch is forbidden: either followed-artists or top-artists failure invalidates the whole snapshot. Provider and catalog data reduce outside Tx2 to a bounded immutable envelope containing only the exact authorization snapshot and normalized primitive candidate/match fields; raw bodies, tokens, database records, and unbounded provider strings do not enter Tx2.
4. **Exact Tx2 revalidation.** Before GET returns or POST writes, a short Serializable Tx2 locks User first and the exact connection second and revalidates the full verified, non-banned ordinary-user predicate plus exact connection id/generation, Spotify provider, current owner when the credential came from refresh, provider account id, and database-generated credential/result-version digest that authorized the reads. Disconnect/reconnect, refresh or terminal replay to a different result, OAuth replacement, token rotation, source/current-owner drift, provider-account switch, ban, verification loss, or managed-persona restoration fails closed. A successful refresh advances the authorized snapshot to its committed result version before any artist read; Tx2 never accepts the obsolete pre-refresh digest. Tx2 contains no refresh, Spotify, catalog, decryption, or other provider I/O.
5. **GET result boundary.** GET first reduces all provider/catalog results to bounded response primitives, then returns them only after read-only Tx2 succeeds. Tx2 performs no writes and no provider/catalog reads. Serialization failure, `P2034`, source/current-owner drift, disconnect/reconnect, or authorization drift returns the existing stable private no-store retry/reconnect/refusal contract as appropriate; it never returns an empty or partial list as disguised success and never exposes connection, command, credential, or provider evidence.
6. **POST atomicity and post-commit email.** Before Tx1, the request boundary only parses, deduplicates, freezes, and enforces explicit count/length/type bounds on selected Spotify ids and `includeUnmatched`; unknown fields or hostile nesting cannot enlarge later work. Provider reads and all candidate/match envelope preparation occur only after Tx1 or the post-refresh acquisition transaction commits. Outside Tx2, the already bounded candidate/match envelope is frozen and filtered against that exact client selection. Tx2 performs no provider/catalog read and atomically writes only that locked user's imports/preferences and unmatched catalog requests derived from the frozen envelope. A validation failure, authorization or source/current-owner drift, disconnect/reconnect, rollback, or `P2034` commits none of those writes. Only after Tx2 commits may the route build committed response and notification primitives or attempt unmatched-request admin email. That email envelope contains only committed request ids and bounded committed artist names, preserves the existing request-id-derived idempotency identity and outward email contract, and remains independently contained so provider rejection cannot change the committed HTTP result or database state.
7. **Reconnect-generation compatibility.** A disconnect may remove only the locked current connection and clear its current refresh ownership as required by the refresh-command schema; historical command rows remain. A reconnect creates a new connection id and generation. When a current refresh owner exists, the OAuth callback must not mutate or replace that linked credential projection in place; it must create the new generation or use a separately designed and reviewed authorized-replacement transition. Neither a historical success/current-owner link nor a candidate envelope from the old generation may authorize reads or writes against the new connection, even when provider account id and user id are unchanged.
8. **Retained proof boundary.** Deterministic unit and disposable-PostgreSQL coverage must assert transaction-callback completion, commit, provider/catalog call, Tx2, and email ordering; rollback and `P2034` at Tx1 and Tx2; and zero refresh/Spotify/catalog/email I/O from any transaction callback or failed Tx1. It must also prove simultaneous-refresh convergence to one provider caller; durable `ATTEMPTING` replay and every terminal replay; `SUCCEEDED` replay without a returned token and exact linked-projection reacquisition; credential rotation, callback replacement, disconnect, or reconnect between refresh completion and that fresh snapshot causes zero artist reads; ciphertext/version absence or mismatch is refused before decryption/provider use; stable no-provider/no-retry handling for in-progress and reconnect outcomes; persona, ban, verification, provider, source, current-owner, result-version, disconnect/reconnect, and callback-replacement drift; full failure on either Spotify branch; hostile request and provider-payload bounds; GET read-only drift refusal; POST all-or-nothing writes scoped to the exact user; and post-commit notification using only committed request ids/names. Private/no-store headers, existing status and error meanings, idempotency contracts, and the independently contained email contract must remain unchanged. Provider fetches and email remain mocked and credentials remain absent in validation. The original documentation checkpoint authorized no executable change; the subsequent separately reviewed implementation successors completed the route wiring, schema/runtime boundary, and legacy-path retirement without any live-provider or deployment action.

### Staging-only synthetic buyer purchase and admission journey

- The isolated `/staging/primary` console can seed and advance a fresh synthetic generation through inventory hold, immutable order pricing, local payment preparation, synthetic provider attachment/success, admission credential evidence, and accepted admission.
- Each click performs one durable PostgreSQL transition and exposes the resulting state. All identities are reserved synthetic values; the provider and credential identifiers are explicitly synthetic and never usable outside the isolated preview.
- The journey performs no Stripe SDK call, email, webhook, payout, refund, cron, public sale, or live-data access. Prior generations remain intact for auditability.
- Every advance revalidates the complete reserved buyer and organizer fixture under the journey advisory lock. Any capability, role, contact, ownership, payment-account, or business-identity drift fails closed before purchase or admission mutation; an explicit admin reseed restores only the reserved synthetic fixture and creates a new generation.
- The current generation's event and ticket type are also revalidated as exact deterministic synthetic roots before a hold can be created. Event identity/contact/schedule/policy/capacity/approval drift or ticket inventory/limit/currency/price drift fails closed without purchase mutation; reseed preserves the drifted generation as evidence and creates a clean successor.
- Once a hold exists, its tenant, event, ticket type, buyer, quantity, lifecycle timestamps, and idempotency evidence are revalidated before order creation or any later transition. Reservation drift fails closed without creating an order.
- A committed synthetic reservation must follow its creation timestamp and retain the exact two-minute reconciliation deadline established during payment preparation. Commitment-clock drift fails before provider attachment.
- An uncommitted synthetic hold must still be live, and commitment must occur no later than both its immutable expiry and the current transaction clock. Expired-hold advancement, late commitment, or future-dated commitment fails before provider attachment.
- The reservation commitment and order payment-processing timestamps must remain identical evidence of their single atomic preparation transition. Cross-record clock divergence fails before provider attachment.
- Once an order exists, its tenant/buyer/reservation bindings, lifecycle, totals, currency, idempotency key, sole line snapshot, and exact face-value/organizer-fee components are revalidated before payment preparation. Order, line, and component creation evidence must follow the reserved hold and cannot be future-dated. Snapshot or chronology drift fails closed without committing the hold or creating a payment attempt.
- Admission validation binds the deterministic ticket and credential to the authoritative paid lifecycle and current transaction clock. Issuance before payment or in the future, and future-dated accepted scans, fail closed before any new scan or check-in mutation.
- The synthetic payment chain requires an empty provider-event and exception history. Unexpected webhook-like provenance or payment-exception evidence fails closed before admission issuance or check-in.
- Payment-attempt creation must follow the order's authoritative preparation timestamp without being future-dated relative to the transaction clock, and synthetic provider attachment must likewise follow attempt creation without being future-dated. Chronology drift fails before provider attachment, payment success, or admission mutation.
- Buyer-journey advance retries a bounded five times on PostgreSQL serialization aborts before returning a stable retry-exhausted rejection. A failed retry commits no purchase transition, and a recovered retry advances exactly once under the existing advisory lock.

### Refund, cancellation, and revocation design gate

- Refund Design Revision 1 establishes one authoritative local-commit-before-provider sequence for both payment and refund work. Refund financial states remain separate from the implemented `ISSUED | VOIDED | CHECKED_IN` admission lifecycle; eligible unscanned tickets become terminally `VOIDED` before any refund provider call and never resurrect after failure or ambiguity.
- Provider-attempt design distinguishes proven `NOT_SENT`, `SEND_UNCERTAIN`, attached, terminal-failed, and succeeded outcomes. Uncertain/attached attempts must reconcile or retry with the identical provider identity; a new identity is forbidden until authenticated evidence proves the prior attempt incapable of later success.
- Refund Design Revision 2 separates attempt failure from parent finality: the parent remains `PROVIDER_PENDING` while an authenticated terminal-failed attempt is eligible for reviewed retry. A serializable authorization creates at most one next attempt; parent `FAILED` requires explicit final abandonment, and `SUCCEEDED`, `FAILED`, or `CANCELLED_BEFORE_PROVIDER` parents reject all later attempts and state changes.
- Event cancellation uses a small atomic activation/freeze transaction followed by bounded idempotent batches keyed to an immutable cancellation generation. Admission fails closed from activation, and resolution requires exact count/amount coverage plus resolved obligations.
- A future isolated migration must deterministically materialize existing synthetic orders' per-ticket allocations from immutable snapshots, prove exact reconciliation, and fail closed on incomplete/ambiguous history before refund commands can be enabled.
- No refund schema, migration, service, Stripe refund call, ledger entry, route, UI, or runtime behavior is implemented by this revision.
- Implementation remains blocked on merchant-of-record responsibility, refundable component/tax/processor-fee policy, checked-in refunds, cancellation/insurance obligations, reserves and negative balances, inventory resale, chargebacks, accounting/tax documentation, privacy retention, and operational reconciliation ownership.

- The preflight depends on explicit environment configuration and database naming. Deployment configuration remains intentionally absent until an isolated preview is separately authorized.
- Event authoring remains pre-publication only. Event publication, discoverability, purchasability, DST-to-instant resolution, ticketing, and operational venue normalization remain deferred.
- Audit snapshots use a deliberately small allowlist. Each later domain must explicitly add safe fields rather than persisting arbitrary request objects.
- The outbox foundation stores delivery intent only. No dispatcher, email, webhook, or external side effect is implemented.
- CRUD endpoints remain out of scope. Invitation acceptance is service-only and enforces authenticated ownership of the normalized verified email.
- Service inputs assume the route boundary has authenticated the supplied actor identity; the service independently reloads the user to enforce current role, verified email, and ban state. No route is included in this milestone.
- Invitation delivery is intentionally absent. The raw invitation token is returned once to a future approved delivery boundary and is never persisted or audited.
- Organizer rejection reasons and operational review policy remain product/operations decisions. The service requires a reason but does not expose a workflow or send notifications.
- Event categories remain normalized free text pending an approved product taxonomy. No category drives public navigation or policy behavior.
- Venue data is a denormalized draft snapshot for this milestone and remains editable only in allowed draft states; venue deduplication and a reusable `PrimaryVenue` entity remain deferred.
- Draft policy text is not buyer-facing and has no legal effect. Legal must approve the final refund/cancellation/terms structure before publication.

### Append-only audit enforcement before real data

The current synthetic-only foundation treats `PrimaryAuditEvent` rows as append-only by service convention; Prisma's application role can still technically update or delete them. Before any real organizer or attendee data is permitted, deployment must use a dedicated PostgreSQL application role with `SELECT` and `INSERT` on `PrimaryAuditEvent`, no `UPDATE`, `DELETE`, or `TRUNCATE`, and no table-owner or bypass-RLS privileges. Migrations must run under a separate owner role. A database integration test must prove the runtime role can insert but receives a permission error for update, delete, and truncate. The same test and grants must be release-gate evidence; synthetic development may continue until then.

## Explicit exclusions

Outside the fail-closed isolated-preview consoles documented above, this implementation contains no organizer/event/scanner UI or route, external email delivery, event publication/discovery, QR presentation, offline scanning, transfer/reissue, real refund execution, ledger, settlement, report, public navigation, real data, or production deployment functionality. Payment, admission, refund, and cancellation remain isolated local service/test foundations; no client secret or bearer credential is delivered through a public surface.
